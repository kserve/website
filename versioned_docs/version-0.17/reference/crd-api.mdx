---
title: Control Plane API
toc_min_heading_level: 2
toc_max_heading_level: 4
---


## serving.kserve.io/v1alpha1

Package v1alpha1 contains API Schema definitions for the serving v1alpha1 API group


## Resource Kinds

### Available Kinds
- [ClusterServingRuntime](#clusterservingruntime)
- [ClusterServingRuntimeList](#clusterservingruntimelist)
- [ClusterStorageContainer](#clusterstoragecontainer)
- [ClusterStorageContainerList](#clusterstoragecontainerlist)
- [InferenceGraph](#inferencegraph)
- [InferenceGraphList](#inferencegraphlist)
- [LLMInferenceService](#llminferenceservice)
- [LLMInferenceServiceConfig](#llminferenceserviceconfig)
- [LLMInferenceServiceConfigList](#llminferenceserviceconfiglist)
- [LLMInferenceServiceList](#llminferenceservicelist)
- [LocalModelCache](#localmodelcache)
- [LocalModelCacheList](#localmodelcachelist)
- [LocalModelNode](#localmodelnode)
- [LocalModelNodeGroup](#localmodelnodegroup)
- [LocalModelNodeGroupList](#localmodelnodegrouplist)
- [LocalModelNodeList](#localmodelnodelist)
- [ServingRuntime](#servingruntime)
- [ServingRuntimeList](#servingruntimelist)
- [TrainedModel](#trainedmodel)
- [TrainedModelList](#trainedmodellist)

### Kind Definitions
#### ClusterServingRuntime



**Appears in:**
- [ClusterServingRuntimeList](#clusterservingruntimelist)

ClusterServingRuntime is the Schema for the servingruntimes API

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>ClusterServingRuntime</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[ServingRuntimeSpec](#servingruntimespec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[ServingRuntimeStatus](#servingruntimestatus)"
  required="true"
  description=""
/>


#### ClusterServingRuntimeList




ClusterServingRuntimeList contains a list of ServingRuntime

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>ClusterServingRuntimeList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[ClusterServingRuntime](#clusterservingruntime) array"
  required="true"
  description=""
/>


#### ClusterStorageContainer



**Appears in:**
- [ClusterStorageContainerList](#clusterstoragecontainerlist)



##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>ClusterStorageContainer</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[StorageContainerSpec](#storagecontainerspec)"
  required="true"
  description=""
/><ApiField
  name="disabled"
  type="boolean"
  required="false"
  description=""
/>


#### ClusterStorageContainerList






##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>ClusterStorageContainerList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[ClusterStorageContainer](#clusterstoragecontainer) array"
  required="true"
  description=""
/>


#### InferenceGraph



**Appears in:**
- [InferenceGraphList](#inferencegraphlist)

InferenceGraph is the Schema for the InferenceGraph API for multiple models

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>InferenceGraph</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[InferenceGraphSpec](#inferencegraphspec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[InferenceGraphStatus](#inferencegraphstatus)"
  required="true"
  description=""
/>


#### InferenceGraphList




InferenceGraphList contains a list of InferenceGraph

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>InferenceGraphList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[InferenceGraph](#inferencegraph) array"
  required="true"
  description=""
/>


#### LLMInferenceService



**Appears in:**
- [LLMInferenceServiceList](#llminferenceservicelist)

LLMInferenceService is the Schema for the llminferenceservices API, representing a single LLM deployment.
It orchestrates the creation of underlying Kubernetes resources like Deployments and Services,
and configures networking for exposing the model.

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LLMInferenceService</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[LLMInferenceServiceSpec](#llminferenceservicespec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[LLMInferenceServiceStatus](#llminferenceservicestatus)"
  required="true"
  description=""
/>


#### LLMInferenceServiceConfig



**Appears in:**
- [LLMInferenceServiceConfigList](#llminferenceserviceconfiglist)

LLMInferenceServiceConfig is the Schema for the llminferenceserviceconfigs API.
It acts as a template to provide base configurations that can be inherited by multiple LLMInferenceService instances.

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LLMInferenceServiceConfig</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[LLMInferenceServiceSpec](#llminferenceservicespec)"
  required="true"
  description=""
/>


#### LLMInferenceServiceConfigList




LLMInferenceServiceConfigList is the list type for LLMInferenceServiceConfig.

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LLMInferenceServiceConfigList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[LLMInferenceServiceConfig](#llminferenceserviceconfig) array"
  required="true"
  description=""
/>


#### LLMInferenceServiceList




LLMInferenceServiceList is the list type for LLMInferenceService.

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LLMInferenceServiceList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[LLMInferenceService](#llminferenceservice) array"
  required="true"
  description=""
/>


#### LocalModelCache



**Appears in:**
- [LocalModelCacheList](#localmodelcachelist)

LocalModelCache

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LocalModelCache</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[LocalModelCacheSpec](#localmodelcachespec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[LocalModelCacheStatus](#localmodelcachestatus)"
  required="true"
  description=""
/>


#### LocalModelCacheList




LocalModelCacheList

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LocalModelCacheList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[LocalModelCache](#localmodelcache) array"
  required="true"
  description=""
/>


#### LocalModelNode



**Appears in:**
- [LocalModelNodeList](#localmodelnodelist)



##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LocalModelNode</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[LocalModelNodeSpec](#localmodelnodespec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[LocalModelNodeStatus](#localmodelnodestatus)"
  required="true"
  description=""
/>


#### LocalModelNodeGroup



**Appears in:**
- [LocalModelNodeGroupList](#localmodelnodegrouplist)



##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LocalModelNodeGroup</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[LocalModelNodeGroupSpec](#localmodelnodegroupspec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[LocalModelNodeGroupStatus](#localmodelnodegroupstatus)"
  required="true"
  description=""
/>


#### LocalModelNodeGroupList






##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LocalModelNodeGroupList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[LocalModelNodeGroup](#localmodelnodegroup) array"
  required="true"
  description=""
/>


#### LocalModelNodeList






##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LocalModelNodeList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[LocalModelNode](#localmodelnode) array"
  required="true"
  description=""
/>


#### ServingRuntime



**Appears in:**
- [ServingRuntimeList](#servingruntimelist)

ServingRuntime is the Schema for the servingruntimes API

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>ServingRuntime</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[ServingRuntimeSpec](#servingruntimespec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[ServingRuntimeStatus](#servingruntimestatus)"
  required="true"
  description=""
/>


#### ServingRuntimeList




ServingRuntimeList contains a list of ServingRuntime

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>ServingRuntimeList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[ServingRuntime](#servingruntime) array"
  required="true"
  description=""
/>


#### TrainedModel



**Appears in:**
- [TrainedModelList](#trainedmodellist)

TrainedModel is the Schema for the TrainedModel API

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>TrainedModel</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[TrainedModelSpec](#trainedmodelspec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[TrainedModelStatus](#trainedmodelstatus)"
  required="true"
  description=""
/>


#### TrainedModelList




TrainedModelList contains a list of TrainedModel

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>TrainedModelList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[TrainedModel](#trainedmodel) array"
  required="true"
  description=""
/>


## Supporting Types

### Available Types
- [ActuatorSpec](#actuatorspec)
- [BuiltInAdapter](#builtinadapter)
- [Criticality](#criticality)
- [GatewayRoutesSpec](#gatewayroutesspec)
- [GatewaySpec](#gatewayspec)
- [HPAScalingSpec](#hpascalingspec)
- [HTTPRouteSpec](#httproutespec)
- [InfereceGraphRouterTimeouts](#inferecegraphroutertimeouts)
- [InferenceGraphSpec](#inferencegraphspec)
- [InferenceGraphStatus](#inferencegraphstatus)
- [InferenceGraphValidator](#inferencegraphvalidator)
- [InferencePoolSpec](#inferencepoolspec)
- [InferenceRouter](#inferencerouter)
- [InferenceRouterType](#inferenceroutertype)
- [InferenceStep](#inferencestep)
- [InferenceStepDependencyType](#inferencestepdependencytype)
- [InferenceTarget](#inferencetarget)
- [IngressSpec](#ingressspec)
- [KEDAScalingSpec](#kedascalingspec)
- [LLMInferenceServiceConfigValidator](#llminferenceserviceconfigvalidator)
- [LLMInferenceServiceSpec](#llminferenceservicespec)
- [LLMInferenceServiceStatus](#llminferenceservicestatus)
- [LLMInferenceServiceValidator](#llminferenceservicevalidator)
- [LLMModelSpec](#llmmodelspec)
- [LoRASpec](#loraspec)
- [LocalModelCacheSpec](#localmodelcachespec)
- [LocalModelCacheStatus](#localmodelcachestatus)
- [LocalModelInfo](#localmodelinfo)
- [LocalModelNodeGroupSpec](#localmodelnodegroupspec)
- [LocalModelNodeGroupStatus](#localmodelnodegroupstatus)
- [LocalModelNodeSpec](#localmodelnodespec)
- [LocalModelNodeStatus](#localmodelnodestatus)
- [ModelCopies](#modelcopies)
- [ModelSpec](#modelspec)
- [ModelStatus](#modelstatus)
- [NamespacedName](#namespacedname)
- [NodeStatus](#nodestatus)
- [ParallelismSpec](#parallelismspec)
- [RouterSpec](#routerspec)
- [ScaleMetric](#scalemetric)
- [ScalingSpec](#scalingspec)
- [SchedulerConfigSpec](#schedulerconfigspec)
- [SchedulerSpec](#schedulerspec)
- [ServerType](#servertype)
- [ServingRuntimePodSpec](#servingruntimepodspec)
- [ServingRuntimeSpec](#servingruntimespec)
- [ServingRuntimeStatus](#servingruntimestatus)
- [StorageContainerSpec](#storagecontainerspec)
- [StorageHelper](#storagehelper)
- [StorageInitializerSpec](#storageinitializerspec)
- [SupportedModelFormat](#supportedmodelformat)
- [SupportedRuntime](#supportedruntime)
- [SupportedUriFormat](#supporteduriformat)
- [TrainedModelSpec](#trainedmodelspec)
- [TrainedModelStatus](#trainedmodelstatus)
- [TrainedModelValidator](#trainedmodelvalidator)
- [UntypedObjectReference](#untypedobjectreference)
- [WVASpec](#wvaspec)
- [WorkerSpec](#workerspec)
- [WorkloadSpec](#workloadspec)
- [WorkloadType](#workloadtype)

### Type Definitions
#### ActuatorSpec



**Appears in:**
- [WVASpec](#wvaspec)

ActuatorSpec defines the autoscaling actuator backend for WVA.
Exactly one of HPA or KEDA must be specified.

##### Fields



<ApiField
  name="hpa"
  type="[HPAScalingSpec](#hpascalingspec)"
  required="false"
  description="HPA configures the HorizontalPodAutoscaler as the actuator backend.<br />When specified, HPA reads the wva_desired_replicas metric via the Kubernetes Metrics API<br />(requires Prometheus Adapter) and scales the deployment accordingly.<br />Mutually exclusive with KEDA."
/><ApiField
  name="keda"
  type="[KEDAScalingSpec](#kedascalingspec)"
  required="false"
  description="KEDA configures a KEDA ScaledObject as the actuator backend.<br />When specified, KEDA queries Prometheus directly for the wva_desired_replicas metric<br />and scales the deployment accordingly. KEDA does not require a Prometheus Adapter.<br />Mutually exclusive with HPA."
/>


#### BuiltInAdapter



**Appears in:**
- [ServingRuntimeSpec](#servingruntimespec)



##### Fields



<ApiField
  name="serverType"
  type="[ServerType](#servertype)"
  required="true"
  description="ServerType must be one of the supported built-in types such as &quot;triton&quot; or &quot;mlserver&quot;,<br />and the runtime's container must have the same name"
/><ApiField
  name="runtimeManagementPort"
  type="integer"
  required="true"
  description="Port which the runtime server listens for model management requests"
/><ApiField
  name="memBufferBytes"
  type="integer"
  required="true"
  description="Fixed memory overhead to subtract from runtime container's memory allocation to determine model capacity"
/><ApiField
  name="modelLoadingTimeoutMillis"
  type="integer"
  required="true"
  description="Timeout for model loading operations in milliseconds"
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="true"
  description="Environment variables used to control other aspects of the built-in adapter's behaviour (uncommon)"
/>


#### Criticality

**Underlying type:** string

**Appears in:**
- [LLMModelSpec](#llmmodelspec)

Criticality defines how important it is to serve the model compared to other models.
Criticality is intentionally a bounded enum to contain the possibilities that need to
be supported by the load balancing algorithm.



##### Possible Values

<ApiField
  name="Critical"
  type="enum"
  required="false"
  description="Critical - Requests to this model should be shed last.<br />"
/><ApiField
  name="Standard"
  type="enum"
  required="false"
  description="Standard - Requests to this model will be queued or shed before critical traffic.<br />"
/><ApiField
  name="Sheddable"
  type="enum"
  required="false"
  description="Sheddable - Requests to this model should be shed before critical and standard traffic.<br />"
/>
#### GatewayRoutesSpec



**Appears in:**
- [RouterSpec](#routerspec)

GatewayRoutesSpec defines the configuration for a Gateway API route.

##### Fields



<ApiField
  name="http"
  type="[HTTPRouteSpec](#httproutespec)"
  required="false"
  description="HTTP route configuration."
/>


#### GatewaySpec



**Appears in:**
- [RouterSpec](#routerspec)

GatewaySpec defines the configuration for a Gateway API Gateway.

##### Fields



<ApiField
  name="refs"
  type="[UntypedObjectReference](#untypedobjectreference) array"
  required="false"
  description="Refs provides references to existing, user-managed Gateway objects (&quot;Bring Your Own&quot; gateway).<br />The controller will use the specified Gateway instead of creating one."
/>


#### HPAScalingSpec



**Appears in:**
- [ActuatorSpec](#actuatorspec)
- [WVASpec](#wvaspec)

HPAScalingSpec configures the HorizontalPodAutoscaler behavior.
The fields are directly from the upstream Kubernetes autoscaling/v2 API.

##### Fields



<ApiField
  name="behavior"
  type="[HorizontalPodAutoscalerBehavior](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#horizontalpodautoscalerbehavior-v2-autoscaling)"
  required="false"
  description="Behavior configures the scaling behavior of the target in both Up and Down directions<br />(scaleUp and scaleDown fields respectively)."
/>


#### HTTPRouteSpec



**Appears in:**
- [GatewayRoutesSpec](#gatewayroutesspec)

HTTPRouteSpec defines configurations for a Gateway API HTTPRoute.
'Spec' and 'Refs' are mutually exclusive and determine whether the route is managed by the controller or user-managed.

##### Fields



<ApiField
  name="refs"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="Refs provides references to existing, user-managed HTTPRoute objects (&quot;Bring Your Own&quot; route).<br />The controller will validate the existence of these routes but will not modify them."
/><ApiField
  name="spec"
  type="[HTTPRouteSpec](#httproutespec)"
  required="false"
  description="Spec allows for providing a custom specification for an HTTPRoute.<br />If provided, the controller will create and manage an HTTPRoute with this specification."
/>


#### InfereceGraphRouterTimeouts



**Appears in:**
- [InferenceGraphSpec](#inferencegraphspec)



##### Fields



<ApiField
  name="serverRead"
  type="integer"
  required="false"
  description="ServerRead specifies the number of seconds to wait before timing out a request read by the server."
/><ApiField
  name="serverWrite"
  type="integer"
  required="false"
  description="ServerWrite specifies the maximum duration in seconds before timing out writes of the response."
/><ApiField
  name="serverIdle"
  type="integer"
  required="false"
  description="ServerIdle specifies the maximum amount of time in seconds to wait for the next request when keep-alives are enabled."
/><ApiField
  name="serviceClient"
  type="integer"
  required="false"
  description="ServiceClient specifies a time limit in seconds for requests made to the graph components by HTTP client."
/>


#### InferenceGraphSpec



**Appears in:**
- [InferenceGraph](#inferencegraph)

InferenceGraphSpec defines the InferenceGraph spec

##### Fields



<ApiField
  name="nodes"
  type="object (keys:string, values:[InferenceRouter](#inferencerouter))"
  required="true"
  description="Map of InferenceGraph router nodes<br />Each node defines the router which can be different routing types"
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description=""
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description=""
/><ApiField
  name="timeout"
  type="integer"
  required="false"
  description="TimeoutSeconds specifies the number of seconds to wait before timing out a request to the component."
/><ApiField
  name="routerTimeouts"
  type="[InfereceGraphRouterTimeouts](#inferecegraphroutertimeouts)"
  required="false"
  description=""
/><ApiField
  name="minReplicas"
  type="integer"
  required="false"
  description="Minimum number of replicas, defaults to 1 but can be set to 0 to enable scale-to-zero."
/><ApiField
  name="maxReplicas"
  type="integer"
  required="false"
  description="Maximum number of replicas for autoscaling."
/><ApiField
  name="scaleTarget"
  type="integer"
  required="false"
  description="ScaleTarget specifies the integer target value of the metric type the Autoscaler watches for.<br />concurrency and rps targets are supported by Knative Pod Autoscaler<br />(https://knative.dev/docs/serving/autoscaling/autoscaling-targets/)."
/><ApiField
  name="scaleMetric"
  type="[ScaleMetric](#scalemetric)"
  required="false"
  description="ScaleMetric defines the scaling metric type watched by autoscaler<br />possible values are concurrency, rps, cpu, memory. concurrency, rps are supported via<br />Knative Pod Autoscaler(https://knative.dev/docs/serving/autoscaling/autoscaling-metrics)."
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="Toleration specifies the toleration for the InferenceGraph.<br />https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/"
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector specifies the node selector for the InferenceGraph.<br />https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/"
/><ApiField
  name="nodeName"
  type="string"
  required="false"
  description="NodeName specifies the node name for the InferenceGraph.<br />https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/"
/><ApiField
  name="serviceAccountName"
  type="string"
  required="false"
  description="ServiceAccountName specifies the service account name for the InferenceGraph.<br />https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/"
/>


#### InferenceGraphStatus



**Appears in:**
- [InferenceGraph](#inferencegraph)

InferenceGraphStatus defines the InferenceGraph conditions and status

##### Fields



<ApiField
  name="observedGeneration"
  type="integer"
  required="false"
  description="ObservedGeneration is the 'Generation' of the Service that<br />was last processed by the controller."
/><ApiField
  name="conditions"
  type="[Conditions](#conditions)"
  required="false"
  description="Conditions the latest available observations of a resource's current state."
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="true"
  description="Annotations is additional Status fields for the Resource to save some<br />additional State as well as convey more information to the user. This is<br />roughly akin to Annotations on any k8s resource, just the reconciler conveying<br />richer information outwards."
/><ApiField
  name="url"
  type="[URL](#url)"
  required="false"
  description="Url for the InferenceGraph"
/><ApiField
  name="deploymentMode"
  type="string"
  required="true"
  description="InferenceGraph DeploymentMode"
/>



#### InferencePoolSpec



**Appears in:**
- [SchedulerSpec](#schedulerspec)

InferencePoolSpec defines the configuration for an InferencePool.
'Spec' and 'Ref' are mutually exclusive.

##### Fields



<ApiField
  name="spec"
  type="[InferencePoolSpec](#inferencepoolspec)"
  required="false"
  description="Spec defines an inline InferencePool specification."
/><ApiField
  name="ref"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core)"
  required="false"
  description="Ref is a reference to an existing InferencePool."
/>


#### InferenceRouter



**Appears in:**
- [InferenceGraphSpec](#inferencegraphspec)

InferenceRouter defines the router for each InferenceGraph node with one or multiple steps

```yaml
kind: InferenceGraph
metadata:

	name: canary-route

spec:

	nodes:
	  root:
	    routerType: Splitter
	    routes:
	    - service: mymodel1
	      weight: 20
	    - service: mymodel2
	      weight: 80

```

```yaml
kind: InferenceGraph
metadata:

	name: abtest

spec:

	nodes:
	  mymodel:
	    routerType: Switch
	    routes:
	    - service: mymodel1
	      condition: "{ .input.userId == 1 }"
	    - service: mymodel2
	      condition: "{ .input.userId == 2 }"

```

Scoring a case using a model ensemble consists of scoring it using each model separately,
then combining the results into a single scoring result using one of the pre-defined combination methods.

Tree Ensemble constitutes a case where simple algorithms for combining results of either classification or regression trees are well known.
Multiple classification trees, for example, are commonly combined using a "majority-vote" method.
Multiple regression trees are often combined using various averaging techniques.
e.g tagging models with segment identifiers and weights to be used for their combination in these ways.
```yaml
kind: InferenceGraph
metadata:

	name: ensemble

spec:

	nodes:
	  root:
	    routerType: Sequence
	    routes:
	    - service: feast
	    - nodeName: ensembleModel
	      data: $response
	  ensembleModel:
	    routerType: Ensemble
	    routes:
	    - service: sklearn-model
	    - service: xgboost-model

```

Scoring a case using a sequence, or chain of models allows the output of one model to be passed in as input to the subsequent models.
```yaml
kind: InferenceGraph
metadata:

	name: model-chainer

spec:

	nodes:
	  root:
	    routerType: Sequence
	    routes:
	    - service: mymodel-s1
	    - service: mymodel-s2
	      data: $response
	    - service: mymodel-s3
	      data: $response

```

In the flow described below, the pre_processing node base64 encodes the image and passes it to two model nodes in the flow.
The encoded data is available to both these nodes for classification. The second node i.e. dog-breed-classification takes the
original input from the pre_processing node along-with the response from the cat-dog-classification node to do further classification
of the dog breed if required.
```yaml
kind: InferenceGraph
metadata:

	name: dog-breed-classification

spec:

	nodes:
	  root:
	    routerType: Sequence
	    routes:
	    - service: cat-dog-classifier
	    - nodeName: breed-classifier
	      data: $request
	  breed-classifier:
	    routerType: Switch
	    routes:
	    - service: dog-breed-classifier
	      condition: { .predictions.class == "dog" }
	    - service: cat-breed-classifier
	      condition: { .predictions.class == "cat" }

```

##### Fields



<ApiField
  name="routerType"
  type="[InferenceRouterType](#inferenceroutertype)"
  required="true"
  description="RouterType<br />- `Sequence:` chain multiple inference steps with input/output from previous step<br />- `Splitter:` randomly routes to the target service according to the weight<br />- `Ensemble:` routes the request to multiple models and then merge the responses<br />- `Switch:` routes the request to one of the steps based on condition"
/><ApiField
  name="steps"
  type="[InferenceStep](#inferencestep) array"
  required="false"
  description="Steps defines destinations for the current router node"
/>


#### InferenceRouterType

**Underlying type:** string

**Appears in:**
- [InferenceRouter](#inferencerouter)

InferenceRouterType constant for inference routing types



##### Possible Values

<ApiField
  name="Sequence"
  type="enum"
  required="false"
  description="Sequence Default type only route to one destination<br />"
/><ApiField
  name="Splitter"
  type="enum"
  required="false"
  description="Splitter router randomly routes the requests to the named service according to the weight<br />"
/><ApiField
  name="Ensemble"
  type="enum"
  required="false"
  description="Ensemble router routes the requests to multiple models and then merge the responses<br />"
/><ApiField
  name="Switch"
  type="enum"
  required="false"
  description="Switch routes the request to the model based on certain condition<br />"
/>
#### InferenceStep



**Appears in:**
- [InferenceRouter](#inferencerouter)

InferenceStep defines the inference target of the current step with condition, weights and data.

##### Fields



<ApiField
  name="name"
  type="string"
  required="false"
  description="Unique name for the step within this node"
/><ApiField
  name="nodeName"
  type="string"
  required="false"
  description="The node name for routing as next step"
/><ApiField
  name="serviceName"
  type="string"
  required="true"
  description="named reference for InferenceService"
/><ApiField
  name="serviceUrl"
  type="string"
  required="false"
  description="InferenceService URL, mutually exclusive with ServiceName"
/><ApiField
  name="data"
  type="string"
  required="false"
  description="request data sent to the next route with input/output from the previous step<br />$request<br />$response.predictions"
/><ApiField
  name="mapPredictionsToInstances"
  type="boolean"
  required="false"
  description="If true, maps the 'predictions' field from the previous step's response<br />to the 'instances' field of this step's request.<br />Useful in sequential inference graphs where one step's output<br />becomes the input for the next."
/><ApiField
  name="weight"
  type="integer"
  required="false"
  description="the weight for split of the traffic, only used for Split Router<br />when weight is specified all the routing targets should be sum to 100"
/><ApiField
  name="condition"
  type="string"
  required="false"
  description="routing based on the condition"
/><ApiField
  name="dependency"
  type="[InferenceStepDependencyType](#inferencestepdependencytype)"
  required="false"
  description="to decide whether a step is a hard or a soft dependency in the Inference Graph"
/>


#### InferenceStepDependencyType

**Underlying type:** string

**Appears in:**
- [InferenceStep](#inferencestep)

InferenceStepDependencyType constant for inference step dependency



##### Possible Values

<ApiField
  name="Soft"
  type="enum"
  required="false"
  description="Soft<br />"
/><ApiField
  name="Hard"
  type="enum"
  required="false"
  description="Hard<br />"
/>
#### InferenceTarget



**Appears in:**
- [InferenceStep](#inferencestep)

Exactly one InferenceTarget field must be specified

##### Fields



<ApiField
  name="nodeName"
  type="string"
  required="false"
  description="The node name for routing as next step"
/><ApiField
  name="serviceName"
  type="string"
  required="true"
  description="named reference for InferenceService"
/><ApiField
  name="serviceUrl"
  type="string"
  required="false"
  description="InferenceService URL, mutually exclusive with ServiceName"
/>


#### IngressSpec



**Appears in:**
- [RouterSpec](#routerspec)

IngressSpec defines the configuration for a Kubernetes Ingress.

##### Fields



<ApiField
  name="refs"
  type="[UntypedObjectReference](#untypedobjectreference) array"
  required="false"
  description="Refs provides a reference to an existing, user-managed Ingress object (&quot;Bring Your Own&quot; ingress).<br />The controller will not create an Ingress but will use the referenced one to populate status URLs."
/>


#### KEDAScalingSpec



**Appears in:**
- [ActuatorSpec](#actuatorspec)
- [WVASpec](#wvaspec)

KEDAScalingSpec configures the KEDA ScaledObject for autoscaling.
The fields are directly from the upstream KEDA ScaledObject API.

##### Fields



<ApiField
  name="pollingInterval"
  type="integer"
  required="false"
  description="PollingInterval is the interval in seconds to check each trigger on.<br />Must be at least 1 second."
/><ApiField
  name="cooldownPeriod"
  type="integer"
  required="false"
  description="CooldownPeriod is the period in seconds to wait after the last trigger reported active<br />before scaling the resource back to its minimum replica count.<br />A value of 0 means scale down immediately with no cooldown."
/><ApiField
  name="initialCooldownPeriod"
  type="integer"
  required="false"
  description="InitialCooldownPeriod is the period in seconds to wait after the ScaledObject is created<br />before KEDA starts evaluating triggers. Useful for LLM deployments where the model<br />takes time to load before it can serve traffic, preventing premature scale-up decisions."
/><ApiField
  name="idleReplicaCount"
  type="integer"
  required="false"
  description="IdleReplicaCount is the number of replicas KEDA will scale the resource down to<br />when there are no triggers active. This must be less than minReplicas.<br />If not set, KEDA will not scale below minReplicas."
/><ApiField
  name="fallback"
  type="[Fallback](#fallback)"
  required="false"
  description="Fallback defines the replica count to maintain when the scaler is in a fallback state<br />(e.g., when Prometheus or WVA metrics are unavailable). This allows the deployment to<br />hold a safe replica count during metric outages rather than scaling to zero."
/><ApiField
  name="advanced"
  type="[AdvancedConfig](#advancedconfig)"
  required="false"
  description="Advanced specifies the advanced KEDA configuration options.<br />This includes HPA behavior configuration and restore-to-original replica count settings."
/>



#### LLMInferenceServiceSpec



**Appears in:**
- [LLMInferenceService](#llminferenceservice)
- [LLMInferenceServiceConfig](#llminferenceserviceconfig)

LLMInferenceServiceSpec defines the desired state of LLMInferenceService.

##### Fields



<ApiField
  name="model"
  type="[LLMModelSpec](#llmmodelspec)"
  required="false"
  description="Model specification, including its URI, potential LoRA adapters, and storage details.<br />It's optional for `LLMInferenceServiceConfig` kind."
/><ApiField
  name="storageInitializer"
  type="[StorageInitializerSpec](#storageinitializerspec)"
  required="false"
  description="StorageInitializer configuration for model artifact fetching."
/><ApiField
  name="replicas"
  type="integer"
  required="false"
  description="Number of replicas for the deployment."
/><ApiField
  name="scaling"
  type="[ScalingSpec](#scalingspec)"
  required="false"
  description="Scaling configuration for autoscaling this workload.<br />When specified, the controller creates and manages autoscaling resources<br />(VariantAutoscaling CR, ServiceMonitor, and the selected actuator — HPA or KEDA ScaledObject)<br />targeting this workload.<br />Mutually exclusive with the static 'replicas' field.<br />In a disaggregated setup, each workload (decode and prefill) can have its own independent scaling configuration,<br />resulting in separate autoscaling resources per workload."
/><ApiField
  name="parallelism"
  type="[ParallelismSpec](#parallelismspec)"
  required="false"
  description="Parallelism configurations for the runtime, such as tensor and pipeline parallelism.<br />These values are used to configure the underlying inference runtime (e.g., vLLM)."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="template"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Template for the main pod spec.<br />In a multi-node deployment, this configures the &quot;head&quot; or &quot;master&quot; pod.<br />In a disaggregated deployment, this configures the &quot;decode&quot; pod if it's the top-level template,<br />or the &quot;prefill&quot; pod if it's within the Prefill block."
/><ApiField
  name="worker"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Worker configuration for multi-node deployments.<br />The presence of this field triggers the creation of a multi-node (distributed) setup.<br />This spec defines the configuration for the worker pods, while the main 'Template' field defines the head pod.<br />The controller is responsible for enabling discovery between head and worker pods."
/><ApiField
  name="router"
  type="[RouterSpec](#routerspec)"
  required="false"
  description="Router configuration for how the service is exposed. This section dictates the creation and management<br />of networking resources like Ingress or Gateway API objects (HTTPRoute, Gateway)."
/><ApiField
  name="prefill"
  type="[WorkloadSpec](#workloadspec)"
  required="false"
  description="Prefill configuration for disaggregated serving.<br />When this section is included, the controller creates a separate deployment for prompt processing (prefill)<br />in addition to the main 'decode' deployment, inspired by the llm-d architecture.<br />This allows for independent scaling and hardware allocation for prefill and decode steps."
/><ApiField
  name="baseRefs"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="BaseRefs allows inheriting and overriding configurations from one or more LLMInferenceServiceConfig instances.<br />The controller merges these base configurations, with the current LLMInferenceService spec taking the highest precedence.<br />When multiple baseRefs are provided, the last one in the list overrides previous ones."
/>


#### LLMInferenceServiceStatus



**Appears in:**
- [LLMInferenceService](#llminferenceservice)

LLMInferenceServiceStatus defines the observed state of LLMInferenceService.

##### Fields



<ApiField
  name="url"
  type="[URL](#url)"
  required="false"
  description="URL of the publicly exposed service."
/><ApiField
  name="observedGeneration"
  type="integer"
  required="false"
  description="ObservedGeneration is the 'Generation' of the Service that<br />was last processed by the controller."
/><ApiField
  name="conditions"
  type="[Conditions](#conditions)"
  required="false"
  description="Conditions the latest available observations of a resource's current state."
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="true"
  description="Annotations is additional Status fields for the Resource to save some<br />additional State as well as convey more information to the user. This is<br />roughly akin to Annotations on any k8s resource, just the reconciler conveying<br />richer information outwards."
/><ApiField
  name="address"
  type="[Addressable](#addressable)"
  required="false"
  description="Address is a single Addressable address.<br />If Addresses is present, Address will be ignored by clients."
/><ApiField
  name="addresses"
  type="[Addressable](#addressable) array"
  required="false"
  description="Addresses is a list of addresses for different protocols (HTTP and HTTPS)<br />If Addresses is present, Address must be ignored by clients."
/>



#### LLMModelSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)
- [LoRASpec](#loraspec)

LLMModelSpec defines the model source and its characteristics.

##### Fields



<ApiField
  name="uri"
  type="[URL](#url)"
  required="true"
  description="URI of the model, specifying its location, e.g., hf://meta-llama/Llama-4-Scout-17B-16E-Instruct<br />The storage-initializer init container uses this URI to download the model."
/><ApiField
  name="name"
  type="string"
  required="false"
  description="Name is the name of the model as it will be set in the &quot;model&quot; parameter for an incoming request.<br />If omitted, it will default to `metadata.name`. For LoRA adapters, this field is required."
/><ApiField
  name="criticality"
  type="[Criticality](#criticality)"
  required="false"
  description="Criticality defines how important it is to serve the model compared to other models.<br />This is used by the Inference Gateway scheduler."
/><ApiField
  name="lora"
  type="[LoRASpec](#loraspec)"
  required="false"
  description="LoRA (Low-Rank Adaptation) adapters configurations.<br />Allows for specifying one or more LoRA adapters to be applied to the base model."
/>


#### LoRASpec



**Appears in:**
- [LLMModelSpec](#llmmodelspec)

LoRASpec defines the configuration for LoRA adapters.

##### Fields



<ApiField
  name="adapters"
  type="[LLMModelSpec](#llmmodelspec) array"
  required="false"
  description="Adapters is the static specification for one or more LoRA adapters.<br />Each adapter is defined by its own ModelSpec.<br />This type is recursive https://github.com/kubernetes-sigs/controller-tools/issues/585"
/>


#### LocalModelCacheSpec



**Appears in:**
- [LocalModelCache](#localmodelcache)

LocalModelCacheSpec

##### Fields



<ApiField
  name="sourceModelUri"
  type="string"
  required="true"
  description="Original StorageUri"
/><ApiField
  name="modelSize"
  type="[Quantity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#quantity-resource-api)"
  required="true"
  description="Model size to make sure it does not exceed the disk space reserved for local models. The limit is defined on the NodeGroup."
/><ApiField
  name="nodeGroups"
  type="string array"
  required="true"
  description="group of nodes to cache the model on.<br />Todo: support more than 1 node groups"
/>


#### LocalModelCacheStatus



**Appears in:**
- [LocalModelCache](#localmodelcache)



##### Fields



<ApiField
  name="nodeStatus"
  type="object (keys:string, values:[NodeStatus](#nodestatus))"
  required="true"
  description="Status of the model on a node, like NodeDownloaded or NodeNotReady"
/><ApiField
  name="copies"
  type="[ModelCopies](#modelcopies)"
  required="false"
  description="How many nodes have the model available locally"
/><ApiField
  name="inferenceServices"
  type="[NamespacedName](#namespacedname) array"
  required="true"
  description="Inference services using this local model"
/>


#### LocalModelInfo



**Appears in:**
- [LocalModelNodeSpec](#localmodelnodespec)



##### Fields



<ApiField
  name="sourceModelUri"
  type="string"
  required="true"
  description="Original StorageUri"
/><ApiField
  name="modelName"
  type="string"
  required="true"
  description="Model name. Used as the subdirectory name to store this model on local file system"
/>


#### LocalModelNodeGroupSpec



**Appears in:**
- [LocalModelNodeGroup](#localmodelnodegroup)

LocalModelNodeGroupSpec defines a group of nodes for to download the model to.

##### Fields



<ApiField
  name="storageLimit"
  type="[Quantity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#quantity-resource-api)"
  required="true"
  description="Max storage size per node in this node group"
/><ApiField
  name="persistentVolumeSpec"
  type="[PersistentVolumeSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#persistentvolumespec-v1-core)"
  required="true"
  description="Used to create PersistentVolumes for downloading models and in inference service namespaces"
/><ApiField
  name="persistentVolumeClaimSpec"
  type="[PersistentVolumeClaimSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#persistentvolumeclaimspec-v1-core)"
  required="true"
  description="Used to create PersistentVolumeClaims for download and in inference service namespaces"
/>


#### LocalModelNodeGroupStatus



**Appears in:**
- [LocalModelNodeGroup](#localmodelnodegroup)



##### Fields



<ApiField
  name="used"
  type="[Quantity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#quantity-resource-api)"
  required="true"
  description="Used storage space on any node for this node group"
/><ApiField
  name="available"
  type="[Quantity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#quantity-resource-api)"
  required="true"
  description="Available storage space on any node for this node group"
/>


#### LocalModelNodeSpec



**Appears in:**
- [LocalModelNode](#localmodelnode)



##### Fields



<ApiField
  name="localModels"
  type="[LocalModelInfo](#localmodelinfo) array"
  required="true"
  description="List of model source URI and their names"
/>


#### LocalModelNodeStatus



**Appears in:**
- [LocalModelNode](#localmodelnode)



##### Fields



<ApiField
  name="modelStatus"
  type="object (keys:string, values:[ModelStatus](#modelstatus))"
  required="true"
  description="Status of each local model"
/>


#### ModelCopies



**Appears in:**
- [LocalModelCacheStatus](#localmodelcachestatus)



##### Fields



<ApiField
  name="available"
  type="integer"
  required="true"
  description=""
/><ApiField
  name="total"
  type="integer"
  required="true"
  description="Total number of nodes that we expect the model to be downloaded. Including nodes that are not ready"
/><ApiField
  name="failed"
  type="integer"
  required="true"
  description="Download Failed"
/>


#### ModelSpec



**Appears in:**
- [TrainedModelSpec](#trainedmodelspec)

ModelSpec describes a TrainedModel

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="true"
  description="Storage URI for the model repository"
/><ApiField
  name="framework"
  type="string"
  required="true"
  description="Machine Learning &lt;framework name&gt;<br />The values could be: &quot;tensorflow&quot;,&quot;pytorch&quot;,&quot;sklearn&quot;,&quot;onnx&quot;,&quot;xgboost&quot;, &quot;myawesomeinternalframework&quot; etc."
/><ApiField
  name="memory"
  type="[Quantity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#quantity-resource-api)"
  required="true"
  description="Maximum memory this model will consume, this field is used to decide if a model server has enough memory to load this model."
/>


#### ModelStatus

**Underlying type:** string

**Appears in:**
- [LocalModelNodeStatus](#localmodelnodestatus)

ModelStatus enum



##### Possible Values

<ApiField
  name="ModelDownloadPending"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="ModelDownloading"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="ModelDownloaded"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="ModelDownloadError"
  type="enum"
  required="false"
  description=""
/>
#### NamespacedName



**Appears in:**
- [LocalModelCacheStatus](#localmodelcachestatus)



##### Fields



<ApiField
  name="namespace"
  type="string"
  required="true"
  description=""
/><ApiField
  name="name"
  type="string"
  required="true"
  description=""
/>


#### NodeStatus

**Underlying type:** string

**Appears in:**
- [LocalModelCacheStatus](#localmodelcachestatus)

NodeStatus enum



##### Possible Values

<ApiField
  name="NodeNotReady"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="NodeDownloadPending"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="NodeDownloading"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="NodeDownloaded"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="NodeDownloadError"
  type="enum"
  required="false"
  description=""
/>
#### ParallelismSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)
- [WorkloadSpec](#workloadspec)

ParallelismSpec defines the parallelism parameters for distributed inference.

##### Fields



<ApiField
  name="tensor"
  type="integer"
  required="false"
  description="Tensor parallelism size."
/><ApiField
  name="pipeline"
  type="integer"
  required="false"
  description="Pipeline parallelism size."
/><ApiField
  name="data"
  type="integer"
  required="false"
  description="Data parallelism size."
/><ApiField
  name="dataLocal"
  type="integer"
  required="false"
  description="DataLocal data local parallelism size."
/><ApiField
  name="dataRPCPort"
  type="integer"
  required="false"
  description="DataRPCPort is the data parallelism RPC port."
/><ApiField
  name="expert"
  type="boolean"
  required="false"
  description="Expert enables expert parallelism."
/>


#### RouterSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)

RouterSpec defines the routing configuration for exposing the service.
It supports Kubernetes Ingress and the Gateway API. The fields are mutually exclusive.

##### Fields



<ApiField
  name="route"
  type="[GatewayRoutesSpec](#gatewayroutesspec)"
  required="false"
  description="Route configuration for the Gateway API.<br />If an empty object `\{\}` is provided, the controller creates and manages a new HTTPRoute."
/><ApiField
  name="gateway"
  type="[GatewaySpec](#gatewayspec)"
  required="false"
  description="Gateway configuration for the Gateway API, mutually exclusive with Ingress.<br />If an empty object `\{\}` is provided, the controller uses a default Gateway.<br />This must be used in conjunction with the 'Route' field for managed Gateway API resources."
/><ApiField
  name="ingress"
  type="[IngressSpec](#ingressspec)"
  required="false"
  description="Ingress configuration. This is mutually exclusive with Route and Gateway.<br />If an empty object `\{\}` is provided, the controller creates and manages a default Ingress resource."
/><ApiField
  name="scheduler"
  type="[SchedulerSpec](#schedulerspec)"
  required="false"
  description="Scheduler configuration for the Inference Gateway extension.<br />If this field is non-empty, an InferenceModel resource will be created to integrate with the gateway's scheduler."
/>


#### ScaleMetric

**Underlying type:** string

**Appears in:**
- [InferenceGraphSpec](#inferencegraphspec)

ScaleMetric enum




#### ScalingSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)
- [WorkloadSpec](#workloadspec)

ScalingSpec configures autoscaling for the LLM inference deployment.
When scaling is configured, the controller creates and manages autoscaling resources
(VariantAutoscaling CR, ServiceMonitor, and the selected actuator — HPA or KEDA ScaledObject).

##### Fields



<ApiField
  name="minReplicas"
  type="integer"
  required="false"
  description="MinReplicas is the minimum number of replicas for the deployment during active scaling.<br />This is the scaling floor when triggers are active.<br />For idle scale-down, use KEDA's idleReplicaCount instead.<br />Defaults to 1 if not specified."
/><ApiField
  name="maxReplicas"
  type="integer"
  required="false"
  description="MaxReplicas is the maximum number of replicas for the deployment."
/><ApiField
  name="wva"
  type="[WVASpec](#wvaspec)"
  required="false"
  description="WVA configures the Workload Variant Autoscaler (WVA) for scaling.<br />WVA scales based on a variety of inference metrics (KV cache utilization, queue depth, etc.)<br />rather than traditional CPU/memory metrics."
/>


#### SchedulerConfigSpec



**Appears in:**
- [SchedulerSpec](#schedulerspec)



##### Fields



<ApiField
  name="inline"
  type="[RawExtension](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#rawextension-runtime-pkg)"
  required="true"
  description="Inline EndpointPickerConfig"
/><ApiField
  name="ref"
  type="[ConfigMapKeySelector](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#configmapkeyselector-v1-core)"
  required="true"
  description="Ref is a reference to a ConfigMap key with EndpointPickerConfig."
/>


#### SchedulerSpec



**Appears in:**
- [RouterSpec](#routerspec)

SchedulerSpec defines the Inference Gateway extension configuration.

The SchedulerSpec configures the connection from the Gateway to the model deployment leveraging the LLM optimized
request Scheduler, also known as the Endpoint Picker (EPP) which determines the exact pod that should handle the
request and responds back to Envoy with the target pod, Envoy will then forward the request to the chosen pod.

The Scheduler is only effective when having multiple inference pod replicas.

Step 1: Gateway (Envoy) &lt;-- ExtProc --&gt; EPP (select the optimal replica to handle the request)
Step 2: Gateway (Envoy) &lt;-- forward request --&gt; Inference Pod X

##### Fields



<ApiField
  name="pool"
  type="[InferencePoolSpec](#inferencepoolspec)"
  required="false"
  description="Pool configuration for the InferencePool, which is part of the Inference Gateway extension."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the scheduler component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the scheduler component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="template"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Template for the Inference Gateway Extension pod spec.<br />This configures the Endpoint Picker (EPP) Deployment."
/><ApiField
  name="config"
  type="[SchedulerConfigSpec](#schedulerconfigspec)"
  required="true"
  description="Config is the configuration for the EndpointPicker."
/><ApiField
  name="replicas"
  type="integer"
  required="true"
  description="Replicas is the number of replicas for the scheduler."
/>


#### ServerType

**Underlying type:** string

**Appears in:**
- [BuiltInAdapter](#builtinadapter)

ServerType constant for specifying the runtime name



##### Possible Values

<ApiField
  name="triton"
  type="enum"
  required="false"
  description="Model server is Triton<br />"
/><ApiField
  name="mlserver"
  type="enum"
  required="false"
  description="Model server is MLServer<br />"
/><ApiField
  name="ovms"
  type="enum"
  required="false"
  description="Model server is OpenVino Model Server<br />"
/>
#### ServingRuntimePodSpec



**Appears in:**
- [ServingRuntimeSpec](#servingruntimespec)
- [WorkerSpec](#workerspec)



##### Fields



<ApiField
  name="containers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of containers belonging to the pod.<br />Containers cannot currently be added or removed.<br />There must be at least one container in a Pod.<br />Cannot be updated."
/><ApiField
  name="volumes"
  type="[Volume](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volume-v1-core) array"
  required="false"
  description="List of volumes that can be mounted by containers belonging to the pod.<br />More info: https://kubernetes.io/docs/concepts/storage/volumes"
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector is a selector which must be true for the pod to fit on a node.<br />Selector which must match a node's labels for the pod to be scheduled on that node.<br />More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description="If specified, the pod's scheduling constraints"
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="If specified, the pod's tolerations."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be add to the pod.<br />More info: http://kubernetes.io/docs/user-guide/labels"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be add to the pod.<br />More info: http://kubernetes.io/docs/user-guide/annotations"
/><ApiField
  name="imagePullSecrets"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.<br />If specified, these secrets will be passed to individual puller implementations for them to use. For example,<br />in the case of docker, only DockerConfig type secrets are honored.<br />More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
/><ApiField
  name="hostIPC"
  type="boolean"
  required="false"
  description="Use the host's ipc namespace.<br />Optional: Default to false."
/><ApiField
  name="schedulerName"
  type="string"
  required="false"
  description="If specified, the pod will be dispatched by specified scheduler.<br />If not specified, the pod will be dispatched by default scheduler."
/>


#### ServingRuntimeSpec



**Appears in:**
- [ClusterServingRuntime](#clusterservingruntime)
- [ServingRuntime](#servingruntime)
- [SupportedRuntime](#supportedruntime)

ServingRuntimeSpec defines the desired state of ServingRuntime. This spec is currently provisional
and are subject to change as details regarding single-model serving and multi-model serving
are hammered out.

##### Fields



<ApiField
  name="supportedModelFormats"
  type="[SupportedModelFormat](#supportedmodelformat) array"
  required="true"
  description="Model formats and version supported by this runtime"
/><ApiField
  name="multiModel"
  type="boolean"
  required="false"
  description="Whether this ServingRuntime is intended for multi-model usage or not."
/><ApiField
  name="disabled"
  type="boolean"
  required="false"
  description="Set to true to disable use of this runtime"
/><ApiField
  name="protocolVersions"
  type="InferenceServiceProtocol array"
  required="false"
  description="Supported protocol versions (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="workerSpec"
  type="[WorkerSpec](#workerspec)"
  required="false"
  description="Set WorkerSpec to enable multi-node/multi-gpu"
/><ApiField
  name="containers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of containers belonging to the pod.<br />Containers cannot currently be added or removed.<br />There must be at least one container in a Pod.<br />Cannot be updated."
/><ApiField
  name="volumes"
  type="[Volume](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volume-v1-core) array"
  required="false"
  description="List of volumes that can be mounted by containers belonging to the pod.<br />More info: https://kubernetes.io/docs/concepts/storage/volumes"
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector is a selector which must be true for the pod to fit on a node.<br />Selector which must match a node's labels for the pod to be scheduled on that node.<br />More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description="If specified, the pod's scheduling constraints"
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="If specified, the pod's tolerations."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be add to the pod.<br />More info: http://kubernetes.io/docs/user-guide/labels"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be add to the pod.<br />More info: http://kubernetes.io/docs/user-guide/annotations"
/><ApiField
  name="imagePullSecrets"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.<br />If specified, these secrets will be passed to individual puller implementations for them to use. For example,<br />in the case of docker, only DockerConfig type secrets are honored.<br />More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
/><ApiField
  name="hostIPC"
  type="boolean"
  required="false"
  description="Use the host's ipc namespace.<br />Optional: Default to false."
/><ApiField
  name="schedulerName"
  type="string"
  required="false"
  description="If specified, the pod will be dispatched by specified scheduler.<br />If not specified, the pod will be dispatched by default scheduler."
/><ApiField
  name="grpcEndpoint"
  type="string"
  required="false"
  description="Grpc endpoint for internal model-management (implementing mmesh.ModelRuntime gRPC service)<br />Assumed to be single-model runtime if omitted"
/><ApiField
  name="grpcDataEndpoint"
  type="string"
  required="false"
  description="Grpc endpoint for inferencing"
/><ApiField
  name="httpDataEndpoint"
  type="string"
  required="false"
  description="HTTP endpoint for inferencing"
/><ApiField
  name="replicas"
  type="integer"
  required="false"
  description="Configure the number of replicas in the Deployment generated by this ServingRuntime<br />If specified, this overrides the podsPerRuntime configuration value"
/><ApiField
  name="storageHelper"
  type="[StorageHelper](#storagehelper)"
  required="false"
  description="Configuration for this runtime's use of the storage helper (model puller)<br />It is enabled unless explicitly disabled"
/><ApiField
  name="builtInAdapter"
  type="[BuiltInAdapter](#builtinadapter)"
  required="false"
  description="Provide the details about built-in runtime adapter"
/>


#### ServingRuntimeStatus



**Appears in:**
- [ClusterServingRuntime](#clusterservingruntime)
- [ServingRuntime](#servingruntime)

ServingRuntimeStatus defines the observed state of ServingRuntime




#### StorageContainerSpec



**Appears in:**
- [ClusterStorageContainer](#clusterstoragecontainer)

StorageContainerSpec defines the container spec for the storage initializer init container, and the protocols it supports.

##### Fields



<ApiField
  name="container"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core)"
  required="true"
  description="Container spec for the storage initializer init container"
/><ApiField
  name="supportedUriFormats"
  type="[SupportedUriFormat](#supporteduriformat) array"
  required="true"
  description="List of URI formats that this container supports"
/><ApiField
  name="workloadType"
  type="[WorkloadType](#workloadtype)"
  required="true"
  defaultValue="initContainer"
  description=""
/><ApiField
  name="supportsMultiModelDownload"
  type="boolean"
  required="true"
  defaultValue="false"
  description=""
/>


#### StorageHelper



**Appears in:**
- [ServingRuntimeSpec](#servingruntimespec)



##### Fields



<ApiField
  name="disabled"
  type="boolean"
  required="false"
  description=""
/>


#### StorageInitializerSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)

StorageInitializerSpec defines the configuration for the storage initializer.
The storage initializer is an initContainer responsible for downloading model artifacts
from remote storage (s3://, hf://) before the main container starts.

Example - Disable storage initializer:

	storageInitializer:
	  enabled: false

Example - Explicitly enable (same as default):

	storageInitializer:
	  enabled: true

##### Fields



<ApiField
  name="enabled"
  type="boolean"
  required="false"
  description="Enabled controls whether the storage-initializer initContainer is created.<br />When nil or true, storage-initializer is created for applicable URIs (s3://, hf://).<br />When explicitly set to false, storage-initializer creation is skipped.<br />This is useful when models are pre-loaded via alternative mechanisms (e.g., custom init containers, modelcars).<br />Default: true (nil is treated as true for backward compatibility)"
/>


#### SupportedModelFormat



**Appears in:**
- [ServingRuntimeSpec](#servingruntimespec)



##### Fields



<ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the model format."
/><ApiField
  name="version"
  type="string"
  required="false"
  description="Version of the model format.<br />Used in validating that a predictor is supported by a runtime.<br />Can be &quot;major&quot;, &quot;major.minor&quot; or &quot;major.minor.patch&quot;."
/><ApiField
  name="autoSelect"
  type="boolean"
  required="false"
  description="Set to true to allow the ServingRuntime to be used for automatic model placement if<br />this model format is specified with no explicit runtime."
/><ApiField
  name="priority"
  type="integer"
  required="false"
  description="Priority of this serving runtime for auto selection.<br />This is used to select the serving runtime if more than one serving runtime supports the same model format.<br />The value should be greater than zero.  The higher the value, the higher the priority.<br />Priority is not considered if AutoSelect is either false or not specified.<br />Priority can be overridden by specifying the runtime in the InferenceService."
/>



#### SupportedUriFormat



**Appears in:**
- [StorageContainerSpec](#storagecontainerspec)

SupportedUriFormat can be either prefix or regex. Todo: Add validation that only one of them is set.

##### Fields



<ApiField
  name="prefix"
  type="string"
  required="true"
  description=""
/><ApiField
  name="regex"
  type="string"
  required="true"
  description=""
/>


#### TrainedModelSpec



**Appears in:**
- [TrainedModel](#trainedmodel)

TrainedModelSpec defines the TrainedModel spec

##### Fields



<ApiField
  name="inferenceService"
  type="string"
  required="true"
  description="parent inference service to deploy to"
/><ApiField
  name="model"
  type="[ModelSpec](#modelspec)"
  required="true"
  description="Predictor model spec"
/>


#### TrainedModelStatus



**Appears in:**
- [TrainedModel](#trainedmodel)

TrainedModelStatus defines the observed state of TrainedModel

##### Fields



<ApiField
  name="observedGeneration"
  type="integer"
  required="false"
  description="ObservedGeneration is the 'Generation' of the Service that<br />was last processed by the controller."
/><ApiField
  name="conditions"
  type="[Conditions](#conditions)"
  required="false"
  description="Conditions the latest available observations of a resource's current state."
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="true"
  description="Annotations is additional Status fields for the Resource to save some<br />additional State as well as convey more information to the user. This is<br />roughly akin to Annotations on any k8s resource, just the reconciler conveying<br />richer information outwards."
/><ApiField
  name="url"
  type="[URL](#url)"
  required="true"
  description="URL holds the url that will distribute traffic over the provided traffic targets.<br />For v1: http[s]://\{route-name\}.\{route-namespace\}.\{cluster-level-suffix\}/v1/models/&lt;trainedmodel&gt;:predict<br />For v2: http[s]://\{route-name\}.\{route-namespace\}.\{cluster-level-suffix\}/v2/models/&lt;trainedmodel&gt;/infer"
/><ApiField
  name="address"
  type="[Addressable](#addressable)"
  required="true"
  description="Addressable endpoint for the deployed trained model<br />http://&lt;inferenceservice.metadata.name&gt;/v1/models/&lt;trainedmodel&gt;.metadata.name"
/>



#### UntypedObjectReference



**Appears in:**
- [GatewaySpec](#gatewayspec)
- [IngressSpec](#ingressspec)

UntypedObjectReference is a reference to an object without a specific Group/Version/Kind.
It's used for referencing networking resources like Gateways and Ingresses where the exact type
might be inferred or is not strictly required by this controller.

##### Fields



<ApiField
  name="name"
  type="[ObjectName](#objectname)"
  required="true"
  description="Name of the referenced object."
/><ApiField
  name="namespace"
  type="[Namespace](#namespace)"
  required="true"
  description="Namespace of the referenced object."
/>


#### WVASpec



**Appears in:**
- [ScalingSpec](#scalingspec)

WVASpec configures the Workload Variant Autoscaler.

##### Fields



<ApiField
  name="variantCost"
  type="string"
  required="false"
  defaultValue="10.0"
  description="VariantCost specifies the cost per replica for this variant (used in saturation analysis).<br />Must be a non-negative numeric string (e.g., &quot;10&quot;, &quot;10.0&quot;, &quot;0.5&quot;).<br />Defaults to &quot;10.0&quot; if not specified."
/><ApiField
  name="hpa"
  type="[HPAScalingSpec](#hpascalingspec)"
  required="false"
  description="HPA configures the HorizontalPodAutoscaler as the actuator backend.<br />When specified, HPA reads the wva_desired_replicas metric via the Kubernetes Metrics API<br />(requires Prometheus Adapter) and scales the deployment accordingly.<br />Mutually exclusive with KEDA."
/><ApiField
  name="keda"
  type="[KEDAScalingSpec](#kedascalingspec)"
  required="false"
  description="KEDA configures a KEDA ScaledObject as the actuator backend.<br />When specified, KEDA queries Prometheus directly for the wva_desired_replicas metric<br />and scales the deployment accordingly. KEDA does not require a Prometheus Adapter.<br />Mutually exclusive with HPA."
/>


#### WorkerSpec



**Appears in:**
- [ServingRuntimeSpec](#servingruntimespec)

WorkerSpec is the schema for multi-node/multi-GPU feature

##### Fields



<ApiField
  name="containers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of containers belonging to the pod.<br />Containers cannot currently be added or removed.<br />There must be at least one container in a Pod.<br />Cannot be updated."
/><ApiField
  name="volumes"
  type="[Volume](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volume-v1-core) array"
  required="false"
  description="List of volumes that can be mounted by containers belonging to the pod.<br />More info: https://kubernetes.io/docs/concepts/storage/volumes"
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector is a selector which must be true for the pod to fit on a node.<br />Selector which must match a node's labels for the pod to be scheduled on that node.<br />More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description="If specified, the pod's scheduling constraints"
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="If specified, the pod's tolerations."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be add to the pod.<br />More info: http://kubernetes.io/docs/user-guide/labels"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be add to the pod.<br />More info: http://kubernetes.io/docs/user-guide/annotations"
/><ApiField
  name="imagePullSecrets"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.<br />If specified, these secrets will be passed to individual puller implementations for them to use. For example,<br />in the case of docker, only DockerConfig type secrets are honored.<br />More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
/><ApiField
  name="hostIPC"
  type="boolean"
  required="false"
  description="Use the host's ipc namespace.<br />Optional: Default to false."
/><ApiField
  name="schedulerName"
  type="string"
  required="false"
  description="If specified, the pod will be dispatched by specified scheduler.<br />If not specified, the pod will be dispatched by default scheduler."
/><ApiField
  name="pipelineParallelSize"
  type="integer"
  required="false"
  description="PipelineParallelSize defines the number of parallel workers.<br />It specifies the number of model partitions across multiple devices, allowing large models to be split and processed concurrently across these partitions<br />It also represents the number of replicas in the worker set, where each worker set serves as a scaling unit."
/><ApiField
  name="tensorParallelSize"
  type="integer"
  required="false"
  description="TensorParallelSize specifies the number of GPUs to be used per node.<br />It indicates the degree of parallelism for tensor computations across the available GPUs."
/>


#### WorkloadSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)

WorkloadSpec defines the configuration for a deployment workload, such as replicas and pod specifications.

##### Fields



<ApiField
  name="replicas"
  type="integer"
  required="false"
  description="Number of replicas for the deployment."
/><ApiField
  name="scaling"
  type="[ScalingSpec](#scalingspec)"
  required="false"
  description="Scaling configuration for autoscaling this workload.<br />When specified, the controller creates and manages autoscaling resources<br />(VariantAutoscaling CR, ServiceMonitor, and the selected actuator — HPA or KEDA ScaledObject)<br />targeting this workload.<br />Mutually exclusive with the static 'replicas' field.<br />In a disaggregated setup, each workload (decode and prefill) can have its own independent scaling configuration,<br />resulting in separate autoscaling resources per workload."
/><ApiField
  name="parallelism"
  type="[ParallelismSpec](#parallelismspec)"
  required="false"
  description="Parallelism configurations for the runtime, such as tensor and pipeline parallelism.<br />These values are used to configure the underlying inference runtime (e.g., vLLM)."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="template"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Template for the main pod spec.<br />In a multi-node deployment, this configures the &quot;head&quot; or &quot;master&quot; pod.<br />In a disaggregated deployment, this configures the &quot;decode&quot; pod if it's the top-level template,<br />or the &quot;prefill&quot; pod if it's within the Prefill block."
/><ApiField
  name="worker"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Worker configuration for multi-node deployments.<br />The presence of this field triggers the creation of a multi-node (distributed) setup.<br />This spec defines the configuration for the worker pods, while the main 'Template' field defines the head pod.<br />The controller is responsible for enabling discovery between head and worker pods."
/>


#### WorkloadType

**Underlying type:** string

**Appears in:**
- [StorageContainerSpec](#storagecontainerspec)





##### Possible Values

<ApiField
  name="initContainer"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="localModelDownloadJob"
  type="enum"
  required="false"
  description=""
/>

## serving.kserve.io/v1alpha2

Package v1alpha2 contains API Schema definitions for the serving v1alpha2 API group


## Resource Kinds

### Available Kinds
- [LLMInferenceService](#llminferenceservice)
- [LLMInferenceServiceConfig](#llminferenceserviceconfig)
- [LLMInferenceServiceConfigList](#llminferenceserviceconfiglist)
- [LLMInferenceServiceList](#llminferenceservicelist)

### Kind Definitions
#### LLMInferenceService



**Appears in:**
- [LLMInferenceServiceList](#llminferenceservicelist)

LLMInferenceService is the Schema for the llminferenceservices API, representing a single LLM deployment.
It orchestrates the creation of underlying Kubernetes resources like Deployments and Services,
and configures networking for exposing the model.

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha2</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LLMInferenceService</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[LLMInferenceServiceSpec](#llminferenceservicespec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[LLMInferenceServiceStatus](#llminferenceservicestatus)"
  required="true"
  description=""
/>


#### LLMInferenceServiceConfig



**Appears in:**
- [LLMInferenceServiceConfigList](#llminferenceserviceconfiglist)

LLMInferenceServiceConfig is the Schema for the llminferenceserviceconfigs API.
It acts as a template to provide base configurations that can be inherited by multiple LLMInferenceService instances.

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha2</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LLMInferenceServiceConfig</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[LLMInferenceServiceSpec](#llminferenceservicespec)"
  required="true"
  description=""
/>


#### LLMInferenceServiceConfigList




LLMInferenceServiceConfigList is the list type for LLMInferenceServiceConfig.

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha2</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LLMInferenceServiceConfigList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[LLMInferenceServiceConfig](#llminferenceserviceconfig) array"
  required="true"
  description=""
/>


#### LLMInferenceServiceList




LLMInferenceServiceList is the list type for LLMInferenceService.

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1alpha2</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>LLMInferenceServiceList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[LLMInferenceService](#llminferenceservice) array"
  required="true"
  description=""
/>


## Supporting Types

### Available Types
- [ActuatorSpec](#actuatorspec)
- [GatewayRoutesSpec](#gatewayroutesspec)
- [GatewaySpec](#gatewayspec)
- [HPAScalingSpec](#hpascalingspec)
- [HTTPRouteSpec](#httproutespec)
- [InferencePoolSpec](#inferencepoolspec)
- [IngressSpec](#ingressspec)
- [KEDAScalingSpec](#kedascalingspec)
- [LLMInferenceServiceConfigValidator](#llminferenceserviceconfigvalidator)
- [LLMInferenceServiceSpec](#llminferenceservicespec)
- [LLMInferenceServiceStatus](#llminferenceservicestatus)
- [LLMInferenceServiceValidator](#llminferenceservicevalidator)
- [LLMModelSpec](#llmmodelspec)
- [LoRASpec](#loraspec)
- [ParallelismSpec](#parallelismspec)
- [RouterSpec](#routerspec)
- [ScalingSpec](#scalingspec)
- [SchedulerConfigSpec](#schedulerconfigspec)
- [SchedulerSpec](#schedulerspec)
- [StorageInitializerSpec](#storageinitializerspec)
- [UntypedObjectReference](#untypedobjectreference)
- [WVASpec](#wvaspec)
- [WorkloadSpec](#workloadspec)

### Type Definitions
#### ActuatorSpec



**Appears in:**
- [WVASpec](#wvaspec)

ActuatorSpec defines the autoscaling actuator backend for WVA.
Exactly one of HPA or KEDA must be specified.

##### Fields



<ApiField
  name="hpa"
  type="[HPAScalingSpec](#hpascalingspec)"
  required="false"
  description="HPA configures the HorizontalPodAutoscaler as the actuator backend.<br />When specified, HPA reads the wva_desired_replicas metric via the Kubernetes Metrics API<br />(requires Prometheus Adapter) and scales the deployment accordingly.<br />Mutually exclusive with KEDA."
/><ApiField
  name="keda"
  type="[KEDAScalingSpec](#kedascalingspec)"
  required="false"
  description="KEDA configures a KEDA ScaledObject as the actuator backend.<br />When specified, KEDA queries Prometheus directly for the wva_desired_replicas metric<br />and scales the deployment accordingly. KEDA does not require a Prometheus Adapter.<br />Mutually exclusive with HPA."
/>


#### GatewayRoutesSpec



**Appears in:**
- [RouterSpec](#routerspec)

GatewayRoutesSpec defines the configuration for a Gateway API route.

##### Fields



<ApiField
  name="http"
  type="[HTTPRouteSpec](#httproutespec)"
  required="false"
  description="HTTP route configuration."
/>


#### GatewaySpec



**Appears in:**
- [RouterSpec](#routerspec)

GatewaySpec defines the configuration for a Gateway API Gateway.

##### Fields



<ApiField
  name="refs"
  type="[UntypedObjectReference](#untypedobjectreference) array"
  required="false"
  description="Refs provides references to existing, user-managed Gateway objects (&quot;Bring Your Own&quot; gateway).<br />The controller will use the specified Gateway instead of creating one."
/>


#### HPAScalingSpec



**Appears in:**
- [ActuatorSpec](#actuatorspec)
- [WVASpec](#wvaspec)

HPAScalingSpec configures the HorizontalPodAutoscaler behavior.
The fields are directly from the upstream Kubernetes autoscaling/v2 API.

##### Fields



<ApiField
  name="behavior"
  type="[HorizontalPodAutoscalerBehavior](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#horizontalpodautoscalerbehavior-v2-autoscaling)"
  required="false"
  description="Behavior configures the scaling behavior of the target in both Up and Down directions<br />(scaleUp and scaleDown fields respectively)."
/>


#### HTTPRouteSpec



**Appears in:**
- [GatewayRoutesSpec](#gatewayroutesspec)

HTTPRouteSpec defines configurations for a Gateway API HTTPRoute.
'Spec' and 'Refs' are mutually exclusive and determine whether the route is managed by the controller or user-managed.

##### Fields



<ApiField
  name="refs"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="Refs provides references to existing, user-managed HTTPRoute objects (&quot;Bring Your Own&quot; route).<br />The controller will validate the existence of these routes but will not modify them."
/><ApiField
  name="spec"
  type="[HTTPRouteSpec](#httproutespec)"
  required="false"
  description="Spec allows for providing a custom specification for an HTTPRoute.<br />If provided, the controller will create and manage an HTTPRoute with this specification."
/>


#### InferencePoolSpec



**Appears in:**
- [SchedulerSpec](#schedulerspec)

InferencePoolSpec defines the configuration for an InferencePool.
'Spec' and 'Ref' are mutually exclusive.

##### Fields



<ApiField
  name="spec"
  type="[InferencePoolSpec](#inferencepoolspec)"
  required="false"
  description="Spec defines an inline InferencePool specification."
/><ApiField
  name="ref"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core)"
  required="false"
  description="Ref is a reference to an existing InferencePool."
/>


#### IngressSpec



**Appears in:**
- [RouterSpec](#routerspec)

IngressSpec defines the configuration for a Kubernetes Ingress.

##### Fields



<ApiField
  name="refs"
  type="[UntypedObjectReference](#untypedobjectreference) array"
  required="false"
  description="Refs provides a reference to an existing, user-managed Ingress object (&quot;Bring Your Own&quot; ingress).<br />The controller will not create an Ingress but will use the referenced one to populate status URLs."
/>


#### KEDAScalingSpec



**Appears in:**
- [ActuatorSpec](#actuatorspec)
- [WVASpec](#wvaspec)

KEDAScalingSpec configures the KEDA ScaledObject for autoscaling.
The fields are directly from the upstream KEDA ScaledObject API.

##### Fields



<ApiField
  name="pollingInterval"
  type="integer"
  required="false"
  description="PollingInterval is the interval in seconds to check each trigger on.<br />Must be at least 1 second."
/><ApiField
  name="cooldownPeriod"
  type="integer"
  required="false"
  description="CooldownPeriod is the period in seconds to wait after the last trigger reported active<br />before scaling the resource back to its minimum replica count.<br />A value of 0 means scale down immediately with no cooldown."
/><ApiField
  name="initialCooldownPeriod"
  type="integer"
  required="false"
  description="InitialCooldownPeriod is the period in seconds to wait after the ScaledObject is created<br />before KEDA starts evaluating triggers. Useful for LLM deployments where the model<br />takes time to load before it can serve traffic, preventing premature scale-up decisions."
/><ApiField
  name="idleReplicaCount"
  type="integer"
  required="false"
  description="IdleReplicaCount is the number of replicas KEDA will scale the resource down to<br />when there are no triggers active. This must be less than minReplicas.<br />If not set, KEDA will not scale below minReplicas."
/><ApiField
  name="fallback"
  type="[Fallback](#fallback)"
  required="false"
  description="Fallback defines the replica count to maintain when the scaler is in a fallback state<br />(e.g., when Prometheus or WVA metrics are unavailable). This allows the deployment to<br />hold a safe replica count during metric outages rather than scaling to zero."
/><ApiField
  name="advanced"
  type="[AdvancedConfig](#advancedconfig)"
  required="false"
  description="Advanced specifies the advanced KEDA configuration options.<br />This includes HPA behavior configuration and restore-to-original replica count settings."
/>



#### LLMInferenceServiceSpec



**Appears in:**
- [LLMInferenceService](#llminferenceservice)
- [LLMInferenceServiceConfig](#llminferenceserviceconfig)

LLMInferenceServiceSpec defines the desired state of LLMInferenceService.

##### Fields



<ApiField
  name="model"
  type="[LLMModelSpec](#llmmodelspec)"
  required="false"
  description="Model specification, including its URI, potential LoRA adapters, and storage details.<br />It's optional for `LLMInferenceServiceConfig` kind."
/><ApiField
  name="storageInitializer"
  type="[StorageInitializerSpec](#storageinitializerspec)"
  required="false"
  description="StorageInitializer configuration for model artifact fetching."
/><ApiField
  name="replicas"
  type="integer"
  required="false"
  description="Number of replicas for the deployment."
/><ApiField
  name="scaling"
  type="[ScalingSpec](#scalingspec)"
  required="false"
  description="Scaling configuration for autoscaling this workload.<br />When specified, the controller creates and manages autoscaling resources<br />(VariantAutoscaling CR, ServiceMonitor, and the selected actuator — HPA or KEDA ScaledObject)<br />targeting this workload.<br />Mutually exclusive with the static 'replicas' field.<br />In a disaggregated setup, each workload (decode and prefill) can have its own independent scaling configuration,<br />resulting in separate autoscaling resources per workload."
/><ApiField
  name="parallelism"
  type="[ParallelismSpec](#parallelismspec)"
  required="false"
  description="Parallelism configurations for the runtime, such as tensor and pipeline parallelism.<br />These values are used to configure the underlying inference runtime (e.g., vLLM)."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="template"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Template for the main pod spec.<br />In a multi-node deployment, this configures the &quot;head&quot; or &quot;master&quot; pod.<br />In a disaggregated deployment, this configures the &quot;decode&quot; pod if it's the top-level template,<br />or the &quot;prefill&quot; pod if it's within the Prefill block."
/><ApiField
  name="worker"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Worker configuration for multi-node deployments.<br />The presence of this field triggers the creation of a multi-node (distributed) setup.<br />This spec defines the configuration for the worker pods, while the main 'Template' field defines the head pod.<br />The controller is responsible for enabling discovery between head and worker pods."
/><ApiField
  name="router"
  type="[RouterSpec](#routerspec)"
  required="false"
  description="Router configuration for how the service is exposed. This section dictates the creation and management<br />of networking resources like Ingress or Gateway API objects (HTTPRoute, Gateway)."
/><ApiField
  name="prefill"
  type="[WorkloadSpec](#workloadspec)"
  required="false"
  description="Prefill configuration for disaggregated serving.<br />When this section is included, the controller creates a separate deployment for prompt processing (prefill)<br />in addition to the main 'decode' deployment, inspired by the llm-d architecture.<br />This allows for independent scaling and hardware allocation for prefill and decode steps."
/><ApiField
  name="baseRefs"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="BaseRefs allows inheriting and overriding configurations from one or more LLMInferenceServiceConfig instances.<br />The controller merges these base configurations, with the current LLMInferenceService spec taking the highest precedence.<br />When multiple baseRefs are provided, the last one in the list overrides previous ones."
/>


#### LLMInferenceServiceStatus



**Appears in:**
- [LLMInferenceService](#llminferenceservice)

LLMInferenceServiceStatus defines the observed state of LLMInferenceService.

##### Fields



<ApiField
  name="url"
  type="[URL](#url)"
  required="false"
  description="URL of the publicly exposed service."
/><ApiField
  name="observedGeneration"
  type="integer"
  required="false"
  description="ObservedGeneration is the 'Generation' of the Service that<br />was last processed by the controller."
/><ApiField
  name="conditions"
  type="[Conditions](#conditions)"
  required="false"
  description="Conditions the latest available observations of a resource's current state."
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="true"
  description="Annotations is additional Status fields for the Resource to save some<br />additional State as well as convey more information to the user. This is<br />roughly akin to Annotations on any k8s resource, just the reconciler conveying<br />richer information outwards."
/><ApiField
  name="address"
  type="[Addressable](#addressable)"
  required="false"
  description="Address is a single Addressable address.<br />If Addresses is present, Address will be ignored by clients."
/><ApiField
  name="addresses"
  type="[Addressable](#addressable) array"
  required="false"
  description="Addresses is a list of addresses for different protocols (HTTP and HTTPS)<br />If Addresses is present, Address must be ignored by clients."
/>



#### LLMModelSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)
- [LoRASpec](#loraspec)

LLMModelSpec defines the model source and its characteristics.

##### Fields



<ApiField
  name="uri"
  type="[URL](#url)"
  required="true"
  description="URI of the model, specifying its location, e.g., hf://meta-llama/Llama-4-Scout-17B-16E-Instruct<br />The storage-initializer init container uses this URI to download the model."
/><ApiField
  name="name"
  type="string"
  required="false"
  description="Name is the name of the model as it will be set in the &quot;model&quot; parameter for an incoming request.<br />If omitted, it will default to `metadata.name`. For LoRA adapters, this field is required."
/><ApiField
  name="lora"
  type="[LoRASpec](#loraspec)"
  required="false"
  description="LoRA (Low-Rank Adaptation) adapters configurations.<br />Allows for specifying one or more LoRA adapters to be applied to the base model."
/>


#### LoRASpec



**Appears in:**
- [LLMModelSpec](#llmmodelspec)

LoRASpec defines the configuration for LoRA adapters.

##### Fields



<ApiField
  name="adapters"
  type="[LLMModelSpec](#llmmodelspec) array"
  required="false"
  description="Adapters is the static specification for one or more LoRA adapters.<br />Each adapter is defined by its own ModelSpec.<br />This type is recursive https://github.com/kubernetes-sigs/controller-tools/issues/585"
/>


#### ParallelismSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)
- [WorkloadSpec](#workloadspec)

ParallelismSpec defines the parallelism parameters for distributed inference.

##### Fields



<ApiField
  name="tensor"
  type="integer"
  required="false"
  description="Tensor parallelism size."
/><ApiField
  name="pipeline"
  type="integer"
  required="false"
  description="Pipeline parallelism size."
/><ApiField
  name="data"
  type="integer"
  required="false"
  description="Data parallelism size."
/><ApiField
  name="dataLocal"
  type="integer"
  required="false"
  description="DataLocal data local parallelism size."
/><ApiField
  name="dataRPCPort"
  type="integer"
  required="false"
  description="DataRPCPort is the data parallelism RPC port."
/><ApiField
  name="expert"
  type="boolean"
  required="false"
  description="Expert enables expert parallelism."
/>


#### RouterSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)

RouterSpec defines the routing configuration for exposing the service.
It supports Kubernetes Ingress and the Gateway API. The fields are mutually exclusive.

##### Fields



<ApiField
  name="route"
  type="[GatewayRoutesSpec](#gatewayroutesspec)"
  required="false"
  description="Route configuration for the Gateway API.<br />If an empty object `\{\}` is provided, the controller creates and manages a new HTTPRoute."
/><ApiField
  name="gateway"
  type="[GatewaySpec](#gatewayspec)"
  required="false"
  description="Gateway configuration for the Gateway API, mutually exclusive with Ingress.<br />If an empty object `\{\}` is provided, the controller uses a default Gateway.<br />This must be used in conjunction with the 'Route' field for managed Gateway API resources."
/><ApiField
  name="ingress"
  type="[IngressSpec](#ingressspec)"
  required="false"
  description="Ingress configuration. This is mutually exclusive with Route and Gateway.<br />If an empty object `\{\}` is provided, the controller creates and manages a default Ingress resource."
/><ApiField
  name="scheduler"
  type="[SchedulerSpec](#schedulerspec)"
  required="false"
  description="Scheduler configuration for the Inference Gateway extension.<br />If this field is non-empty, an InferenceModel resource will be created to integrate with the gateway's scheduler."
/>


#### ScalingSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)
- [WorkloadSpec](#workloadspec)

ScalingSpec configures autoscaling for the LLM inference deployment.
When scaling is configured, the controller creates and manages autoscaling resources
(VariantAutoscaling CR, ServiceMonitor, and the selected actuator — HPA or KEDA ScaledObject).

##### Fields



<ApiField
  name="minReplicas"
  type="integer"
  required="false"
  description="MinReplicas is the minimum number of replicas for the deployment during active scaling.<br />This is the scaling floor when triggers are active.<br />For idle scale-down, use KEDA's idleReplicaCount instead.<br />Defaults to 1 if not specified."
/><ApiField
  name="maxReplicas"
  type="integer"
  required="false"
  description="MaxReplicas is the maximum number of replicas for the deployment."
/><ApiField
  name="wva"
  type="[WVASpec](#wvaspec)"
  required="false"
  description="WVA configures the Workload Variant Autoscaler (WVA) for scaling.<br />WVA scales based on a variety of inference metrics (KV cache utilization, queue depth, etc.)<br />rather than traditional CPU/memory metrics."
/>


#### SchedulerConfigSpec



**Appears in:**
- [SchedulerSpec](#schedulerspec)



##### Fields



<ApiField
  name="inline"
  type="[RawExtension](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#rawextension-runtime-pkg)"
  required="true"
  description="Inline EndpointPickerConfig"
/><ApiField
  name="ref"
  type="[ConfigMapKeySelector](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#configmapkeyselector-v1-core)"
  required="true"
  description="Ref is a reference to a ConfigMap key with EndpointPickerConfig."
/>


#### SchedulerSpec



**Appears in:**
- [RouterSpec](#routerspec)

SchedulerSpec defines the Inference Gateway extension configuration.

The SchedulerSpec configures the connection from the Gateway to the model deployment leveraging the LLM optimized
request Scheduler, also known as the Endpoint Picker (EPP) which determines the exact pod that should handle the
request and responds back to Envoy with the target pod, Envoy will then forward the request to the chosen pod.

The Scheduler is only effective when having multiple inference pod replicas.

Step 1: Gateway (Envoy) &lt;-- ExtProc --&gt; EPP (select the optimal replica to handle the request)
Step 2: Gateway (Envoy) &lt;-- forward request --&gt; Inference Pod X

##### Fields



<ApiField
  name="pool"
  type="[InferencePoolSpec](#inferencepoolspec)"
  required="false"
  description="Pool configuration for the InferencePool, which is part of the Inference Gateway extension."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the scheduler component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the scheduler component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="template"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Template for the Inference Gateway Extension pod spec.<br />This configures the Endpoint Picker (EPP) Deployment."
/><ApiField
  name="config"
  type="[SchedulerConfigSpec](#schedulerconfigspec)"
  required="true"
  description="Config is the configuration for the EndpointPicker."
/><ApiField
  name="replicas"
  type="integer"
  required="true"
  description="Replicas is the number of replicas for the scheduler."
/>


#### StorageInitializerSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)

StorageInitializerSpec defines the configuration for the storage initializer.
The storage initializer is an initContainer responsible for downloading model artifacts
from remote storage (s3://, hf://) before the main container starts.

Example - Disable storage initializer:

	storageInitializer:
	  enabled: false

Example - Explicitly enable (same as default):

	storageInitializer:
	  enabled: true

##### Fields



<ApiField
  name="enabled"
  type="boolean"
  required="false"
  description="Enabled controls whether the storage-initializer initContainer is created.<br />When nil or true, storage-initializer is created for applicable URIs (s3://, hf://).<br />When explicitly set to false, storage-initializer creation is skipped.<br />This is useful when models are pre-loaded via alternative mechanisms (e.g., custom init containers, modelcars).<br />Default: true (nil is treated as true for backward compatibility)"
/>


#### UntypedObjectReference



**Appears in:**
- [GatewaySpec](#gatewayspec)
- [IngressSpec](#ingressspec)

UntypedObjectReference is a reference to an object without a specific Group/Version/Kind.
It's used for referencing networking resources like Gateways and Ingresses where the exact type
might be inferred or is not strictly required by this controller.

##### Fields



<ApiField
  name="name"
  type="[ObjectName](#objectname)"
  required="true"
  description="Name of the referenced object."
/><ApiField
  name="namespace"
  type="[Namespace](#namespace)"
  required="true"
  description="Namespace of the referenced object."
/>


#### WVASpec



**Appears in:**
- [ScalingSpec](#scalingspec)

WVASpec configures the Workload Variant Autoscaler.

##### Fields



<ApiField
  name="variantCost"
  type="string"
  required="false"
  defaultValue="10.0"
  description="VariantCost specifies the cost per replica for this variant (used in saturation analysis).<br />Must be a non-negative numeric string (e.g., &quot;10&quot;, &quot;10.0&quot;, &quot;0.5&quot;).<br />Defaults to &quot;10.0&quot; if not specified."
/><ApiField
  name="hpa"
  type="[HPAScalingSpec](#hpascalingspec)"
  required="false"
  description="HPA configures the HorizontalPodAutoscaler as the actuator backend.<br />When specified, HPA reads the wva_desired_replicas metric via the Kubernetes Metrics API<br />(requires Prometheus Adapter) and scales the deployment accordingly.<br />Mutually exclusive with KEDA."
/><ApiField
  name="keda"
  type="[KEDAScalingSpec](#kedascalingspec)"
  required="false"
  description="KEDA configures a KEDA ScaledObject as the actuator backend.<br />When specified, KEDA queries Prometheus directly for the wva_desired_replicas metric<br />and scales the deployment accordingly. KEDA does not require a Prometheus Adapter.<br />Mutually exclusive with HPA."
/>


#### WorkloadSpec



**Appears in:**
- [LLMInferenceServiceSpec](#llminferenceservicespec)

WorkloadSpec defines the configuration for a deployment workload, such as replicas and pod specifications.

##### Fields



<ApiField
  name="replicas"
  type="integer"
  required="false"
  description="Number of replicas for the deployment."
/><ApiField
  name="scaling"
  type="[ScalingSpec](#scalingspec)"
  required="false"
  description="Scaling configuration for autoscaling this workload.<br />When specified, the controller creates and manages autoscaling resources<br />(VariantAutoscaling CR, ServiceMonitor, and the selected actuator — HPA or KEDA ScaledObject)<br />targeting this workload.<br />Mutually exclusive with the static 'replicas' field.<br />In a disaggregated setup, each workload (decode and prefill) can have its own independent scaling configuration,<br />resulting in separate autoscaling resources per workload."
/><ApiField
  name="parallelism"
  type="[ParallelismSpec](#parallelismspec)"
  required="false"
  description="Parallelism configurations for the runtime, such as tensor and pipeline parallelism.<br />These values are used to configure the underlying inference runtime (e.g., vLLM)."
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="template"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Template for the main pod spec.<br />In a multi-node deployment, this configures the &quot;head&quot; or &quot;master&quot; pod.<br />In a disaggregated deployment, this configures the &quot;decode&quot; pod if it's the top-level template,<br />or the &quot;prefill&quot; pod if it's within the Prefill block."
/><ApiField
  name="worker"
  type="[PodSpec](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podspec-v1-core)"
  required="false"
  description="Worker configuration for multi-node deployments.<br />The presence of this field triggers the creation of a multi-node (distributed) setup.<br />This spec defines the configuration for the worker pods, while the main 'Template' field defines the head pod.<br />The controller is responsible for enabling discovery between head and worker pods."
/>



## serving.kserve.io/v1beta1

Package v1beta1 contains API Schema definitions for the serving v1beta1 API group


## Resource Kinds

### Available Kinds
- [InferenceService](#inferenceservice)
- [InferenceServiceList](#inferenceservicelist)

### Kind Definitions
#### InferenceService



**Appears in:**
- [InferenceServiceList](#inferenceservicelist)

InferenceService is the Schema for the InferenceServices API

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1beta1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>InferenceService</code> resource"
/>

<ApiField
  name="metadata"
  type="[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#objectmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="spec"
  type="[InferenceServiceSpec](#inferenceservicespec)"
  required="true"
  description=""
/><ApiField
  name="status"
  type="[InferenceServiceStatus](#inferenceservicestatus)"
  required="true"
  description=""
/>


#### InferenceServiceList




InferenceServiceList contains a list of Service

##### Fields

<ApiField
  name="apiVersion"
  type="String"
  required="true"
  description="We are on version <code>serving.kserve.io/v1beta1</code> of the API."
/>

<ApiField
  name="kind"
  type="String"
  required="true"
  description="This is a <code>InferenceServiceList</code> resource"
/>

<ApiField
  name="metadata"
  type="[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#listmeta-v1-meta)"
  required="true"
  description="Refer to Kubernetes API documentation for fields of &quot;metadata&quot;."
/><ApiField
  name="items"
  type="[InferenceService](#inferenceservice) array"
  required="true"
  description=""
/>


## Supporting Types

### Available Types
- [ARTExplainerSpec](#artexplainerspec)
- [ARTExplainerType](#artexplainertype)
- [AuthenticationRef](#authenticationref)
- [AutoScalingSpec](#autoscalingspec)
- [AutoscalerConfig](#autoscalerconfig)
- [Batcher](#batcher)
- [Component](#component)
- [ComponentExtensionSpec](#componentextensionspec)
- [ComponentImplementation](#componentimplementation)
- [ComponentStatusSpec](#componentstatusspec)
- [ComponentType](#componenttype)
- [CustomExplainer](#customexplainer)
- [CustomPredictor](#custompredictor)
- [CustomTransformer](#customtransformer)
- [DeployConfig](#deployconfig)
- [DeploymentRolloutStrategy](#deploymentrolloutstrategy)
- [ExplainerConfig](#explainerconfig)
- [ExplainerExtensionSpec](#explainerextensionspec)
- [ExplainerSpec](#explainerspec)
- [ExplainersConfig](#explainersconfig)
- [ExtMetricAuthentication](#extmetricauthentication)
- [ExternalMetricSource](#externalmetricsource)
- [ExternalMetrics](#externalmetrics)
- [FailureInfo](#failureinfo)
- [FailureReason](#failurereason)
- [HuggingFaceRuntimeSpec](#huggingfaceruntimespec)
- [InferenceServiceDefaulter](#inferenceservicedefaulter)
- [InferenceServiceSpec](#inferenceservicespec)
- [InferenceServiceStatus](#inferenceservicestatus)
- [InferenceServiceValidator](#inferenceservicevalidator)
- [InferenceServicesConfig](#inferenceservicesconfig)
- [IngressConfig](#ingressconfig)
- [LightGBMSpec](#lightgbmspec)
- [LocalModelConfig](#localmodelconfig)
- [LoggerSpec](#loggerspec)
- [LoggerStorageSpec](#loggerstoragespec)
- [LoggerType](#loggertype)
- [MetricQuantity](#metricquantity)
- [MetricSourceType](#metricsourcetype)
- [MetricTarget](#metrictarget)
- [MetricTargetType](#metrictargettype)
- [MetricsBackend](#metricsbackend)
- [MetricsSpec](#metricsspec)
- [ModelCopies](#modelcopies)
- [ModelFormat](#modelformat)
- [ModelRevisionStates](#modelrevisionstates)
- [ModelSpec](#modelspec)
- [ModelState](#modelstate)
- [ModelStatus](#modelstatus)
- [ModelStorageSpec](#modelstoragespec)
- [MultiNodeConfig](#multinodeconfig)
- [ONNXRuntimeSpec](#onnxruntimespec)
- [OtelCollectorConfig](#otelcollectorconfig)
- [PMMLSpec](#pmmlspec)
- [PaddleServerSpec](#paddleserverspec)
- [PodMetricSource](#podmetricsource)
- [PodMetrics](#podmetrics)
- [PodSpec](#podspec)
- [PodsMetricsBackend](#podsmetricsbackend)
- [PredictorExtensionSpec](#predictorextensionspec)
- [PredictorImplementation](#predictorimplementation)
- [PredictorSpec](#predictorspec)
- [ResourceConfig](#resourceconfig)
- [ResourceMetric](#resourcemetric)
- [ResourceMetricSource](#resourcemetricsource)
- [RolloutSpec](#rolloutspec)
- [SKLearnSpec](#sklearnspec)
- [ScaleMetric](#scalemetric)
- [SecurityConfig](#securityconfig)
- [ServiceConfig](#serviceconfig)
- [StorageSpec](#storagespec)
- [StorageUri](#storageuri)
- [TFServingSpec](#tfservingspec)
- [TorchServeSpec](#torchservespec)
- [TransformerSpec](#transformerspec)
- [TransitionStatus](#transitionstatus)
- [TritonSpec](#tritonspec)
- [WorkerSpec](#workerspec)
- [XGBoostSpec](#xgboostspec)

### Type Definitions
#### ARTExplainerSpec



**Appears in:**
- [ExplainerSpec](#explainerspec)

ARTExplainerType defines the arguments for configuring an ART Explanation Server

##### Fields



<ApiField
  name="type"
  type="[ARTExplainerType](#artexplainertype)"
  required="true"
  description="The type of ART explainer"
/><ApiField
  name="storageUri"
  type="string"
  required="true"
  description="The location of a trained explanation model"
/><ApiField
  name="runtimeVersion"
  type="string"
  required="true"
  description="Defaults to latest Explainer Version"
/><ApiField
  name="config"
  type="object (keys:string, values:string)"
  required="true"
  description="Inline custom parameter settings for explainer"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### ARTExplainerType

**Underlying type:** string

**Appears in:**
- [ARTExplainerSpec](#artexplainerspec)





##### Possible Values

<ApiField
  name="SquareAttack"
  type="enum"
  required="false"
  description=""
/>
#### AuthenticationRef



**Appears in:**
- [ExtMetricAuthentication](#extmetricauthentication)



##### Fields



<ApiField
  name="name"
  type="string"
  required="true"
  description="name is the name of the authentication secret"
/>


#### AutoScalingSpec



**Appears in:**
- [ComponentExtensionSpec](#componentextensionspec)
- [ExplainerSpec](#explainerspec)
- [PredictorSpec](#predictorspec)
- [TransformerSpec](#transformerspec)



##### Fields



<ApiField
  name="metrics"
  type="[MetricsSpec](#metricsspec) array"
  required="true"
  description="metrics is a list of metrics spec to be used for autoscaling"
/><ApiField
  name="behavior"
  type="[HorizontalPodAutoscalerBehavior](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#horizontalpodautoscalerbehavior-v2-autoscaling)"
  required="false"
  description="Behavior contains the scaling behavior configuration for the Horizontal Pod Autoscaler."
/>



#### Batcher



**Appears in:**
- [ComponentExtensionSpec](#componentextensionspec)
- [ExplainerSpec](#explainerspec)
- [PredictorSpec](#predictorspec)
- [TransformerSpec](#transformerspec)

Batcher specifies optional payload batching available for all components

##### Fields



<ApiField
  name="maxBatchSize"
  type="integer"
  required="false"
  description="Specifies the max number of requests to trigger a batch"
/><ApiField
  name="maxLatency"
  type="integer"
  required="false"
  description="Specifies the max latency to trigger a batch"
/><ApiField
  name="timeout"
  type="integer"
  required="false"
  description="Specifies the timeout of a batch"
/>



#### ComponentExtensionSpec



**Appears in:**
- [ExplainerSpec](#explainerspec)
- [PredictorSpec](#predictorspec)
- [TransformerSpec](#transformerspec)

ComponentExtensionSpec defines the deployment configuration for a given InferenceService component

##### Fields



<ApiField
  name="minReplicas"
  type="integer"
  required="false"
  description="Minimum number of replicas, defaults to 1 but can be set to 0 to enable scale-to-zero."
/><ApiField
  name="maxReplicas"
  type="integer"
  required="false"
  description="Maximum number of replicas for autoscaling."
/><ApiField
  name="scaleTarget"
  type="integer"
  required="false"
  description="ScaleTarget specifies the integer target value of the metric type the Autoscaler watches for.<br />concurrency and rps targets are supported by Knative Pod Autoscaler<br />(https://knative.dev/docs/serving/autoscaling/autoscaling-targets/)."
/><ApiField
  name="scaleMetric"
  type="[ScaleMetric](#scalemetric)"
  required="false"
  description="ScaleMetric defines the scaling metric type watched by autoscaler.<br />possible values are concurrency, rps, cpu, memory. concurrency, rps are supported via<br />Knative Pod Autoscaler(https://knative.dev/docs/serving/autoscaling/autoscaling-metrics)."
/><ApiField
  name="scaleMetricType"
  type="[MetricTargetType](#metrictargettype)"
  required="false"
  description="Type of metric to use. Options are Utilization, or AverageValue."
/><ApiField
  name="autoScaling"
  type="[AutoScalingSpec](#autoscalingspec)"
  required="false"
  description="AutoScaling autoscaling spec which is backed up HPA or KEDA."
/><ApiField
  name="containerConcurrency"
  type="integer"
  required="false"
  description="ContainerConcurrency specifies how many requests can be processed concurrently, this sets the hard limit of the container<br />concurrency(https://knative.dev/docs/serving/autoscaling/concurrency)."
/><ApiField
  name="timeout"
  type="integer"
  required="false"
  description="TimeoutSeconds specifies the number of seconds to wait before timing out a request to the component."
/><ApiField
  name="canaryTrafficPercent"
  type="integer"
  required="false"
  description="CanaryTrafficPercent defines the traffic split percentage between the candidate revision and the last ready revision"
/><ApiField
  name="logger"
  type="[LoggerSpec](#loggerspec)"
  required="false"
  description="Activate request/response logging and logger configurations"
/><ApiField
  name="batcher"
  type="[Batcher](#batcher)"
  required="false"
  description="Activate request batching and batching configurations"
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="deploymentStrategy"
  type="[DeploymentStrategy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#deploymentstrategy-v1-apps)"
  required="false"
  description="The deployment strategy to use to replace existing pods with new ones. Only applicable for raw deployment mode."
/>



#### ComponentStatusSpec



**Appears in:**
- [InferenceServiceStatus](#inferenceservicestatus)

ComponentStatusSpec describes the state of the component

##### Fields



<ApiField
  name="latestReadyRevision"
  type="string"
  required="false"
  description="Latest revision name that is in ready state"
/><ApiField
  name="latestCreatedRevision"
  type="string"
  required="false"
  description="Latest revision name that is created"
/><ApiField
  name="previousRolledoutRevision"
  type="string"
  required="false"
  description="Previous revision name that is rolled out with 100 percent traffic"
/><ApiField
  name="latestRolledoutRevision"
  type="string"
  required="false"
  description="Latest revision name that is rolled out with 100 percent traffic"
/><ApiField
  name="traffic"
  type="TrafficTarget array"
  required="false"
  description="Traffic holds the configured traffic distribution for latest ready revision and previous rolled out revision."
/><ApiField
  name="url"
  type="[URL](#url)"
  required="false"
  description="URL holds the primary url that will distribute traffic over the provided traffic targets.<br />This will be one the REST or gRPC endpoints that are available.<br />It generally has the form http[s]://\{route-name\}.\{route-namespace\}.\{cluster-level-suffix\}"
/><ApiField
  name="restUrl"
  type="[URL](#url)"
  required="false"
  description="REST endpoint of the component if available."
/><ApiField
  name="grpcUrl"
  type="[URL](#url)"
  required="false"
  description="gRPC endpoint of the component if available."
/><ApiField
  name="address"
  type="[Addressable](#addressable)"
  required="false"
  description="Addressable endpoint for the InferenceService"
/>


#### ComponentType

**Underlying type:** string

**Appears in:**
- [InferenceServiceStatus](#inferenceservicestatus)

ComponentType contains the different types of components of the service



##### Possible Values

<ApiField
  name="predictor"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="explainer"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="transformer"
  type="enum"
  required="false"
  description=""
/>




#### DeploymentRolloutStrategy



**Appears in:**
- [DeployConfig](#deployconfig)

DeploymentRolloutStrategy defines the rollout strategy configuration for deployments

##### Fields



<ApiField
  name="defaultRollout"
  type="[RolloutSpec](#rolloutspec)"
  required="false"
  description="DefaultRollout specifies the default rollout configuration"
/>


#### ExplainerConfig



**Appears in:**
- [ExplainersConfig](#explainersconfig)



##### Fields



<ApiField
  name="image"
  type="string"
  required="true"
  description="explainer docker image name"
/><ApiField
  name="defaultImageVersion"
  type="string"
  required="true"
  description="default explainer docker image version"
/>


#### ExplainerExtensionSpec



**Appears in:**
- [ARTExplainerSpec](#artexplainerspec)

ExplainerExtensionSpec defines configuration shared across all explainer frameworks

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="true"
  description="The location of a trained explanation model"
/><ApiField
  name="runtimeVersion"
  type="string"
  required="true"
  description="Defaults to latest Explainer Version"
/><ApiField
  name="config"
  type="object (keys:string, values:string)"
  required="true"
  description="Inline custom parameter settings for explainer"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### ExplainerSpec



**Appears in:**
- [InferenceServiceSpec](#inferenceservicespec)

ExplainerSpec defines the container spec for a model explanation server,
The following fields follow a "1-of" semantic. Users must specify exactly one spec.

##### Fields



<ApiField
  name="art"
  type="[ARTExplainerSpec](#artexplainerspec)"
  required="true"
  description="Spec for ART explainer"
/><ApiField
  name="volumes"
  type="[Volume](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volume-v1-core) array"
  required="false"
  description="List of volumes that can be mounted by containers belonging to the pod.<br />More info: https://kubernetes.io/docs/concepts/storage/volumes"
/><ApiField
  name="initContainers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of initialization containers belonging to the pod.<br />Init containers are executed in order prior to containers being started. If any<br />init container fails, the pod is considered to have failed and is handled according<br />to its restartPolicy. The name for an init container or normal container must be<br />unique among all containers.<br />Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes.<br />The resourceRequirements of an init container are taken into account during scheduling<br />by finding the highest request/limit for each resource type, and then using the max of<br />that value or the sum of the normal containers. Limits are applied to init containers<br />in a similar fashion.<br />Init containers cannot currently be added or removed.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/"
/><ApiField
  name="containers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of containers belonging to the pod.<br />Containers cannot currently be added or removed.<br />There must be at least one container in a Pod.<br />Cannot be updated."
/><ApiField
  name="ephemeralContainers"
  type="[EphemeralContainer](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#ephemeralcontainer-v1-core) array"
  required="false"
  description="List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing<br />pod to perform user-initiated actions such as debugging. This list cannot be specified when<br />creating a pod, and it cannot be modified by updating the pod spec. In order to add an<br />ephemeral container to an existing pod, use the pod's ephemeralcontainers subresource."
/><ApiField
  name="restartPolicy"
  type="[RestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#restartpolicy-v1-core)"
  required="false"
  description="Restart policy for all containers within the pod.<br />One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted.<br />Default to Always.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy"
/><ApiField
  name="terminationGracePeriodSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request.<br />Value must be non-negative integer. The value zero indicates stop immediately via<br />the kill signal (no opportunity to shut down).<br />If this value is nil, the default grace period will be used instead.<br />The grace period is the duration in seconds after the processes running in the pod are sent<br />a termination signal and the time when the processes are forcibly halted with a kill signal.<br />Set this value longer than the expected cleanup time for your process.<br />Defaults to 30 seconds."
/><ApiField
  name="activeDeadlineSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod may be active on the node relative to<br />StartTime before the system will actively try to mark it failed and kill associated containers.<br />Value must be a positive integer."
/><ApiField
  name="dnsPolicy"
  type="[DNSPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#dnspolicy-v1-core)"
  required="false"
  description="Set DNS policy for the pod.<br />Defaults to &quot;ClusterFirst&quot;.<br />Valid values are 'ClusterFirstWithHostNet', 'ClusterFirst', 'Default' or 'None'.<br />DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy.<br />To have DNS options set along with hostNetwork, you have to specify DNS policy<br />explicitly to 'ClusterFirstWithHostNet'."
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector is a selector which must be true for the pod to fit on a node.<br />Selector which must match a node's labels for the pod to be scheduled on that node.<br />More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
/><ApiField
  name="serviceAccountName"
  type="string"
  required="false"
  description="ServiceAccountName is the name of the ServiceAccount to use to run this pod.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/"
/><ApiField
  name="serviceAccount"
  type="string"
  required="false"
  description="DeprecatedServiceAccount is a deprecated alias for ServiceAccountName.<br />Deprecated: Use serviceAccountName instead."
/><ApiField
  name="automountServiceAccountToken"
  type="boolean"
  required="false"
  description="AutomountServiceAccountToken indicates whether a service account token should be automatically mounted."
/><ApiField
  name="nodeName"
  type="string"
  required="false"
  description="NodeName indicates in which node this pod is scheduled.<br />If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName.<br />Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod.<br />This field should not be used to express a desire for the pod to be scheduled on a specific node.<br />https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename"
/><ApiField
  name="hostNetwork"
  type="boolean"
  required="false"
  description="Host networking requested for this pod. Use the host's network namespace.<br />If this option is set, the ports that will be used must be specified.<br />Default to false."
/><ApiField
  name="hostPID"
  type="boolean"
  required="false"
  description="Use the host's pid namespace.<br />Optional: Default to false."
/><ApiField
  name="hostIPC"
  type="boolean"
  required="false"
  description="Use the host's ipc namespace.<br />Optional: Default to false."
/><ApiField
  name="shareProcessNamespace"
  type="boolean"
  required="false"
  description="Share a single process namespace between all of the containers in a pod.<br />When this is set containers will be able to view and signal processes from other containers<br />in the same pod, and the first process in each container will not be assigned PID 1.<br />HostPID and ShareProcessNamespace cannot both be set.<br />Optional: Default to false."
/><ApiField
  name="securityContext"
  type="[PodSecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podsecuritycontext-v1-core)"
  required="false"
  description="SecurityContext holds pod-level security attributes and common container settings.<br />Optional: Defaults to empty.  See type description for default values of each field."
/><ApiField
  name="imagePullSecrets"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.<br />If specified, these secrets will be passed to individual puller implementations for them to use.<br />More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
/><ApiField
  name="hostname"
  type="string"
  required="false"
  description="Specifies the hostname of the Pod<br />If not specified, the pod's hostname will be set to a system-defined value."
/><ApiField
  name="subdomain"
  type="string"
  required="false"
  description="If specified, the fully qualified Pod hostname will be &quot;&lt;hostname&gt;.&lt;subdomain&gt;.&lt;pod namespace&gt;.svc.&lt;cluster domain&gt;&quot;.<br />If not specified, the pod will not have a domainname at all."
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description="If specified, the pod's scheduling constraints"
/><ApiField
  name="schedulerName"
  type="string"
  required="false"
  description="If specified, the pod will be dispatched by specified scheduler.<br />If not specified, the pod will be dispatched by default scheduler."
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="If specified, the pod's tolerations."
/><ApiField
  name="hostAliases"
  type="[HostAlias](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#hostalias-v1-core) array"
  required="false"
  description="HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts<br />file if specified."
/><ApiField
  name="priorityClassName"
  type="string"
  required="false"
  description="If specified, indicates the pod's priority. &quot;system-node-critical&quot; and<br />&quot;system-cluster-critical&quot; are two special keywords which indicate the<br />highest priorities with the former being the highest priority. Any other<br />name must be defined by creating a PriorityClass object with that name.<br />If not specified, the pod priority will be default or zero if there is no<br />default."
/><ApiField
  name="priority"
  type="integer"
  required="false"
  description="The priority value. Various system components use this field to find the<br />priority of the pod. When Priority Admission Controller is enabled, it<br />prevents users from setting this field. The admission controller populates<br />this field from PriorityClassName.<br />The higher the value, the higher the priority."
/><ApiField
  name="dnsConfig"
  type="[PodDNSConfig](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#poddnsconfig-v1-core)"
  required="false"
  description="Specifies the DNS parameters of a pod.<br />Parameters specified here will be merged to the generated DNS<br />configuration based on DNSPolicy."
/><ApiField
  name="readinessGates"
  type="[PodReadinessGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podreadinessgate-v1-core) array"
  required="false"
  description="If specified, all readiness gates will be evaluated for pod readiness.<br />A pod is ready when all its containers are ready AND<br />all conditions specified in the readiness gates have status equal to &quot;True&quot;<br />More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates"
/><ApiField
  name="runtimeClassName"
  type="string"
  required="false"
  description="RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used<br />to run this pod.  If no RuntimeClass resource matches the named class, the pod will not be run.<br />If unset or empty, the &quot;legacy&quot; RuntimeClass will be used, which is an implicit class with an<br />empty definition that uses the default runtime handler.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class"
/><ApiField
  name="enableServiceLinks"
  type="boolean"
  required="false"
  description="EnableServiceLinks indicates whether information about services should be injected into pod's<br />environment variables, matching the syntax of Docker links.<br />Optional: Defaults to true."
/><ApiField
  name="preemptionPolicy"
  type="[PreemptionPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#preemptionpolicy-v1-core)"
  required="false"
  description="PreemptionPolicy is the Policy for preempting pods with lower priority.<br />One of Never, PreemptLowerPriority.<br />Defaults to PreemptLowerPriority if unset."
/><ApiField
  name="overhead"
  type="[ResourceList](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcelist-v1-core)"
  required="false"
  description="Overhead represents the resource overhead associated with running a pod for a given RuntimeClass.<br />This field will be autopopulated at admission time by the RuntimeClass admission controller. If<br />the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests.<br />The RuntimeClass admission controller will reject Pod create requests which have the overhead already<br />set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value<br />defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md"
/><ApiField
  name="topologySpreadConstraints"
  type="[TopologySpreadConstraint](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#topologyspreadconstraint-v1-core) array"
  required="false"
  description="TopologySpreadConstraints describes how a group of pods ought to spread across topology<br />domains. Scheduler will schedule pods in a way which abides by the constraints.<br />All topologySpreadConstraints are ANDed."
/><ApiField
  name="setHostnameAsFQDN"
  type="boolean"
  required="false"
  description="If true the pod's hostname will be configured as the pod's FQDN, rather than the leaf name (the default).<br />In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname).<br />In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Services\\Tcpip\\Parameters to FQDN.<br />If a pod does not have FQDN, this has no effect.<br />Default to false."
/><ApiField
  name="os"
  type="[PodOS](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podos-v1-core)"
  required="false"
  description="Specifies the OS of the containers in the pod.<br />Some pod and container fields are restricted if this is set.<br />If the OS field is set to linux, the following fields must be unset:<br />-securityContext.windowsOptions<br />If the OS field is set to windows, following fields must be unset:<br />- spec.hostPID<br />- spec.hostIPC<br />- spec.hostUsers<br />- spec.securityContext.appArmorProfile<br />- spec.securityContext.seLinuxOptions<br />- spec.securityContext.seccompProfile<br />- spec.securityContext.fsGroup<br />- spec.securityContext.fsGroupChangePolicy<br />- spec.securityContext.sysctls<br />- spec.shareProcessNamespace<br />- spec.securityContext.runAsUser<br />- spec.securityContext.runAsGroup<br />- spec.securityContext.supplementalGroups<br />- spec.securityContext.supplementalGroupsPolicy<br />- spec.containers[*].securityContext.appArmorProfile<br />- spec.containers[*].securityContext.seLinuxOptions<br />- spec.containers[*].securityContext.seccompProfile<br />- spec.containers[*].securityContext.capabilities<br />- spec.containers[*].securityContext.readOnlyRootFilesystem<br />- spec.containers[*].securityContext.privileged<br />- spec.containers[*].securityContext.allowPrivilegeEscalation<br />- spec.containers[*].securityContext.procMount<br />- spec.containers[*].securityContext.runAsUser<br />- spec.containers[*].securityContext.runAsGroup"
/><ApiField
  name="hostUsers"
  type="boolean"
  required="false"
  description="Use the host's user namespace.<br />Optional: Default to true.<br />If set to true or not present, the pod will be run in the host user namespace, useful<br />for when the pod needs a feature only available to the host user namespace, such as<br />loading a kernel module with CAP_SYS_MODULE.<br />When set to false, a new userns is created for the pod. Setting false is useful for<br />mitigating container breakout vulnerabilities even allowing users to run their<br />containers as root without actually having root privileges on the host.<br />This field is alpha-level and is only honored by servers that enable the UserNamespacesSupport feature."
/><ApiField
  name="schedulingGates"
  type="[PodSchedulingGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podschedulinggate-v1-core) array"
  required="false"
  description="SchedulingGates is an opaque list of values that if specified will block scheduling the pod.<br />If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the<br />scheduler will not attempt to schedule the pod.<br />SchedulingGates can only be set at pod creation time, and be removed only afterwards."
/><ApiField
  name="resourceClaims"
  type="[PodResourceClaim](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podresourceclaim-v1-core) array"
  required="false"
  description="ResourceClaims defines which ResourceClaims must be allocated<br />and reserved before the Pod is allowed to start. The resources<br />will be made available to those containers which consume them<br />by name.<br />This is an alpha field and requires enabling the<br />DynamicResourceAllocation feature gate.<br />This field is immutable."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Resources is the total amount of CPU and Memory resources required by all<br />containers in the pod. It supports specifying Requests and Limits for<br />&quot;cpu&quot; and &quot;memory&quot; resource names only. ResourceClaims are not supported.<br />This field enables fine-grained control over resource allocation for the<br />entire pod, allowing resource sharing among containers in a pod.<br />This is an alpha field and requires enabling the PodLevelResources feature<br />gate."
/><ApiField
  name="hostnameOverride"
  type="string"
  required="false"
  description="HostnameOverride specifies an explicit override for the pod's hostname as perceived by the pod.<br />This field only specifies the pod's hostname and does not affect its DNS records.<br />When this field is set to a non-empty string:<br />- It takes precedence over the values set in `hostname` and `subdomain`.<br />- The Pod's hostname will be set to this value.<br />- `setHostnameAsFQDN` must be nil or set to false.<br />- `hostNetwork` must be set to false.<br />This field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters.<br />Requires the HostnameOverride feature gate to be enabled."
/><ApiField
  name="minReplicas"
  type="integer"
  required="false"
  description="Minimum number of replicas, defaults to 1 but can be set to 0 to enable scale-to-zero."
/><ApiField
  name="maxReplicas"
  type="integer"
  required="false"
  description="Maximum number of replicas for autoscaling."
/><ApiField
  name="scaleTarget"
  type="integer"
  required="false"
  description="ScaleTarget specifies the integer target value of the metric type the Autoscaler watches for.<br />concurrency and rps targets are supported by Knative Pod Autoscaler<br />(https://knative.dev/docs/serving/autoscaling/autoscaling-targets/)."
/><ApiField
  name="scaleMetric"
  type="[ScaleMetric](#scalemetric)"
  required="false"
  description="ScaleMetric defines the scaling metric type watched by autoscaler.<br />possible values are concurrency, rps, cpu, memory. concurrency, rps are supported via<br />Knative Pod Autoscaler(https://knative.dev/docs/serving/autoscaling/autoscaling-metrics)."
/><ApiField
  name="scaleMetricType"
  type="[MetricTargetType](#metrictargettype)"
  required="false"
  description="Type of metric to use. Options are Utilization, or AverageValue."
/><ApiField
  name="autoScaling"
  type="[AutoScalingSpec](#autoscalingspec)"
  required="false"
  description="AutoScaling autoscaling spec which is backed up HPA or KEDA."
/><ApiField
  name="containerConcurrency"
  type="integer"
  required="false"
  description="ContainerConcurrency specifies how many requests can be processed concurrently, this sets the hard limit of the container<br />concurrency(https://knative.dev/docs/serving/autoscaling/concurrency)."
/><ApiField
  name="timeout"
  type="integer"
  required="false"
  description="TimeoutSeconds specifies the number of seconds to wait before timing out a request to the component."
/><ApiField
  name="canaryTrafficPercent"
  type="integer"
  required="false"
  description="CanaryTrafficPercent defines the traffic split percentage between the candidate revision and the last ready revision"
/><ApiField
  name="logger"
  type="[LoggerSpec](#loggerspec)"
  required="false"
  description="Activate request/response logging and logger configurations"
/><ApiField
  name="batcher"
  type="[Batcher](#batcher)"
  required="false"
  description="Activate request batching and batching configurations"
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="deploymentStrategy"
  type="[DeploymentStrategy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#deploymentstrategy-v1-apps)"
  required="false"
  description="The deployment strategy to use to replace existing pods with new ones. Only applicable for raw deployment mode."
/><ApiField
  name="storageUris"
  type="[StorageUri](#storageuri) array"
  required="true"
  description="Spec for multiple storage uris."
/>


#### ExplainersConfig



**Appears in:**
- [InferenceServicesConfig](#inferenceservicesconfig)



##### Fields



<ApiField
  name="art"
  type="[ExplainerConfig](#explainerconfig)"
  required="true"
  description=""
/>


#### ExtMetricAuthentication



**Appears in:**
- [ExternalMetricSource](#externalmetricsource)



##### Fields



<ApiField
  name="authenticationRef"
  type="[AuthenticationRef](#authenticationref)"
  required="true"
  description="authenticationRef is a reference to the authentication information<br />for more information see: https://keda.sh/docs/2.17/scalers/prometheus/#authentication-parameters"
/><ApiField
  name="authModes"
  type="string"
  required="false"
  description="authModes defines the authentication modes for the metrics backend<br />possible values are bearer, basic, tls.<br />for more information see: https://keda.sh/docs/2.17/scalers/prometheus/#authentication-parameters"
/>


#### ExternalMetricSource



**Appears in:**
- [MetricsSpec](#metricsspec)



##### Fields



<ApiField
  name="metric"
  type="[ExternalMetrics](#externalmetrics)"
  required="true"
  description="metric identifies the target metric by name and selector"
/><ApiField
  name="authenticationRef"
  type="[ExtMetricAuthentication](#extmetricauthentication)"
  required="false"
  description="authenticationRef is a reference to the authentication information<br />for more information see: https://keda.sh/docs/2.17/scalers/prometheus/#authentication-parameters"
/><ApiField
  name="target"
  type="[MetricTarget](#metrictarget)"
  required="true"
  description="target specifies the target value for the given metric"
/>


#### ExternalMetrics



**Appears in:**
- [ExternalMetricSource](#externalmetricsource)



##### Fields



<ApiField
  name="backend"
  type="[MetricsBackend](#metricsbackend)"
  required="false"
  description="MetricsBackend defines the scaling metric type watched by autoscaler<br />possible values are prometheus, graphite."
/><ApiField
  name="serverAddress"
  type="string"
  required="false"
  description="Address of MetricsBackend server."
/><ApiField
  name="query"
  type="string"
  required="false"
  description="Query to run to get metrics from MetricsBackend"
/><ApiField
  name="namespace"
  type="string"
  required="false"
  description="For namespaced query"
/>


#### FailureInfo



**Appears in:**
- [ModelStatus](#modelstatus)



##### Fields



<ApiField
  name="location"
  type="string"
  required="false"
  description="Name of component to which the failure relates (usually Pod name)"
/><ApiField
  name="reason"
  type="[FailureReason](#failurereason)"
  required="false"
  description="High level class of failure"
/><ApiField
  name="message"
  type="string"
  required="false"
  description="Detailed error message"
/><ApiField
  name="modelRevisionName"
  type="string"
  required="false"
  description="Internal Revision/ID of model, tied to specific Spec contents"
/><ApiField
  name="time"
  type="[Time](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#time-v1-meta)"
  required="false"
  description="Time failure occurred or was discovered"
/><ApiField
  name="exitCode"
  type="integer"
  required="false"
  description="Exit status from the last termination of the container"
/>


#### FailureReason

**Underlying type:** string

**Appears in:**
- [FailureInfo](#failureinfo)

FailureReason enum



##### Possible Values

<ApiField
  name="ModelLoadFailed"
  type="enum"
  required="false"
  description="The model failed to load within a ServingRuntime container<br />"
/><ApiField
  name="RuntimeUnhealthy"
  type="enum"
  required="false"
  description="Corresponding ServingRuntime containers failed to start or are unhealthy<br />"
/><ApiField
  name="RuntimeDisabled"
  type="enum"
  required="false"
  description="The ServingRuntime is disabled<br />"
/><ApiField
  name="NoSupportingRuntime"
  type="enum"
  required="false"
  description="There are no ServingRuntime which support the specified model type<br />"
/><ApiField
  name="RuntimeNotRecognized"
  type="enum"
  required="false"
  description="There is no ServingRuntime defined with the specified runtime name<br />"
/><ApiField
  name="InvalidPredictorSpec"
  type="enum"
  required="false"
  description="The current Predictor Spec is invalid or unsupported<br />"
/>
#### HuggingFaceRuntimeSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

HuggingFaceRuntimeSpec defines arguments for configuring HuggingFace model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>



#### InferenceServiceSpec



**Appears in:**
- [InferenceService](#inferenceservice)

InferenceServiceSpec is the top level type for this resource

##### Fields



<ApiField
  name="predictor"
  type="[PredictorSpec](#predictorspec)"
  required="true"
  description="Predictor defines the model serving spec"
/><ApiField
  name="explainer"
  type="[ExplainerSpec](#explainerspec)"
  required="false"
  description="Explainer defines the model explanation service spec,<br />explainer service calls to predictor or transformer if it is specified."
/><ApiField
  name="transformer"
  type="[TransformerSpec](#transformerspec)"
  required="false"
  description="Transformer defines the pre/post processing before and after the predictor call,<br />transformer service calls to predictor service."
/>


#### InferenceServiceStatus



**Appears in:**
- [InferenceService](#inferenceservice)

InferenceServiceStatus defines the observed state of InferenceService

##### Fields



<ApiField
  name="observedGeneration"
  type="integer"
  required="false"
  description="ObservedGeneration is the 'Generation' of the Service that<br />was last processed by the controller."
/><ApiField
  name="conditions"
  type="[Conditions](#conditions)"
  required="false"
  description="Conditions the latest available observations of a resource's current state."
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="true"
  description="Annotations is additional Status fields for the Resource to save some<br />additional State as well as convey more information to the user. This is<br />roughly akin to Annotations on any k8s resource, just the reconciler conveying<br />richer information outwards."
/><ApiField
  name="address"
  type="[Addressable](#addressable)"
  required="false"
  description="Addressable endpoint for the InferenceService"
/><ApiField
  name="url"
  type="[URL](#url)"
  required="false"
  description="URL holds the url that will distribute traffic over the provided traffic targets.<br />It generally has the form http[s]://\{route-name\}.\{route-namespace\}.\{cluster-level-suffix\}"
/><ApiField
  name="components"
  type="object (keys:[ComponentType](#componenttype), values:[ComponentStatusSpec](#componentstatusspec))"
  required="true"
  description="Statuses for the components of the InferenceService"
/><ApiField
  name="modelStatus"
  type="[ModelStatus](#modelstatus)"
  required="true"
  description="Model related statuses"
/><ApiField
  name="deploymentMode"
  type="string"
  required="true"
  description="InferenceService DeploymentMode"
/><ApiField
  name="servingRuntimeName"
  type="string"
  required="true"
  description="ServingRuntimeName is the name of the ServingRuntime that the InferenceService is using"
/><ApiField
  name="clusterServingRuntimeName"
  type="string"
  required="true"
  description="ClusterServingRuntimeName is the name of the ClusterServingRuntime that the InferenceService is using"
/>





#### LightGBMSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

LightGBMSpec defines arguments for configuring LightGBMSpec model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>



#### LoggerSpec



**Appears in:**
- [ComponentExtensionSpec](#componentextensionspec)
- [ExplainerSpec](#explainerspec)
- [PredictorSpec](#predictorspec)
- [TransformerSpec](#transformerspec)

LoggerSpec specifies optional payload logging available for all components

##### Fields



<ApiField
  name="url"
  type="string"
  required="false"
  description="URL to send logging events"
/><ApiField
  name="mode"
  type="[LoggerType](#loggertype)"
  required="false"
  description="Specifies the scope of the loggers. <br />Valid values are: <br />- &quot;all&quot; (default): log both request and response; <br />- &quot;request&quot;: log only request; <br />- &quot;response&quot;: log only response <br />"
/><ApiField
  name="metadataHeaders"
  type="string array"
  required="false"
  description="Matched metadata HTTP headers for propagating to inference logger cloud events."
/><ApiField
  name="metadataAnnotations"
  type="string array"
  required="false"
  description="Matched inference service annotations for propagating to inference logger cloud events."
/><ApiField
  name="storage"
  type="[LoggerStorageSpec](#loggerstoragespec)"
  required="false"
  description="Specifies the storage location for the inference logger cloud events."
/><ApiField
  name="marshallerUrl"
  type="string"
  required="false"
  description="URL of the log marshaller service that transforms log records before storage.<br />Defaults to the embedded JSON marshaller at http://localhost:9083/marshal."
/><ApiField
  name="batchSize"
  type="integer"
  required="false"
  description="Number of log records per batch for blob storage. Defaults to 1 (immediate)."
/><ApiField
  name="batchInterval"
  type="string"
  required="false"
  description="Max duration to wait before flushing a partial batch (e.g. &quot;5s&quot;, &quot;100ms&quot;).<br />Only used when BatchSize > 1. Defaults to &quot;0&quot; (no time-based flushing)."
/>


#### LoggerStorageSpec



**Appears in:**
- [LoggerSpec](#loggerspec)



##### Fields



<ApiField
  name="path"
  type="string"
  required="false"
  description="The path to the object in the storage. Note that this path is relative to the storage URI."
/><ApiField
  name="parameters"
  type="map[string]string"
  required="false"
  description="Parameters to override the default storage credentials and config."
/><ApiField
  name="key"
  type="string"
  required="false"
  description="The Storage Key in the secret for this object."
/><ApiField
  name="serviceAccountName"
  type="string"
  required="true"
  description=""
/>


#### LoggerType

**Underlying type:** string

**Appears in:**
- [LoggerSpec](#loggerspec)

LoggerType controls the scope of log publishing



##### Possible Values

<ApiField
  name="all"
  type="enum"
  required="false"
  description="LogAll Logger mode to log both request and response<br />"
/><ApiField
  name="request"
  type="enum"
  required="false"
  description="LogRequest Logger mode to log only request<br />"
/><ApiField
  name="response"
  type="enum"
  required="false"
  description="LogResponse Logger mode to log only response<br />"
/>
#### MetricQuantity



**Appears in:**
- [MetricTarget](#metrictarget)

MetricQuantity preserves both the original string and parsed Quantity




#### MetricSourceType

**Underlying type:** string

**Appears in:**
- [MetricsSpec](#metricsspec)

MetricSourceType indicates the type of metric.



##### Possible Values

<ApiField
  name="Resource"
  type="enum"
  required="false"
  description="ResourceMetricSourceType is a resource metric known to Kubernetes, as<br />specified in requests and limits, describing each pod in the current<br />scale target (e.g. CPU or memory).  Such metrics are built in to<br />Kubernetes, and have special scaling options on top of those available<br />to normal per-pod metrics (the &quot;pods&quot; source).<br />"
/><ApiField
  name="External"
  type="enum"
  required="false"
  description="ExternalMetricSourceType is a global metric that is not associated<br />with any Kubernetes object. It allows autoscaling based on information<br />coming from components running outside of cluster<br />(for example length of queue in cloud messaging service, or<br />QPS from loadbalancer running outside of cluster).<br />"
/><ApiField
  name="PodMetric"
  type="enum"
  required="false"
  description="PodMetricSourceType indicates a metric describing each pod in the current<br />scale target (for example, transactions-processed-per-second).  The values<br />will be averaged together before being compared to the target value.<br />"
/>
#### MetricTarget



**Appears in:**
- [ExternalMetricSource](#externalmetricsource)
- [PodMetricSource](#podmetricsource)
- [ResourceMetricSource](#resourcemetricsource)

MetricTarget defines the target value, average value, or average utilization of a specific metric

##### Fields



<ApiField
  name="type"
  type="[MetricTargetType](#metrictargettype)"
  required="false"
  description="type represents whether the metric type is Utilization, Value, or AverageValue"
/><ApiField
  name="value"
  type="[MetricQuantity](#metricquantity)"
  required="false"
  description="value is the target value of the metric (as a quantity)."
/><ApiField
  name="averageValue"
  type="[MetricQuantity](#metricquantity)"
  required="false"
  description="averageValue is the target value of the average of the<br />metric across all relevant pods (as a quantity)"
/><ApiField
  name="averageUtilization"
  type="integer"
  required="false"
  description="averageUtilization is the target value of the average of the<br />resource metric across all relevant pods, represented as a percentage of<br />the requested value of the resource for the pods.<br />Currently only valid for Resource metric source type"
/>


#### MetricTargetType

**Underlying type:** string

**Appears in:**
- [ComponentExtensionSpec](#componentextensionspec)
- [ExplainerSpec](#explainerspec)
- [MetricTarget](#metrictarget)
- [PredictorSpec](#predictorspec)
- [TransformerSpec](#transformerspec)

MetricTargetType specifies the type of metric being targeted, and should be either
"Value", "AverageValue", or "Utilization"



##### Possible Values

<ApiField
  name="Utilization"
  type="enum"
  required="false"
  description="UtilizationMetricType declares a MetricTarget is an AverageUtilization value<br />"
/><ApiField
  name="Value"
  type="enum"
  required="false"
  description="ValueMetricType declares a MetricTarget is a raw value<br />"
/><ApiField
  name="AverageValue"
  type="enum"
  required="false"
  description="AverageValueMetricType declares a MetricTarget is an<br />"
/>
#### MetricsBackend

**Underlying type:** string

**Appears in:**
- [ExternalMetrics](#externalmetrics)

MetricsBackend enum



##### Possible Values

<ApiField
  name="prometheus"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="graphite"
  type="enum"
  required="false"
  description=""
/>
#### MetricsSpec



**Appears in:**
- [AutoScalingSpec](#autoscalingspec)

MetricsSpec specifies how to scale based on a single metric
(only `type` and one other matching field should be set at once).

##### Fields



<ApiField
  name="type"
  type="[MetricSourceType](#metricsourcetype)"
  required="true"
  description="type is the type of metric source.  It should be one of &quot;Resource&quot;, &quot;External&quot;, &quot;PodMetric&quot;.<br />&quot;Resource&quot; or &quot;External&quot; each mapping to a matching field in the object."
/><ApiField
  name="resource"
  type="[ResourceMetricSource](#resourcemetricsource)"
  required="false"
  description="resource refers to a resource metric (such as those specified in<br />requests and limits) known to Kubernetes describing each pod in the<br />current scale target (e.g. CPU or memory). Such metrics are built in to<br />Kubernetes, and have special scaling options on top of those available<br />to normal per-pod metrics using the &quot;pods&quot; source."
/><ApiField
  name="external"
  type="[ExternalMetricSource](#externalmetricsource)"
  required="false"
  description="external refers to a global metric that is not associated<br />with any Kubernetes object. It allows autoscaling based on information<br />coming from components running outside of cluster<br />(for example length of queue in cloud messaging service, or<br />QPS from load balancer running outside of cluster)."
/><ApiField
  name="podmetric"
  type="[PodMetricSource](#podmetricsource)"
  required="false"
  description="pods refers to a metric describing each pod in the current scale target<br />(for example, transactions-processed-per-second).  The values will be<br />averaged together before being compared to the target value."
/>


#### ModelCopies



**Appears in:**
- [ModelStatus](#modelstatus)



##### Fields



<ApiField
  name="failedCopies"
  type="integer"
  required="true"
  defaultValue="0"
  description="How many copies of this predictor's models failed to load recently"
/><ApiField
  name="totalCopies"
  type="integer"
  required="false"
  description="Total number of copies of this predictor's models across all states (Pending, Loading, Loaded, FailedToLoad)."
/>


#### ModelFormat



**Appears in:**
- [ModelSpec](#modelspec)



##### Fields



<ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the model format."
/><ApiField
  name="version"
  type="string"
  required="false"
  description="Version of the model format.<br />Used in validating that a predictor is supported by a runtime.<br />Can be &quot;major&quot;, &quot;major.minor&quot; or &quot;major.minor.patch&quot;."
/>


#### ModelRevisionStates



**Appears in:**
- [ModelStatus](#modelstatus)



##### Fields



<ApiField
  name="activeModelState"
  type="[ModelState](#modelstate)"
  required="true"
  defaultValue="Pending"
  description="High level state string: Pending, Standby, Loading, Loaded, FailedToLoad"
/><ApiField
  name="targetModelState"
  type="[ModelState](#modelstate)"
  required="true"
  description=""
/>


#### ModelSpec



**Appears in:**
- [PredictorSpec](#predictorspec)



##### Fields



<ApiField
  name="modelFormat"
  type="[ModelFormat](#modelformat)"
  required="true"
  description="ModelFormat being served."
/><ApiField
  name="runtime"
  type="string"
  required="false"
  description="Specific ClusterServingRuntime/ServingRuntime name to use for deployment."
/><ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### ModelState

**Underlying type:** string

**Appears in:**
- [ModelRevisionStates](#modelrevisionstates)

ModelState enum



##### Possible Values

<ApiField
  name="Pending"
  type="enum"
  required="false"
  description="Model is not yet registered<br />"
/><ApiField
  name="Standby"
  type="enum"
  required="false"
  description="Model is available but not loaded (will load when used)<br />"
/><ApiField
  name="Loading"
  type="enum"
  required="false"
  description="Model is loading<br />"
/><ApiField
  name="Loaded"
  type="enum"
  required="false"
  description="At least one copy of the model is loaded<br />"
/><ApiField
  name="FailedToLoad"
  type="enum"
  required="false"
  description="All copies of the model failed to load<br />"
/>
#### ModelStatus



**Appears in:**
- [InferenceServiceStatus](#inferenceservicestatus)



##### Fields



<ApiField
  name="transitionStatus"
  type="[TransitionStatus](#transitionstatus)"
  required="true"
  defaultValue="UpToDate"
  description="Whether the available predictor endpoints reflect the current Spec or is in transition"
/><ApiField
  name="states"
  type="[ModelRevisionStates](#modelrevisionstates)"
  required="false"
  description="State information of the predictor's model."
/><ApiField
  name="lastFailureInfo"
  type="[FailureInfo](#failureinfo)"
  required="false"
  description="Details of last failure, when load of target model is failed or blocked."
/><ApiField
  name="copies"
  type="[ModelCopies](#modelcopies)"
  required="false"
  description="Model copy information of the predictor's model."
/>


#### ModelStorageSpec



**Appears in:**
- [ARTExplainerSpec](#artexplainerspec)
- [ExplainerExtensionSpec](#explainerextensionspec)
- [HuggingFaceRuntimeSpec](#huggingfaceruntimespec)
- [LightGBMSpec](#lightgbmspec)
- [ModelSpec](#modelspec)
- [ONNXRuntimeSpec](#onnxruntimespec)
- [PMMLSpec](#pmmlspec)
- [PaddleServerSpec](#paddleserverspec)
- [PredictorExtensionSpec](#predictorextensionspec)
- [SKLearnSpec](#sklearnspec)
- [TFServingSpec](#tfservingspec)
- [TorchServeSpec](#torchservespec)
- [TritonSpec](#tritonspec)
- [XGBoostSpec](#xgboostspec)



##### Fields



<ApiField
  name="path"
  type="string"
  required="false"
  description="The path to the object in the storage. Note that this path is relative to the storage URI."
/><ApiField
  name="parameters"
  type="map[string]string"
  required="false"
  description="Parameters to override the default storage credentials and config."
/><ApiField
  name="key"
  type="string"
  required="false"
  description="The Storage Key in the secret for this object."
/><ApiField
  name="schemaPath"
  type="string"
  required="false"
  description="The path to the model schema file in the storage."
/>



#### ONNXRuntimeSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

ONNXRuntimeSpec defines arguments for configuring ONNX model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>



#### PMMLSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

PMMLSpec defines arguments for configuring PMML model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### PaddleServerSpec



**Appears in:**
- [PredictorSpec](#predictorspec)



##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### PodMetricSource



**Appears in:**
- [MetricsSpec](#metricsspec)

PodMetricSource indicates how to scale on a metric describing each pod in
the current scale target (for example, transactions-processed-per-second).
The values will be averaged together before being compared to the target
value.

##### Fields



<ApiField
  name="metric"
  type="[PodMetrics](#podmetrics)"
  required="true"
  description="metric identifies the target metric by name and selector"
/><ApiField
  name="target"
  type="[MetricTarget](#metrictarget)"
  required="true"
  description="target specifies the target value for the given metric"
/>


#### PodMetrics



**Appears in:**
- [PodMetricSource](#podmetricsource)



##### Fields



<ApiField
  name="backend"
  type="[PodsMetricsBackend](#podsmetricsbackend)"
  required="false"
  description="Backend defines the scaling metric type watched by the autoscaler.<br />Possible value: opentelemetry."
/><ApiField
  name="serverAddress"
  type="string"
  required="false"
  description="ServerAddress specifies the address of the PodsMetricsBackend server."
/><ApiField
  name="metricNames"
  type="string array"
  required="false"
  description="MetricNames is the list of metric names in the backend."
/><ApiField
  name="query"
  type="string"
  required="false"
  description="Query specifies the query to run to get metrics from the PodsMetricsBackend."
/><ApiField
  name="operationOverTime"
  type="string"
  required="false"
  description="OperationOverTime specifies the operation to aggregate the metrics over time.<br />Possible values are last_one, avg, max, min, rate, count. Default is 'last_one'."
/>


#### PodSpec



**Appears in:**
- [ExplainerSpec](#explainerspec)
- [PredictorSpec](#predictorspec)
- [TransformerSpec](#transformerspec)
- [WorkerSpec](#workerspec)

PodSpec is a description of a pod.

##### Fields



<ApiField
  name="volumes"
  type="[Volume](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volume-v1-core) array"
  required="false"
  description="List of volumes that can be mounted by containers belonging to the pod.<br />More info: https://kubernetes.io/docs/concepts/storage/volumes"
/><ApiField
  name="initContainers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of initialization containers belonging to the pod.<br />Init containers are executed in order prior to containers being started. If any<br />init container fails, the pod is considered to have failed and is handled according<br />to its restartPolicy. The name for an init container or normal container must be<br />unique among all containers.<br />Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes.<br />The resourceRequirements of an init container are taken into account during scheduling<br />by finding the highest request/limit for each resource type, and then using the max of<br />that value or the sum of the normal containers. Limits are applied to init containers<br />in a similar fashion.<br />Init containers cannot currently be added or removed.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/"
/><ApiField
  name="containers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of containers belonging to the pod.<br />Containers cannot currently be added or removed.<br />There must be at least one container in a Pod.<br />Cannot be updated."
/><ApiField
  name="ephemeralContainers"
  type="[EphemeralContainer](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#ephemeralcontainer-v1-core) array"
  required="false"
  description="List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing<br />pod to perform user-initiated actions such as debugging. This list cannot be specified when<br />creating a pod, and it cannot be modified by updating the pod spec. In order to add an<br />ephemeral container to an existing pod, use the pod's ephemeralcontainers subresource."
/><ApiField
  name="restartPolicy"
  type="[RestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#restartpolicy-v1-core)"
  required="false"
  description="Restart policy for all containers within the pod.<br />One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted.<br />Default to Always.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy"
/><ApiField
  name="terminationGracePeriodSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request.<br />Value must be non-negative integer. The value zero indicates stop immediately via<br />the kill signal (no opportunity to shut down).<br />If this value is nil, the default grace period will be used instead.<br />The grace period is the duration in seconds after the processes running in the pod are sent<br />a termination signal and the time when the processes are forcibly halted with a kill signal.<br />Set this value longer than the expected cleanup time for your process.<br />Defaults to 30 seconds."
/><ApiField
  name="activeDeadlineSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod may be active on the node relative to<br />StartTime before the system will actively try to mark it failed and kill associated containers.<br />Value must be a positive integer."
/><ApiField
  name="dnsPolicy"
  type="[DNSPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#dnspolicy-v1-core)"
  required="false"
  description="Set DNS policy for the pod.<br />Defaults to &quot;ClusterFirst&quot;.<br />Valid values are 'ClusterFirstWithHostNet', 'ClusterFirst', 'Default' or 'None'.<br />DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy.<br />To have DNS options set along with hostNetwork, you have to specify DNS policy<br />explicitly to 'ClusterFirstWithHostNet'."
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector is a selector which must be true for the pod to fit on a node.<br />Selector which must match a node's labels for the pod to be scheduled on that node.<br />More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
/><ApiField
  name="serviceAccountName"
  type="string"
  required="false"
  description="ServiceAccountName is the name of the ServiceAccount to use to run this pod.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/"
/><ApiField
  name="serviceAccount"
  type="string"
  required="false"
  description="DeprecatedServiceAccount is a deprecated alias for ServiceAccountName.<br />Deprecated: Use serviceAccountName instead."
/><ApiField
  name="automountServiceAccountToken"
  type="boolean"
  required="false"
  description="AutomountServiceAccountToken indicates whether a service account token should be automatically mounted."
/><ApiField
  name="nodeName"
  type="string"
  required="false"
  description="NodeName indicates in which node this pod is scheduled.<br />If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName.<br />Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod.<br />This field should not be used to express a desire for the pod to be scheduled on a specific node.<br />https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename"
/><ApiField
  name="hostNetwork"
  type="boolean"
  required="false"
  description="Host networking requested for this pod. Use the host's network namespace.<br />If this option is set, the ports that will be used must be specified.<br />Default to false."
/><ApiField
  name="hostPID"
  type="boolean"
  required="false"
  description="Use the host's pid namespace.<br />Optional: Default to false."
/><ApiField
  name="hostIPC"
  type="boolean"
  required="false"
  description="Use the host's ipc namespace.<br />Optional: Default to false."
/><ApiField
  name="shareProcessNamespace"
  type="boolean"
  required="false"
  description="Share a single process namespace between all of the containers in a pod.<br />When this is set containers will be able to view and signal processes from other containers<br />in the same pod, and the first process in each container will not be assigned PID 1.<br />HostPID and ShareProcessNamespace cannot both be set.<br />Optional: Default to false."
/><ApiField
  name="securityContext"
  type="[PodSecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podsecuritycontext-v1-core)"
  required="false"
  description="SecurityContext holds pod-level security attributes and common container settings.<br />Optional: Defaults to empty.  See type description for default values of each field."
/><ApiField
  name="imagePullSecrets"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.<br />If specified, these secrets will be passed to individual puller implementations for them to use.<br />More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
/><ApiField
  name="hostname"
  type="string"
  required="false"
  description="Specifies the hostname of the Pod<br />If not specified, the pod's hostname will be set to a system-defined value."
/><ApiField
  name="subdomain"
  type="string"
  required="false"
  description="If specified, the fully qualified Pod hostname will be &quot;&lt;hostname&gt;.&lt;subdomain&gt;.&lt;pod namespace&gt;.svc.&lt;cluster domain&gt;&quot;.<br />If not specified, the pod will not have a domainname at all."
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description="If specified, the pod's scheduling constraints"
/><ApiField
  name="schedulerName"
  type="string"
  required="false"
  description="If specified, the pod will be dispatched by specified scheduler.<br />If not specified, the pod will be dispatched by default scheduler."
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="If specified, the pod's tolerations."
/><ApiField
  name="hostAliases"
  type="[HostAlias](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#hostalias-v1-core) array"
  required="false"
  description="HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts<br />file if specified."
/><ApiField
  name="priorityClassName"
  type="string"
  required="false"
  description="If specified, indicates the pod's priority. &quot;system-node-critical&quot; and<br />&quot;system-cluster-critical&quot; are two special keywords which indicate the<br />highest priorities with the former being the highest priority. Any other<br />name must be defined by creating a PriorityClass object with that name.<br />If not specified, the pod priority will be default or zero if there is no<br />default."
/><ApiField
  name="priority"
  type="integer"
  required="false"
  description="The priority value. Various system components use this field to find the<br />priority of the pod. When Priority Admission Controller is enabled, it<br />prevents users from setting this field. The admission controller populates<br />this field from PriorityClassName.<br />The higher the value, the higher the priority."
/><ApiField
  name="dnsConfig"
  type="[PodDNSConfig](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#poddnsconfig-v1-core)"
  required="false"
  description="Specifies the DNS parameters of a pod.<br />Parameters specified here will be merged to the generated DNS<br />configuration based on DNSPolicy."
/><ApiField
  name="readinessGates"
  type="[PodReadinessGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podreadinessgate-v1-core) array"
  required="false"
  description="If specified, all readiness gates will be evaluated for pod readiness.<br />A pod is ready when all its containers are ready AND<br />all conditions specified in the readiness gates have status equal to &quot;True&quot;<br />More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates"
/><ApiField
  name="runtimeClassName"
  type="string"
  required="false"
  description="RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used<br />to run this pod.  If no RuntimeClass resource matches the named class, the pod will not be run.<br />If unset or empty, the &quot;legacy&quot; RuntimeClass will be used, which is an implicit class with an<br />empty definition that uses the default runtime handler.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class"
/><ApiField
  name="enableServiceLinks"
  type="boolean"
  required="false"
  description="EnableServiceLinks indicates whether information about services should be injected into pod's<br />environment variables, matching the syntax of Docker links.<br />Optional: Defaults to true."
/><ApiField
  name="preemptionPolicy"
  type="[PreemptionPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#preemptionpolicy-v1-core)"
  required="false"
  description="PreemptionPolicy is the Policy for preempting pods with lower priority.<br />One of Never, PreemptLowerPriority.<br />Defaults to PreemptLowerPriority if unset."
/><ApiField
  name="overhead"
  type="[ResourceList](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcelist-v1-core)"
  required="false"
  description="Overhead represents the resource overhead associated with running a pod for a given RuntimeClass.<br />This field will be autopopulated at admission time by the RuntimeClass admission controller. If<br />the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests.<br />The RuntimeClass admission controller will reject Pod create requests which have the overhead already<br />set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value<br />defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md"
/><ApiField
  name="topologySpreadConstraints"
  type="[TopologySpreadConstraint](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#topologyspreadconstraint-v1-core) array"
  required="false"
  description="TopologySpreadConstraints describes how a group of pods ought to spread across topology<br />domains. Scheduler will schedule pods in a way which abides by the constraints.<br />All topologySpreadConstraints are ANDed."
/><ApiField
  name="setHostnameAsFQDN"
  type="boolean"
  required="false"
  description="If true the pod's hostname will be configured as the pod's FQDN, rather than the leaf name (the default).<br />In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname).<br />In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Services\\Tcpip\\Parameters to FQDN.<br />If a pod does not have FQDN, this has no effect.<br />Default to false."
/><ApiField
  name="os"
  type="[PodOS](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podos-v1-core)"
  required="false"
  description="Specifies the OS of the containers in the pod.<br />Some pod and container fields are restricted if this is set.<br />If the OS field is set to linux, the following fields must be unset:<br />-securityContext.windowsOptions<br />If the OS field is set to windows, following fields must be unset:<br />- spec.hostPID<br />- spec.hostIPC<br />- spec.hostUsers<br />- spec.securityContext.appArmorProfile<br />- spec.securityContext.seLinuxOptions<br />- spec.securityContext.seccompProfile<br />- spec.securityContext.fsGroup<br />- spec.securityContext.fsGroupChangePolicy<br />- spec.securityContext.sysctls<br />- spec.shareProcessNamespace<br />- spec.securityContext.runAsUser<br />- spec.securityContext.runAsGroup<br />- spec.securityContext.supplementalGroups<br />- spec.securityContext.supplementalGroupsPolicy<br />- spec.containers[*].securityContext.appArmorProfile<br />- spec.containers[*].securityContext.seLinuxOptions<br />- spec.containers[*].securityContext.seccompProfile<br />- spec.containers[*].securityContext.capabilities<br />- spec.containers[*].securityContext.readOnlyRootFilesystem<br />- spec.containers[*].securityContext.privileged<br />- spec.containers[*].securityContext.allowPrivilegeEscalation<br />- spec.containers[*].securityContext.procMount<br />- spec.containers[*].securityContext.runAsUser<br />- spec.containers[*].securityContext.runAsGroup"
/><ApiField
  name="hostUsers"
  type="boolean"
  required="false"
  description="Use the host's user namespace.<br />Optional: Default to true.<br />If set to true or not present, the pod will be run in the host user namespace, useful<br />for when the pod needs a feature only available to the host user namespace, such as<br />loading a kernel module with CAP_SYS_MODULE.<br />When set to false, a new userns is created for the pod. Setting false is useful for<br />mitigating container breakout vulnerabilities even allowing users to run their<br />containers as root without actually having root privileges on the host.<br />This field is alpha-level and is only honored by servers that enable the UserNamespacesSupport feature."
/><ApiField
  name="schedulingGates"
  type="[PodSchedulingGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podschedulinggate-v1-core) array"
  required="false"
  description="SchedulingGates is an opaque list of values that if specified will block scheduling the pod.<br />If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the<br />scheduler will not attempt to schedule the pod.<br />SchedulingGates can only be set at pod creation time, and be removed only afterwards."
/><ApiField
  name="resourceClaims"
  type="[PodResourceClaim](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podresourceclaim-v1-core) array"
  required="false"
  description="ResourceClaims defines which ResourceClaims must be allocated<br />and reserved before the Pod is allowed to start. The resources<br />will be made available to those containers which consume them<br />by name.<br />This is an alpha field and requires enabling the<br />DynamicResourceAllocation feature gate.<br />This field is immutable."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Resources is the total amount of CPU and Memory resources required by all<br />containers in the pod. It supports specifying Requests and Limits for<br />&quot;cpu&quot; and &quot;memory&quot; resource names only. ResourceClaims are not supported.<br />This field enables fine-grained control over resource allocation for the<br />entire pod, allowing resource sharing among containers in a pod.<br />This is an alpha field and requires enabling the PodLevelResources feature<br />gate."
/><ApiField
  name="hostnameOverride"
  type="string"
  required="false"
  description="HostnameOverride specifies an explicit override for the pod's hostname as perceived by the pod.<br />This field only specifies the pod's hostname and does not affect its DNS records.<br />When this field is set to a non-empty string:<br />- It takes precedence over the values set in `hostname` and `subdomain`.<br />- The Pod's hostname will be set to this value.<br />- `setHostnameAsFQDN` must be nil or set to false.<br />- `hostNetwork` must be set to false.<br />This field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters.<br />Requires the HostnameOverride feature gate to be enabled."
/>


#### PodsMetricsBackend

**Underlying type:** string

**Appears in:**
- [PodMetrics](#podmetrics)

PodsMetricsBackend enum



##### Possible Values

<ApiField
  name="opentelemetry"
  type="enum"
  required="false"
  description=""
/>
#### PredictorExtensionSpec



**Appears in:**
- [HuggingFaceRuntimeSpec](#huggingfaceruntimespec)
- [LightGBMSpec](#lightgbmspec)
- [ModelSpec](#modelspec)
- [ONNXRuntimeSpec](#onnxruntimespec)
- [PMMLSpec](#pmmlspec)
- [PaddleServerSpec](#paddleserverspec)
- [SKLearnSpec](#sklearnspec)
- [TFServingSpec](#tfservingspec)
- [TorchServeSpec](#torchservespec)
- [TritonSpec](#tritonspec)
- [XGBoostSpec](#xgboostspec)

PredictorExtensionSpec defines configuration shared across all predictor frameworks

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>



#### PredictorSpec



**Appears in:**
- [InferenceServiceSpec](#inferenceservicespec)

PredictorSpec defines the configuration for a predictor,
The following fields follow a "1-of" semantic. Users must specify exactly one spec.

##### Fields



<ApiField
  name="sklearn"
  type="[SKLearnSpec](#sklearnspec)"
  required="true"
  description="Spec for SKLearn model server"
/><ApiField
  name="xgboost"
  type="[XGBoostSpec](#xgboostspec)"
  required="true"
  description="Spec for XGBoost model server"
/><ApiField
  name="tensorflow"
  type="[TFServingSpec](#tfservingspec)"
  required="true"
  description="Spec for TFServing (https://github.com/tensorflow/serving)"
/><ApiField
  name="pytorch"
  type="[TorchServeSpec](#torchservespec)"
  required="true"
  description="Spec for TorchServe (https://pytorch.org/serve)"
/><ApiField
  name="triton"
  type="[TritonSpec](#tritonspec)"
  required="true"
  description="Spec for Triton Inference Server (https://github.com/triton-inference-server/server)"
/><ApiField
  name="onnx"
  type="[ONNXRuntimeSpec](#onnxruntimespec)"
  required="true"
  description="Spec for ONNX runtime (https://github.com/microsoft/onnxruntime)"
/><ApiField
  name="huggingface"
  type="[HuggingFaceRuntimeSpec](#huggingfaceruntimespec)"
  required="true"
  description="Spec for HuggingFace runtime (https://github.com/huggingface)"
/><ApiField
  name="pmml"
  type="[PMMLSpec](#pmmlspec)"
  required="true"
  description="Spec for PMML (http://dmg.org/pmml/v4-1/GeneralStructure.html)"
/><ApiField
  name="lightgbm"
  type="[LightGBMSpec](#lightgbmspec)"
  required="true"
  description="Spec for LightGBM model server"
/><ApiField
  name="paddle"
  type="[PaddleServerSpec](#paddleserverspec)"
  required="true"
  description="Spec for Paddle model server (https://github.com/PaddlePaddle/Serving)"
/><ApiField
  name="model"
  type="[ModelSpec](#modelspec)"
  required="true"
  description="Model spec for any arbitrary framework."
/><ApiField
  name="storageUris"
  type="[StorageUri](#storageuri) array"
  required="true"
  description="Spec for multiple storage uris."
/><ApiField
  name="workerSpec"
  type="[WorkerSpec](#workerspec)"
  required="true"
  description="WorkerSpec for enabling multi-node/multi-gpu"
/><ApiField
  name="volumes"
  type="[Volume](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volume-v1-core) array"
  required="false"
  description="List of volumes that can be mounted by containers belonging to the pod.<br />More info: https://kubernetes.io/docs/concepts/storage/volumes"
/><ApiField
  name="initContainers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of initialization containers belonging to the pod.<br />Init containers are executed in order prior to containers being started. If any<br />init container fails, the pod is considered to have failed and is handled according<br />to its restartPolicy. The name for an init container or normal container must be<br />unique among all containers.<br />Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes.<br />The resourceRequirements of an init container are taken into account during scheduling<br />by finding the highest request/limit for each resource type, and then using the max of<br />that value or the sum of the normal containers. Limits are applied to init containers<br />in a similar fashion.<br />Init containers cannot currently be added or removed.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/"
/><ApiField
  name="containers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of containers belonging to the pod.<br />Containers cannot currently be added or removed.<br />There must be at least one container in a Pod.<br />Cannot be updated."
/><ApiField
  name="ephemeralContainers"
  type="[EphemeralContainer](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#ephemeralcontainer-v1-core) array"
  required="false"
  description="List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing<br />pod to perform user-initiated actions such as debugging. This list cannot be specified when<br />creating a pod, and it cannot be modified by updating the pod spec. In order to add an<br />ephemeral container to an existing pod, use the pod's ephemeralcontainers subresource."
/><ApiField
  name="restartPolicy"
  type="[RestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#restartpolicy-v1-core)"
  required="false"
  description="Restart policy for all containers within the pod.<br />One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted.<br />Default to Always.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy"
/><ApiField
  name="terminationGracePeriodSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request.<br />Value must be non-negative integer. The value zero indicates stop immediately via<br />the kill signal (no opportunity to shut down).<br />If this value is nil, the default grace period will be used instead.<br />The grace period is the duration in seconds after the processes running in the pod are sent<br />a termination signal and the time when the processes are forcibly halted with a kill signal.<br />Set this value longer than the expected cleanup time for your process.<br />Defaults to 30 seconds."
/><ApiField
  name="activeDeadlineSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod may be active on the node relative to<br />StartTime before the system will actively try to mark it failed and kill associated containers.<br />Value must be a positive integer."
/><ApiField
  name="dnsPolicy"
  type="[DNSPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#dnspolicy-v1-core)"
  required="false"
  description="Set DNS policy for the pod.<br />Defaults to &quot;ClusterFirst&quot;.<br />Valid values are 'ClusterFirstWithHostNet', 'ClusterFirst', 'Default' or 'None'.<br />DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy.<br />To have DNS options set along with hostNetwork, you have to specify DNS policy<br />explicitly to 'ClusterFirstWithHostNet'."
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector is a selector which must be true for the pod to fit on a node.<br />Selector which must match a node's labels for the pod to be scheduled on that node.<br />More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
/><ApiField
  name="serviceAccountName"
  type="string"
  required="false"
  description="ServiceAccountName is the name of the ServiceAccount to use to run this pod.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/"
/><ApiField
  name="serviceAccount"
  type="string"
  required="false"
  description="DeprecatedServiceAccount is a deprecated alias for ServiceAccountName.<br />Deprecated: Use serviceAccountName instead."
/><ApiField
  name="automountServiceAccountToken"
  type="boolean"
  required="false"
  description="AutomountServiceAccountToken indicates whether a service account token should be automatically mounted."
/><ApiField
  name="nodeName"
  type="string"
  required="false"
  description="NodeName indicates in which node this pod is scheduled.<br />If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName.<br />Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod.<br />This field should not be used to express a desire for the pod to be scheduled on a specific node.<br />https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename"
/><ApiField
  name="hostNetwork"
  type="boolean"
  required="false"
  description="Host networking requested for this pod. Use the host's network namespace.<br />If this option is set, the ports that will be used must be specified.<br />Default to false."
/><ApiField
  name="hostPID"
  type="boolean"
  required="false"
  description="Use the host's pid namespace.<br />Optional: Default to false."
/><ApiField
  name="hostIPC"
  type="boolean"
  required="false"
  description="Use the host's ipc namespace.<br />Optional: Default to false."
/><ApiField
  name="shareProcessNamespace"
  type="boolean"
  required="false"
  description="Share a single process namespace between all of the containers in a pod.<br />When this is set containers will be able to view and signal processes from other containers<br />in the same pod, and the first process in each container will not be assigned PID 1.<br />HostPID and ShareProcessNamespace cannot both be set.<br />Optional: Default to false."
/><ApiField
  name="securityContext"
  type="[PodSecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podsecuritycontext-v1-core)"
  required="false"
  description="SecurityContext holds pod-level security attributes and common container settings.<br />Optional: Defaults to empty.  See type description for default values of each field."
/><ApiField
  name="imagePullSecrets"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.<br />If specified, these secrets will be passed to individual puller implementations for them to use.<br />More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
/><ApiField
  name="hostname"
  type="string"
  required="false"
  description="Specifies the hostname of the Pod<br />If not specified, the pod's hostname will be set to a system-defined value."
/><ApiField
  name="subdomain"
  type="string"
  required="false"
  description="If specified, the fully qualified Pod hostname will be &quot;&lt;hostname&gt;.&lt;subdomain&gt;.&lt;pod namespace&gt;.svc.&lt;cluster domain&gt;&quot;.<br />If not specified, the pod will not have a domainname at all."
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description="If specified, the pod's scheduling constraints"
/><ApiField
  name="schedulerName"
  type="string"
  required="false"
  description="If specified, the pod will be dispatched by specified scheduler.<br />If not specified, the pod will be dispatched by default scheduler."
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="If specified, the pod's tolerations."
/><ApiField
  name="hostAliases"
  type="[HostAlias](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#hostalias-v1-core) array"
  required="false"
  description="HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts<br />file if specified."
/><ApiField
  name="priorityClassName"
  type="string"
  required="false"
  description="If specified, indicates the pod's priority. &quot;system-node-critical&quot; and<br />&quot;system-cluster-critical&quot; are two special keywords which indicate the<br />highest priorities with the former being the highest priority. Any other<br />name must be defined by creating a PriorityClass object with that name.<br />If not specified, the pod priority will be default or zero if there is no<br />default."
/><ApiField
  name="priority"
  type="integer"
  required="false"
  description="The priority value. Various system components use this field to find the<br />priority of the pod. When Priority Admission Controller is enabled, it<br />prevents users from setting this field. The admission controller populates<br />this field from PriorityClassName.<br />The higher the value, the higher the priority."
/><ApiField
  name="dnsConfig"
  type="[PodDNSConfig](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#poddnsconfig-v1-core)"
  required="false"
  description="Specifies the DNS parameters of a pod.<br />Parameters specified here will be merged to the generated DNS<br />configuration based on DNSPolicy."
/><ApiField
  name="readinessGates"
  type="[PodReadinessGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podreadinessgate-v1-core) array"
  required="false"
  description="If specified, all readiness gates will be evaluated for pod readiness.<br />A pod is ready when all its containers are ready AND<br />all conditions specified in the readiness gates have status equal to &quot;True&quot;<br />More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates"
/><ApiField
  name="runtimeClassName"
  type="string"
  required="false"
  description="RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used<br />to run this pod.  If no RuntimeClass resource matches the named class, the pod will not be run.<br />If unset or empty, the &quot;legacy&quot; RuntimeClass will be used, which is an implicit class with an<br />empty definition that uses the default runtime handler.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class"
/><ApiField
  name="enableServiceLinks"
  type="boolean"
  required="false"
  description="EnableServiceLinks indicates whether information about services should be injected into pod's<br />environment variables, matching the syntax of Docker links.<br />Optional: Defaults to true."
/><ApiField
  name="preemptionPolicy"
  type="[PreemptionPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#preemptionpolicy-v1-core)"
  required="false"
  description="PreemptionPolicy is the Policy for preempting pods with lower priority.<br />One of Never, PreemptLowerPriority.<br />Defaults to PreemptLowerPriority if unset."
/><ApiField
  name="overhead"
  type="[ResourceList](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcelist-v1-core)"
  required="false"
  description="Overhead represents the resource overhead associated with running a pod for a given RuntimeClass.<br />This field will be autopopulated at admission time by the RuntimeClass admission controller. If<br />the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests.<br />The RuntimeClass admission controller will reject Pod create requests which have the overhead already<br />set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value<br />defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md"
/><ApiField
  name="topologySpreadConstraints"
  type="[TopologySpreadConstraint](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#topologyspreadconstraint-v1-core) array"
  required="false"
  description="TopologySpreadConstraints describes how a group of pods ought to spread across topology<br />domains. Scheduler will schedule pods in a way which abides by the constraints.<br />All topologySpreadConstraints are ANDed."
/><ApiField
  name="setHostnameAsFQDN"
  type="boolean"
  required="false"
  description="If true the pod's hostname will be configured as the pod's FQDN, rather than the leaf name (the default).<br />In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname).<br />In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Services\\Tcpip\\Parameters to FQDN.<br />If a pod does not have FQDN, this has no effect.<br />Default to false."
/><ApiField
  name="os"
  type="[PodOS](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podos-v1-core)"
  required="false"
  description="Specifies the OS of the containers in the pod.<br />Some pod and container fields are restricted if this is set.<br />If the OS field is set to linux, the following fields must be unset:<br />-securityContext.windowsOptions<br />If the OS field is set to windows, following fields must be unset:<br />- spec.hostPID<br />- spec.hostIPC<br />- spec.hostUsers<br />- spec.securityContext.appArmorProfile<br />- spec.securityContext.seLinuxOptions<br />- spec.securityContext.seccompProfile<br />- spec.securityContext.fsGroup<br />- spec.securityContext.fsGroupChangePolicy<br />- spec.securityContext.sysctls<br />- spec.shareProcessNamespace<br />- spec.securityContext.runAsUser<br />- spec.securityContext.runAsGroup<br />- spec.securityContext.supplementalGroups<br />- spec.securityContext.supplementalGroupsPolicy<br />- spec.containers[*].securityContext.appArmorProfile<br />- spec.containers[*].securityContext.seLinuxOptions<br />- spec.containers[*].securityContext.seccompProfile<br />- spec.containers[*].securityContext.capabilities<br />- spec.containers[*].securityContext.readOnlyRootFilesystem<br />- spec.containers[*].securityContext.privileged<br />- spec.containers[*].securityContext.allowPrivilegeEscalation<br />- spec.containers[*].securityContext.procMount<br />- spec.containers[*].securityContext.runAsUser<br />- spec.containers[*].securityContext.runAsGroup"
/><ApiField
  name="hostUsers"
  type="boolean"
  required="false"
  description="Use the host's user namespace.<br />Optional: Default to true.<br />If set to true or not present, the pod will be run in the host user namespace, useful<br />for when the pod needs a feature only available to the host user namespace, such as<br />loading a kernel module with CAP_SYS_MODULE.<br />When set to false, a new userns is created for the pod. Setting false is useful for<br />mitigating container breakout vulnerabilities even allowing users to run their<br />containers as root without actually having root privileges on the host.<br />This field is alpha-level and is only honored by servers that enable the UserNamespacesSupport feature."
/><ApiField
  name="schedulingGates"
  type="[PodSchedulingGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podschedulinggate-v1-core) array"
  required="false"
  description="SchedulingGates is an opaque list of values that if specified will block scheduling the pod.<br />If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the<br />scheduler will not attempt to schedule the pod.<br />SchedulingGates can only be set at pod creation time, and be removed only afterwards."
/><ApiField
  name="resourceClaims"
  type="[PodResourceClaim](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podresourceclaim-v1-core) array"
  required="false"
  description="ResourceClaims defines which ResourceClaims must be allocated<br />and reserved before the Pod is allowed to start. The resources<br />will be made available to those containers which consume them<br />by name.<br />This is an alpha field and requires enabling the<br />DynamicResourceAllocation feature gate.<br />This field is immutable."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Resources is the total amount of CPU and Memory resources required by all<br />containers in the pod. It supports specifying Requests and Limits for<br />&quot;cpu&quot; and &quot;memory&quot; resource names only. ResourceClaims are not supported.<br />This field enables fine-grained control over resource allocation for the<br />entire pod, allowing resource sharing among containers in a pod.<br />This is an alpha field and requires enabling the PodLevelResources feature<br />gate."
/><ApiField
  name="hostnameOverride"
  type="string"
  required="false"
  description="HostnameOverride specifies an explicit override for the pod's hostname as perceived by the pod.<br />This field only specifies the pod's hostname and does not affect its DNS records.<br />When this field is set to a non-empty string:<br />- It takes precedence over the values set in `hostname` and `subdomain`.<br />- The Pod's hostname will be set to this value.<br />- `setHostnameAsFQDN` must be nil or set to false.<br />- `hostNetwork` must be set to false.<br />This field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters.<br />Requires the HostnameOverride feature gate to be enabled."
/><ApiField
  name="minReplicas"
  type="integer"
  required="false"
  description="Minimum number of replicas, defaults to 1 but can be set to 0 to enable scale-to-zero."
/><ApiField
  name="maxReplicas"
  type="integer"
  required="false"
  description="Maximum number of replicas for autoscaling."
/><ApiField
  name="scaleTarget"
  type="integer"
  required="false"
  description="ScaleTarget specifies the integer target value of the metric type the Autoscaler watches for.<br />concurrency and rps targets are supported by Knative Pod Autoscaler<br />(https://knative.dev/docs/serving/autoscaling/autoscaling-targets/)."
/><ApiField
  name="scaleMetric"
  type="[ScaleMetric](#scalemetric)"
  required="false"
  description="ScaleMetric defines the scaling metric type watched by autoscaler.<br />possible values are concurrency, rps, cpu, memory. concurrency, rps are supported via<br />Knative Pod Autoscaler(https://knative.dev/docs/serving/autoscaling/autoscaling-metrics)."
/><ApiField
  name="scaleMetricType"
  type="[MetricTargetType](#metrictargettype)"
  required="false"
  description="Type of metric to use. Options are Utilization, or AverageValue."
/><ApiField
  name="autoScaling"
  type="[AutoScalingSpec](#autoscalingspec)"
  required="false"
  description="AutoScaling autoscaling spec which is backed up HPA or KEDA."
/><ApiField
  name="containerConcurrency"
  type="integer"
  required="false"
  description="ContainerConcurrency specifies how many requests can be processed concurrently, this sets the hard limit of the container<br />concurrency(https://knative.dev/docs/serving/autoscaling/concurrency)."
/><ApiField
  name="timeout"
  type="integer"
  required="false"
  description="TimeoutSeconds specifies the number of seconds to wait before timing out a request to the component."
/><ApiField
  name="canaryTrafficPercent"
  type="integer"
  required="false"
  description="CanaryTrafficPercent defines the traffic split percentage between the candidate revision and the last ready revision"
/><ApiField
  name="logger"
  type="[LoggerSpec](#loggerspec)"
  required="false"
  description="Activate request/response logging and logger configurations"
/><ApiField
  name="batcher"
  type="[Batcher](#batcher)"
  required="false"
  description="Activate request batching and batching configurations"
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="deploymentStrategy"
  type="[DeploymentStrategy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#deploymentstrategy-v1-apps)"
  required="false"
  description="The deployment strategy to use to replace existing pods with new ones. Only applicable for raw deployment mode."
/>


#### ResourceConfig



**Appears in:**
- [InferenceServicesConfig](#inferenceservicesconfig)
- [OtelCollectorConfig](#otelcollectorconfig)



##### Fields



<ApiField
  name="cpuLimit"
  type="string"
  required="true"
  description=""
/><ApiField
  name="memoryLimit"
  type="string"
  required="true"
  description=""
/><ApiField
  name="cpuRequest"
  type="string"
  required="true"
  description=""
/><ApiField
  name="memoryRequest"
  type="string"
  required="true"
  description=""
/>


#### ResourceMetric

**Underlying type:** string

**Appears in:**
- [ResourceMetricSource](#resourcemetricsource)

ResourceMetric enum



##### Possible Values

<ApiField
  name="cpu"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="memory"
  type="enum"
  required="false"
  description=""
/>
#### ResourceMetricSource



**Appears in:**
- [MetricsSpec](#metricsspec)



##### Fields



<ApiField
  name="name"
  type="[ResourceMetric](#resourcemetric)"
  required="true"
  description="name is the name of the resource in question."
/><ApiField
  name="target"
  type="[MetricTarget](#metrictarget)"
  required="true"
  description="target specifies the target value for the given metric"
/>


#### RolloutSpec



**Appears in:**
- [DeploymentRolloutStrategy](#deploymentrolloutstrategy)

RolloutSpec defines the rollout strategy configuration using Kubernetes deployment strategy

##### Fields



<ApiField
  name="maxSurge"
  type="string"
  required="true"
  description="MaxSurge specifies the maximum number of pods that can be created above the desired replica count.<br />Can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%)."
/><ApiField
  name="maxUnavailable"
  type="string"
  required="true"
  description="MaxUnavailable specifies the maximum number of pods that can be unavailable during the update.<br />Can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%)."
/>


#### SKLearnSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

SKLearnSpec defines arguments for configuring SKLearn model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### ScaleMetric

**Underlying type:** string

**Appears in:**
- [ComponentExtensionSpec](#componentextensionspec)
- [ExplainerSpec](#explainerspec)
- [PredictorSpec](#predictorspec)
- [TransformerSpec](#transformerspec)

ScaleMetric enum



##### Possible Values

<ApiField
  name="cpu"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="memory"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="concurrency"
  type="enum"
  required="false"
  description=""
/><ApiField
  name="rps"
  type="enum"
  required="false"
  description=""
/>


#### StorageSpec



**Appears in:**
- [LoggerStorageSpec](#loggerstoragespec)
- [ModelStorageSpec](#modelstoragespec)

StorageSpec defines a spec for an object in an object store

##### Fields



<ApiField
  name="path"
  type="string"
  required="false"
  description="The path to the object in the storage. Note that this path is relative to the storage URI."
/><ApiField
  name="parameters"
  type="map[string]string"
  required="false"
  description="Parameters to override the default storage credentials and config."
/><ApiField
  name="key"
  type="string"
  required="false"
  description="The Storage Key in the secret for this object."
/>


#### StorageUri



**Appears in:**
- [ExplainerSpec](#explainerspec)
- [PredictorSpec](#predictorspec)
- [TransformerSpec](#transformerspec)



##### Fields



<ApiField
  name="uri"
  type="string"
  required="true"
  description=""
/><ApiField
  name="mountPath"
  type="string"
  required="false"
  defaultValue="/mnt/models"
  description="MountPath is the path where the model will be mounted inside the container.<br />If not specified, it defaults to /mnt/models."
/>


#### TFServingSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

TFServingSpec defines arguments for configuring Tensorflow model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### TorchServeSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

TorchServeSpec defines arguments for configuring PyTorch model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### TransformerSpec



**Appears in:**
- [InferenceServiceSpec](#inferenceservicespec)

TransformerSpec defines transformer service for pre/post processing

##### Fields



<ApiField
  name="volumes"
  type="[Volume](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volume-v1-core) array"
  required="false"
  description="List of volumes that can be mounted by containers belonging to the pod.<br />More info: https://kubernetes.io/docs/concepts/storage/volumes"
/><ApiField
  name="initContainers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of initialization containers belonging to the pod.<br />Init containers are executed in order prior to containers being started. If any<br />init container fails, the pod is considered to have failed and is handled according<br />to its restartPolicy. The name for an init container or normal container must be<br />unique among all containers.<br />Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes.<br />The resourceRequirements of an init container are taken into account during scheduling<br />by finding the highest request/limit for each resource type, and then using the max of<br />that value or the sum of the normal containers. Limits are applied to init containers<br />in a similar fashion.<br />Init containers cannot currently be added or removed.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/"
/><ApiField
  name="containers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of containers belonging to the pod.<br />Containers cannot currently be added or removed.<br />There must be at least one container in a Pod.<br />Cannot be updated."
/><ApiField
  name="ephemeralContainers"
  type="[EphemeralContainer](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#ephemeralcontainer-v1-core) array"
  required="false"
  description="List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing<br />pod to perform user-initiated actions such as debugging. This list cannot be specified when<br />creating a pod, and it cannot be modified by updating the pod spec. In order to add an<br />ephemeral container to an existing pod, use the pod's ephemeralcontainers subresource."
/><ApiField
  name="restartPolicy"
  type="[RestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#restartpolicy-v1-core)"
  required="false"
  description="Restart policy for all containers within the pod.<br />One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted.<br />Default to Always.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy"
/><ApiField
  name="terminationGracePeriodSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request.<br />Value must be non-negative integer. The value zero indicates stop immediately via<br />the kill signal (no opportunity to shut down).<br />If this value is nil, the default grace period will be used instead.<br />The grace period is the duration in seconds after the processes running in the pod are sent<br />a termination signal and the time when the processes are forcibly halted with a kill signal.<br />Set this value longer than the expected cleanup time for your process.<br />Defaults to 30 seconds."
/><ApiField
  name="activeDeadlineSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod may be active on the node relative to<br />StartTime before the system will actively try to mark it failed and kill associated containers.<br />Value must be a positive integer."
/><ApiField
  name="dnsPolicy"
  type="[DNSPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#dnspolicy-v1-core)"
  required="false"
  description="Set DNS policy for the pod.<br />Defaults to &quot;ClusterFirst&quot;.<br />Valid values are 'ClusterFirstWithHostNet', 'ClusterFirst', 'Default' or 'None'.<br />DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy.<br />To have DNS options set along with hostNetwork, you have to specify DNS policy<br />explicitly to 'ClusterFirstWithHostNet'."
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector is a selector which must be true for the pod to fit on a node.<br />Selector which must match a node's labels for the pod to be scheduled on that node.<br />More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
/><ApiField
  name="serviceAccountName"
  type="string"
  required="false"
  description="ServiceAccountName is the name of the ServiceAccount to use to run this pod.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/"
/><ApiField
  name="serviceAccount"
  type="string"
  required="false"
  description="DeprecatedServiceAccount is a deprecated alias for ServiceAccountName.<br />Deprecated: Use serviceAccountName instead."
/><ApiField
  name="automountServiceAccountToken"
  type="boolean"
  required="false"
  description="AutomountServiceAccountToken indicates whether a service account token should be automatically mounted."
/><ApiField
  name="nodeName"
  type="string"
  required="false"
  description="NodeName indicates in which node this pod is scheduled.<br />If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName.<br />Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod.<br />This field should not be used to express a desire for the pod to be scheduled on a specific node.<br />https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename"
/><ApiField
  name="hostNetwork"
  type="boolean"
  required="false"
  description="Host networking requested for this pod. Use the host's network namespace.<br />If this option is set, the ports that will be used must be specified.<br />Default to false."
/><ApiField
  name="hostPID"
  type="boolean"
  required="false"
  description="Use the host's pid namespace.<br />Optional: Default to false."
/><ApiField
  name="hostIPC"
  type="boolean"
  required="false"
  description="Use the host's ipc namespace.<br />Optional: Default to false."
/><ApiField
  name="shareProcessNamespace"
  type="boolean"
  required="false"
  description="Share a single process namespace between all of the containers in a pod.<br />When this is set containers will be able to view and signal processes from other containers<br />in the same pod, and the first process in each container will not be assigned PID 1.<br />HostPID and ShareProcessNamespace cannot both be set.<br />Optional: Default to false."
/><ApiField
  name="securityContext"
  type="[PodSecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podsecuritycontext-v1-core)"
  required="false"
  description="SecurityContext holds pod-level security attributes and common container settings.<br />Optional: Defaults to empty.  See type description for default values of each field."
/><ApiField
  name="imagePullSecrets"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.<br />If specified, these secrets will be passed to individual puller implementations for them to use.<br />More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
/><ApiField
  name="hostname"
  type="string"
  required="false"
  description="Specifies the hostname of the Pod<br />If not specified, the pod's hostname will be set to a system-defined value."
/><ApiField
  name="subdomain"
  type="string"
  required="false"
  description="If specified, the fully qualified Pod hostname will be &quot;&lt;hostname&gt;.&lt;subdomain&gt;.&lt;pod namespace&gt;.svc.&lt;cluster domain&gt;&quot;.<br />If not specified, the pod will not have a domainname at all."
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description="If specified, the pod's scheduling constraints"
/><ApiField
  name="schedulerName"
  type="string"
  required="false"
  description="If specified, the pod will be dispatched by specified scheduler.<br />If not specified, the pod will be dispatched by default scheduler."
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="If specified, the pod's tolerations."
/><ApiField
  name="hostAliases"
  type="[HostAlias](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#hostalias-v1-core) array"
  required="false"
  description="HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts<br />file if specified."
/><ApiField
  name="priorityClassName"
  type="string"
  required="false"
  description="If specified, indicates the pod's priority. &quot;system-node-critical&quot; and<br />&quot;system-cluster-critical&quot; are two special keywords which indicate the<br />highest priorities with the former being the highest priority. Any other<br />name must be defined by creating a PriorityClass object with that name.<br />If not specified, the pod priority will be default or zero if there is no<br />default."
/><ApiField
  name="priority"
  type="integer"
  required="false"
  description="The priority value. Various system components use this field to find the<br />priority of the pod. When Priority Admission Controller is enabled, it<br />prevents users from setting this field. The admission controller populates<br />this field from PriorityClassName.<br />The higher the value, the higher the priority."
/><ApiField
  name="dnsConfig"
  type="[PodDNSConfig](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#poddnsconfig-v1-core)"
  required="false"
  description="Specifies the DNS parameters of a pod.<br />Parameters specified here will be merged to the generated DNS<br />configuration based on DNSPolicy."
/><ApiField
  name="readinessGates"
  type="[PodReadinessGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podreadinessgate-v1-core) array"
  required="false"
  description="If specified, all readiness gates will be evaluated for pod readiness.<br />A pod is ready when all its containers are ready AND<br />all conditions specified in the readiness gates have status equal to &quot;True&quot;<br />More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates"
/><ApiField
  name="runtimeClassName"
  type="string"
  required="false"
  description="RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used<br />to run this pod.  If no RuntimeClass resource matches the named class, the pod will not be run.<br />If unset or empty, the &quot;legacy&quot; RuntimeClass will be used, which is an implicit class with an<br />empty definition that uses the default runtime handler.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class"
/><ApiField
  name="enableServiceLinks"
  type="boolean"
  required="false"
  description="EnableServiceLinks indicates whether information about services should be injected into pod's<br />environment variables, matching the syntax of Docker links.<br />Optional: Defaults to true."
/><ApiField
  name="preemptionPolicy"
  type="[PreemptionPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#preemptionpolicy-v1-core)"
  required="false"
  description="PreemptionPolicy is the Policy for preempting pods with lower priority.<br />One of Never, PreemptLowerPriority.<br />Defaults to PreemptLowerPriority if unset."
/><ApiField
  name="overhead"
  type="[ResourceList](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcelist-v1-core)"
  required="false"
  description="Overhead represents the resource overhead associated with running a pod for a given RuntimeClass.<br />This field will be autopopulated at admission time by the RuntimeClass admission controller. If<br />the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests.<br />The RuntimeClass admission controller will reject Pod create requests which have the overhead already<br />set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value<br />defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md"
/><ApiField
  name="topologySpreadConstraints"
  type="[TopologySpreadConstraint](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#topologyspreadconstraint-v1-core) array"
  required="false"
  description="TopologySpreadConstraints describes how a group of pods ought to spread across topology<br />domains. Scheduler will schedule pods in a way which abides by the constraints.<br />All topologySpreadConstraints are ANDed."
/><ApiField
  name="setHostnameAsFQDN"
  type="boolean"
  required="false"
  description="If true the pod's hostname will be configured as the pod's FQDN, rather than the leaf name (the default).<br />In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname).<br />In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Services\\Tcpip\\Parameters to FQDN.<br />If a pod does not have FQDN, this has no effect.<br />Default to false."
/><ApiField
  name="os"
  type="[PodOS](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podos-v1-core)"
  required="false"
  description="Specifies the OS of the containers in the pod.<br />Some pod and container fields are restricted if this is set.<br />If the OS field is set to linux, the following fields must be unset:<br />-securityContext.windowsOptions<br />If the OS field is set to windows, following fields must be unset:<br />- spec.hostPID<br />- spec.hostIPC<br />- spec.hostUsers<br />- spec.securityContext.appArmorProfile<br />- spec.securityContext.seLinuxOptions<br />- spec.securityContext.seccompProfile<br />- spec.securityContext.fsGroup<br />- spec.securityContext.fsGroupChangePolicy<br />- spec.securityContext.sysctls<br />- spec.shareProcessNamespace<br />- spec.securityContext.runAsUser<br />- spec.securityContext.runAsGroup<br />- spec.securityContext.supplementalGroups<br />- spec.securityContext.supplementalGroupsPolicy<br />- spec.containers[*].securityContext.appArmorProfile<br />- spec.containers[*].securityContext.seLinuxOptions<br />- spec.containers[*].securityContext.seccompProfile<br />- spec.containers[*].securityContext.capabilities<br />- spec.containers[*].securityContext.readOnlyRootFilesystem<br />- spec.containers[*].securityContext.privileged<br />- spec.containers[*].securityContext.allowPrivilegeEscalation<br />- spec.containers[*].securityContext.procMount<br />- spec.containers[*].securityContext.runAsUser<br />- spec.containers[*].securityContext.runAsGroup"
/><ApiField
  name="hostUsers"
  type="boolean"
  required="false"
  description="Use the host's user namespace.<br />Optional: Default to true.<br />If set to true or not present, the pod will be run in the host user namespace, useful<br />for when the pod needs a feature only available to the host user namespace, such as<br />loading a kernel module with CAP_SYS_MODULE.<br />When set to false, a new userns is created for the pod. Setting false is useful for<br />mitigating container breakout vulnerabilities even allowing users to run their<br />containers as root without actually having root privileges on the host.<br />This field is alpha-level and is only honored by servers that enable the UserNamespacesSupport feature."
/><ApiField
  name="schedulingGates"
  type="[PodSchedulingGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podschedulinggate-v1-core) array"
  required="false"
  description="SchedulingGates is an opaque list of values that if specified will block scheduling the pod.<br />If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the<br />scheduler will not attempt to schedule the pod.<br />SchedulingGates can only be set at pod creation time, and be removed only afterwards."
/><ApiField
  name="resourceClaims"
  type="[PodResourceClaim](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podresourceclaim-v1-core) array"
  required="false"
  description="ResourceClaims defines which ResourceClaims must be allocated<br />and reserved before the Pod is allowed to start. The resources<br />will be made available to those containers which consume them<br />by name.<br />This is an alpha field and requires enabling the<br />DynamicResourceAllocation feature gate.<br />This field is immutable."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Resources is the total amount of CPU and Memory resources required by all<br />containers in the pod. It supports specifying Requests and Limits for<br />&quot;cpu&quot; and &quot;memory&quot; resource names only. ResourceClaims are not supported.<br />This field enables fine-grained control over resource allocation for the<br />entire pod, allowing resource sharing among containers in a pod.<br />This is an alpha field and requires enabling the PodLevelResources feature<br />gate."
/><ApiField
  name="hostnameOverride"
  type="string"
  required="false"
  description="HostnameOverride specifies an explicit override for the pod's hostname as perceived by the pod.<br />This field only specifies the pod's hostname and does not affect its DNS records.<br />When this field is set to a non-empty string:<br />- It takes precedence over the values set in `hostname` and `subdomain`.<br />- The Pod's hostname will be set to this value.<br />- `setHostnameAsFQDN` must be nil or set to false.<br />- `hostNetwork` must be set to false.<br />This field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters.<br />Requires the HostnameOverride feature gate to be enabled."
/><ApiField
  name="minReplicas"
  type="integer"
  required="false"
  description="Minimum number of replicas, defaults to 1 but can be set to 0 to enable scale-to-zero."
/><ApiField
  name="maxReplicas"
  type="integer"
  required="false"
  description="Maximum number of replicas for autoscaling."
/><ApiField
  name="scaleTarget"
  type="integer"
  required="false"
  description="ScaleTarget specifies the integer target value of the metric type the Autoscaler watches for.<br />concurrency and rps targets are supported by Knative Pod Autoscaler<br />(https://knative.dev/docs/serving/autoscaling/autoscaling-targets/)."
/><ApiField
  name="scaleMetric"
  type="[ScaleMetric](#scalemetric)"
  required="false"
  description="ScaleMetric defines the scaling metric type watched by autoscaler.<br />possible values are concurrency, rps, cpu, memory. concurrency, rps are supported via<br />Knative Pod Autoscaler(https://knative.dev/docs/serving/autoscaling/autoscaling-metrics)."
/><ApiField
  name="scaleMetricType"
  type="[MetricTargetType](#metrictargettype)"
  required="false"
  description="Type of metric to use. Options are Utilization, or AverageValue."
/><ApiField
  name="autoScaling"
  type="[AutoScalingSpec](#autoscalingspec)"
  required="false"
  description="AutoScaling autoscaling spec which is backed up HPA or KEDA."
/><ApiField
  name="containerConcurrency"
  type="integer"
  required="false"
  description="ContainerConcurrency specifies how many requests can be processed concurrently, this sets the hard limit of the container<br />concurrency(https://knative.dev/docs/serving/autoscaling/concurrency)."
/><ApiField
  name="timeout"
  type="integer"
  required="false"
  description="TimeoutSeconds specifies the number of seconds to wait before timing out a request to the component."
/><ApiField
  name="canaryTrafficPercent"
  type="integer"
  required="false"
  description="CanaryTrafficPercent defines the traffic split percentage between the candidate revision and the last ready revision"
/><ApiField
  name="logger"
  type="[LoggerSpec](#loggerspec)"
  required="false"
  description="Activate request/response logging and logger configurations"
/><ApiField
  name="batcher"
  type="[Batcher](#batcher)"
  required="false"
  description="Activate request batching and batching configurations"
/><ApiField
  name="labels"
  type="object (keys:string, values:string)"
  required="false"
  description="Labels that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/"
/><ApiField
  name="annotations"
  type="object (keys:string, values:string)"
  required="false"
  description="Annotations that will be added to the component pod.<br />More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/"
/><ApiField
  name="deploymentStrategy"
  type="[DeploymentStrategy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#deploymentstrategy-v1-apps)"
  required="false"
  description="The deployment strategy to use to replace existing pods with new ones. Only applicable for raw deployment mode."
/><ApiField
  name="storageUris"
  type="[StorageUri](#storageuri) array"
  required="true"
  description="Spec for multiple storage uris."
/>


#### TransitionStatus

**Underlying type:** string

**Appears in:**
- [ModelStatus](#modelstatus)

TransitionStatus enum



##### Possible Values

<ApiField
  name="UpToDate"
  type="enum"
  required="false"
  description="Predictor is up-to-date (reflects current spec)<br />"
/><ApiField
  name="InProgress"
  type="enum"
  required="false"
  description="Waiting for target model to reach state of active model<br />"
/><ApiField
  name="BlockedByFailedLoad"
  type="enum"
  required="false"
  description="Target model failed to load<br />"
/><ApiField
  name="InvalidSpec"
  type="enum"
  required="false"
  description="Target predictor spec failed validation<br />"
/>
#### TritonSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

TritonSpec defines arguments for configuring Triton model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


#### WorkerSpec



**Appears in:**
- [PredictorSpec](#predictorspec)



##### Fields



<ApiField
  name="volumes"
  type="[Volume](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volume-v1-core) array"
  required="false"
  description="List of volumes that can be mounted by containers belonging to the pod.<br />More info: https://kubernetes.io/docs/concepts/storage/volumes"
/><ApiField
  name="initContainers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of initialization containers belonging to the pod.<br />Init containers are executed in order prior to containers being started. If any<br />init container fails, the pod is considered to have failed and is handled according<br />to its restartPolicy. The name for an init container or normal container must be<br />unique among all containers.<br />Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes.<br />The resourceRequirements of an init container are taken into account during scheduling<br />by finding the highest request/limit for each resource type, and then using the max of<br />that value or the sum of the normal containers. Limits are applied to init containers<br />in a similar fashion.<br />Init containers cannot currently be added or removed.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/"
/><ApiField
  name="containers"
  type="[Container](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#container-v1-core) array"
  required="true"
  description="List of containers belonging to the pod.<br />Containers cannot currently be added or removed.<br />There must be at least one container in a Pod.<br />Cannot be updated."
/><ApiField
  name="ephemeralContainers"
  type="[EphemeralContainer](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#ephemeralcontainer-v1-core) array"
  required="false"
  description="List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing<br />pod to perform user-initiated actions such as debugging. This list cannot be specified when<br />creating a pod, and it cannot be modified by updating the pod spec. In order to add an<br />ephemeral container to an existing pod, use the pod's ephemeralcontainers subresource."
/><ApiField
  name="restartPolicy"
  type="[RestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#restartpolicy-v1-core)"
  required="false"
  description="Restart policy for all containers within the pod.<br />One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted.<br />Default to Always.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy"
/><ApiField
  name="terminationGracePeriodSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request.<br />Value must be non-negative integer. The value zero indicates stop immediately via<br />the kill signal (no opportunity to shut down).<br />If this value is nil, the default grace period will be used instead.<br />The grace period is the duration in seconds after the processes running in the pod are sent<br />a termination signal and the time when the processes are forcibly halted with a kill signal.<br />Set this value longer than the expected cleanup time for your process.<br />Defaults to 30 seconds."
/><ApiField
  name="activeDeadlineSeconds"
  type="integer"
  required="false"
  description="Optional duration in seconds the pod may be active on the node relative to<br />StartTime before the system will actively try to mark it failed and kill associated containers.<br />Value must be a positive integer."
/><ApiField
  name="dnsPolicy"
  type="[DNSPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#dnspolicy-v1-core)"
  required="false"
  description="Set DNS policy for the pod.<br />Defaults to &quot;ClusterFirst&quot;.<br />Valid values are 'ClusterFirstWithHostNet', 'ClusterFirst', 'Default' or 'None'.<br />DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy.<br />To have DNS options set along with hostNetwork, you have to specify DNS policy<br />explicitly to 'ClusterFirstWithHostNet'."
/><ApiField
  name="nodeSelector"
  type="object (keys:string, values:string)"
  required="false"
  description="NodeSelector is a selector which must be true for the pod to fit on a node.<br />Selector which must match a node's labels for the pod to be scheduled on that node.<br />More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
/><ApiField
  name="serviceAccountName"
  type="string"
  required="false"
  description="ServiceAccountName is the name of the ServiceAccount to use to run this pod.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/"
/><ApiField
  name="serviceAccount"
  type="string"
  required="false"
  description="DeprecatedServiceAccount is a deprecated alias for ServiceAccountName.<br />Deprecated: Use serviceAccountName instead."
/><ApiField
  name="automountServiceAccountToken"
  type="boolean"
  required="false"
  description="AutomountServiceAccountToken indicates whether a service account token should be automatically mounted."
/><ApiField
  name="nodeName"
  type="string"
  required="false"
  description="NodeName indicates in which node this pod is scheduled.<br />If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName.<br />Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod.<br />This field should not be used to express a desire for the pod to be scheduled on a specific node.<br />https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename"
/><ApiField
  name="hostNetwork"
  type="boolean"
  required="false"
  description="Host networking requested for this pod. Use the host's network namespace.<br />If this option is set, the ports that will be used must be specified.<br />Default to false."
/><ApiField
  name="hostPID"
  type="boolean"
  required="false"
  description="Use the host's pid namespace.<br />Optional: Default to false."
/><ApiField
  name="hostIPC"
  type="boolean"
  required="false"
  description="Use the host's ipc namespace.<br />Optional: Default to false."
/><ApiField
  name="shareProcessNamespace"
  type="boolean"
  required="false"
  description="Share a single process namespace between all of the containers in a pod.<br />When this is set containers will be able to view and signal processes from other containers<br />in the same pod, and the first process in each container will not be assigned PID 1.<br />HostPID and ShareProcessNamespace cannot both be set.<br />Optional: Default to false."
/><ApiField
  name="securityContext"
  type="[PodSecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podsecuritycontext-v1-core)"
  required="false"
  description="SecurityContext holds pod-level security attributes and common container settings.<br />Optional: Defaults to empty.  See type description for default values of each field."
/><ApiField
  name="imagePullSecrets"
  type="[LocalObjectReference](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#localobjectreference-v1-core) array"
  required="false"
  description="ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.<br />If specified, these secrets will be passed to individual puller implementations for them to use.<br />More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
/><ApiField
  name="hostname"
  type="string"
  required="false"
  description="Specifies the hostname of the Pod<br />If not specified, the pod's hostname will be set to a system-defined value."
/><ApiField
  name="subdomain"
  type="string"
  required="false"
  description="If specified, the fully qualified Pod hostname will be &quot;&lt;hostname&gt;.&lt;subdomain&gt;.&lt;pod namespace&gt;.svc.&lt;cluster domain&gt;&quot;.<br />If not specified, the pod will not have a domainname at all."
/><ApiField
  name="affinity"
  type="[Affinity](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#affinity-v1-core)"
  required="false"
  description="If specified, the pod's scheduling constraints"
/><ApiField
  name="schedulerName"
  type="string"
  required="false"
  description="If specified, the pod will be dispatched by specified scheduler.<br />If not specified, the pod will be dispatched by default scheduler."
/><ApiField
  name="tolerations"
  type="[Toleration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#toleration-v1-core) array"
  required="false"
  description="If specified, the pod's tolerations."
/><ApiField
  name="hostAliases"
  type="[HostAlias](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#hostalias-v1-core) array"
  required="false"
  description="HostAliases is an optional list of hosts and IPs that will be injected into the pod's hosts<br />file if specified."
/><ApiField
  name="priorityClassName"
  type="string"
  required="false"
  description="If specified, indicates the pod's priority. &quot;system-node-critical&quot; and<br />&quot;system-cluster-critical&quot; are two special keywords which indicate the<br />highest priorities with the former being the highest priority. Any other<br />name must be defined by creating a PriorityClass object with that name.<br />If not specified, the pod priority will be default or zero if there is no<br />default."
/><ApiField
  name="priority"
  type="integer"
  required="false"
  description="The priority value. Various system components use this field to find the<br />priority of the pod. When Priority Admission Controller is enabled, it<br />prevents users from setting this field. The admission controller populates<br />this field from PriorityClassName.<br />The higher the value, the higher the priority."
/><ApiField
  name="dnsConfig"
  type="[PodDNSConfig](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#poddnsconfig-v1-core)"
  required="false"
  description="Specifies the DNS parameters of a pod.<br />Parameters specified here will be merged to the generated DNS<br />configuration based on DNSPolicy."
/><ApiField
  name="readinessGates"
  type="[PodReadinessGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podreadinessgate-v1-core) array"
  required="false"
  description="If specified, all readiness gates will be evaluated for pod readiness.<br />A pod is ready when all its containers are ready AND<br />all conditions specified in the readiness gates have status equal to &quot;True&quot;<br />More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates"
/><ApiField
  name="runtimeClassName"
  type="string"
  required="false"
  description="RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used<br />to run this pod.  If no RuntimeClass resource matches the named class, the pod will not be run.<br />If unset or empty, the &quot;legacy&quot; RuntimeClass will be used, which is an implicit class with an<br />empty definition that uses the default runtime handler.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class"
/><ApiField
  name="enableServiceLinks"
  type="boolean"
  required="false"
  description="EnableServiceLinks indicates whether information about services should be injected into pod's<br />environment variables, matching the syntax of Docker links.<br />Optional: Defaults to true."
/><ApiField
  name="preemptionPolicy"
  type="[PreemptionPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#preemptionpolicy-v1-core)"
  required="false"
  description="PreemptionPolicy is the Policy for preempting pods with lower priority.<br />One of Never, PreemptLowerPriority.<br />Defaults to PreemptLowerPriority if unset."
/><ApiField
  name="overhead"
  type="[ResourceList](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcelist-v1-core)"
  required="false"
  description="Overhead represents the resource overhead associated with running a pod for a given RuntimeClass.<br />This field will be autopopulated at admission time by the RuntimeClass admission controller. If<br />the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests.<br />The RuntimeClass admission controller will reject Pod create requests which have the overhead already<br />set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value<br />defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero.<br />More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md"
/><ApiField
  name="topologySpreadConstraints"
  type="[TopologySpreadConstraint](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#topologyspreadconstraint-v1-core) array"
  required="false"
  description="TopologySpreadConstraints describes how a group of pods ought to spread across topology<br />domains. Scheduler will schedule pods in a way which abides by the constraints.<br />All topologySpreadConstraints are ANDed."
/><ApiField
  name="setHostnameAsFQDN"
  type="boolean"
  required="false"
  description="If true the pod's hostname will be configured as the pod's FQDN, rather than the leaf name (the default).<br />In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname).<br />In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\SYSTEM\\CurrentControlSet\\Services\\Tcpip\\Parameters to FQDN.<br />If a pod does not have FQDN, this has no effect.<br />Default to false."
/><ApiField
  name="os"
  type="[PodOS](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podos-v1-core)"
  required="false"
  description="Specifies the OS of the containers in the pod.<br />Some pod and container fields are restricted if this is set.<br />If the OS field is set to linux, the following fields must be unset:<br />-securityContext.windowsOptions<br />If the OS field is set to windows, following fields must be unset:<br />- spec.hostPID<br />- spec.hostIPC<br />- spec.hostUsers<br />- spec.securityContext.appArmorProfile<br />- spec.securityContext.seLinuxOptions<br />- spec.securityContext.seccompProfile<br />- spec.securityContext.fsGroup<br />- spec.securityContext.fsGroupChangePolicy<br />- spec.securityContext.sysctls<br />- spec.shareProcessNamespace<br />- spec.securityContext.runAsUser<br />- spec.securityContext.runAsGroup<br />- spec.securityContext.supplementalGroups<br />- spec.securityContext.supplementalGroupsPolicy<br />- spec.containers[*].securityContext.appArmorProfile<br />- spec.containers[*].securityContext.seLinuxOptions<br />- spec.containers[*].securityContext.seccompProfile<br />- spec.containers[*].securityContext.capabilities<br />- spec.containers[*].securityContext.readOnlyRootFilesystem<br />- spec.containers[*].securityContext.privileged<br />- spec.containers[*].securityContext.allowPrivilegeEscalation<br />- spec.containers[*].securityContext.procMount<br />- spec.containers[*].securityContext.runAsUser<br />- spec.containers[*].securityContext.runAsGroup"
/><ApiField
  name="hostUsers"
  type="boolean"
  required="false"
  description="Use the host's user namespace.<br />Optional: Default to true.<br />If set to true or not present, the pod will be run in the host user namespace, useful<br />for when the pod needs a feature only available to the host user namespace, such as<br />loading a kernel module with CAP_SYS_MODULE.<br />When set to false, a new userns is created for the pod. Setting false is useful for<br />mitigating container breakout vulnerabilities even allowing users to run their<br />containers as root without actually having root privileges on the host.<br />This field is alpha-level and is only honored by servers that enable the UserNamespacesSupport feature."
/><ApiField
  name="schedulingGates"
  type="[PodSchedulingGate](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podschedulinggate-v1-core) array"
  required="false"
  description="SchedulingGates is an opaque list of values that if specified will block scheduling the pod.<br />If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the<br />scheduler will not attempt to schedule the pod.<br />SchedulingGates can only be set at pod creation time, and be removed only afterwards."
/><ApiField
  name="resourceClaims"
  type="[PodResourceClaim](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#podresourceclaim-v1-core) array"
  required="false"
  description="ResourceClaims defines which ResourceClaims must be allocated<br />and reserved before the Pod is allowed to start. The resources<br />will be made available to those containers which consume them<br />by name.<br />This is an alpha field and requires enabling the<br />DynamicResourceAllocation feature gate.<br />This field is immutable."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Resources is the total amount of CPU and Memory resources required by all<br />containers in the pod. It supports specifying Requests and Limits for<br />&quot;cpu&quot; and &quot;memory&quot; resource names only. ResourceClaims are not supported.<br />This field enables fine-grained control over resource allocation for the<br />entire pod, allowing resource sharing among containers in a pod.<br />This is an alpha field and requires enabling the PodLevelResources feature<br />gate."
/><ApiField
  name="hostnameOverride"
  type="string"
  required="false"
  description="HostnameOverride specifies an explicit override for the pod's hostname as perceived by the pod.<br />This field only specifies the pod's hostname and does not affect its DNS records.<br />When this field is set to a non-empty string:<br />- It takes precedence over the values set in `hostname` and `subdomain`.<br />- The Pod's hostname will be set to this value.<br />- `setHostnameAsFQDN` must be nil or set to false.<br />- `hostNetwork` must be set to false.<br />This field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters.<br />Requires the HostnameOverride feature gate to be enabled."
/><ApiField
  name="pipelineParallelSize"
  type="integer"
  required="false"
  description="PipelineParallelSize defines the number of parallel workers.<br />It also represents the number of replicas in the worker set, where each worker set serves as a scaling unit."
/><ApiField
  name="tensorParallelSize"
  type="integer"
  required="false"
  description="TensorParallelSize specifies the number of GPUs to be used per node.<br />It indicates the degree of parallelism for tensor computations across the available GPUs."
/>


#### XGBoostSpec



**Appears in:**
- [PredictorSpec](#predictorspec)

XGBoostSpec defines arguments for configuring XGBoost model serving.

##### Fields



<ApiField
  name="storageUri"
  type="string"
  required="false"
  description="This field points to the location of the trained model which is mounted onto the pod."
/><ApiField
  name="runtimeVersion"
  type="string"
  required="false"
  description="Runtime version of the predictor docker image"
/><ApiField
  name="protocolVersion"
  type="[InferenceServiceProtocol](#inferenceserviceprotocol)"
  required="false"
  description="Protocol version to use by the predictor (i.e. v1 or v2 or grpc-v1 or grpc-v2)"
/><ApiField
  name="name"
  type="string"
  required="true"
  description="Name of the container specified as a DNS_LABEL.<br />Each container in a pod must have a unique name (DNS_LABEL).<br />Cannot be updated."
/><ApiField
  name="image"
  type="string"
  required="false"
  description="Container image name.<br />More info: https://kubernetes.io/docs/concepts/containers/images<br />This field is optional to allow higher level config management to default or override<br />container images in workload controllers like Deployments and StatefulSets."
/><ApiField
  name="command"
  type="string array"
  required="false"
  description="Entrypoint array. Not executed within a shell.<br />The container image's ENTRYPOINT is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="args"
  type="string array"
  required="false"
  description="Arguments to the entrypoint.<br />The container image's CMD is used if this is not provided.<br />Variable references $(VAR_NAME) are expanded using the container's environment. If a variable<br />cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced<br />to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. &quot;$$(VAR_NAME)&quot; will<br />produce the string literal &quot;$(VAR_NAME)&quot;. Escaped references will never be expanded, regardless<br />of whether the variable exists or not. Cannot be updated.<br />More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell"
/><ApiField
  name="workingDir"
  type="string"
  required="false"
  description="Container's working directory.<br />If not specified, the container runtime's default will be used, which<br />might be configured in the container image.<br />Cannot be updated."
/><ApiField
  name="ports"
  type="[ContainerPort](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerport-v1-core) array"
  required="false"
  description="List of ports to expose from the container. Not specifying a port here<br />DOES NOT prevent that port from being exposed. Any port which is<br />listening on the default &quot;0.0.0.0&quot; address inside a container will be<br />accessible from the network.<br />Modifying this array with strategic merge patch may corrupt the data.<br />For more information See https://github.com/kubernetes/kubernetes/issues/108255.<br />Cannot be updated."
/><ApiField
  name="envFrom"
  type="[EnvFromSource](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envfromsource-v1-core) array"
  required="false"
  description="List of sources to populate environment variables in the container.<br />The keys defined within a source may consist of any printable ASCII characters except '='.<br />When a key exists in multiple<br />sources, the value associated with the last source will take precedence.<br />Values defined by an Env with a duplicate key will take precedence.<br />Cannot be updated."
/><ApiField
  name="env"
  type="[EnvVar](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#envvar-v1-core) array"
  required="false"
  description="List of environment variables to set in the container.<br />Cannot be updated."
/><ApiField
  name="resources"
  type="[ResourceRequirements](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#resourcerequirements-v1-core)"
  required="false"
  description="Compute Resources required by this container.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
/><ApiField
  name="resizePolicy"
  type="[ContainerResizePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerresizepolicy-v1-core) array"
  required="false"
  description="Resources resize policy for the container."
/><ApiField
  name="restartPolicy"
  type="[ContainerRestartPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartpolicy-v1-core)"
  required="false"
  description="RestartPolicy defines the restart behavior of individual containers in a pod.<br />This overrides the pod-level restart policy. When this field is not specified,<br />the restart behavior is defined by the Pod's restart policy and the container type.<br />Additionally, setting the RestartPolicy as &quot;Always&quot; for the init container will<br />have the following effect:<br />this init container will be continually restarted on<br />exit until all regular containers have terminated. Once all regular<br />containers have completed, all init containers with restartPolicy &quot;Always&quot;<br />will be shut down. This lifecycle differs from normal init containers and<br />is often referred to as a &quot;sidecar&quot; container. Although this init<br />container still starts in the init container sequence, it does not wait<br />for the container to complete before proceeding to the next init<br />container. Instead, the next init container starts immediately after this<br />init container is started, or after any startupProbe has successfully<br />completed."
/><ApiField
  name="restartPolicyRules"
  type="[ContainerRestartRule](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#containerrestartrule-v1-core) array"
  required="false"
  description="Represents a list of rules to be checked to determine if the<br />container should be restarted on exit. The rules are evaluated in<br />order. Once a rule matches a container exit condition, the remaining<br />rules are ignored. If no rule matches the container exit condition,<br />the Container-level restart policy determines the whether the container<br />is restarted or not. Constraints on the rules:<br />- At most 20 rules are allowed.<br />- Rules can have the same action.<br />- Identical rules are not forbidden in validations.<br />When rules are specified, container MUST set RestartPolicy explicitly<br />even it if matches the Pod's RestartPolicy."
/><ApiField
  name="volumeMounts"
  type="[VolumeMount](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumemount-v1-core) array"
  required="false"
  description="Pod volumes to mount into the container's filesystem.<br />Cannot be updated."
/><ApiField
  name="volumeDevices"
  type="[VolumeDevice](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#volumedevice-v1-core) array"
  required="false"
  description="volumeDevices is the list of block devices to be used by the container."
/><ApiField
  name="livenessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container liveness.<br />Container will be restarted if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="readinessProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="Periodic probe of container service readiness.<br />Container will be removed from service endpoints if the probe fails.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="startupProbe"
  type="[Probe](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#probe-v1-core)"
  required="false"
  description="StartupProbe indicates that the Pod has successfully initialized.<br />If specified, no other probes are executed until this completes successfully.<br />If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.<br />This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,<br />when it might take a long time to load data or warm a cache, than during steady-state operation.<br />This cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
/><ApiField
  name="lifecycle"
  type="[Lifecycle](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#lifecycle-v1-core)"
  required="false"
  description="Actions that the management system should take in response to container lifecycle events.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePath"
  type="string"
  required="false"
  description="Optional: Path at which the file to which the container's termination message<br />will be written is mounted into the container's filesystem.<br />Message written is intended to be brief final status, such as an assertion failure message.<br />Will be truncated by the node if greater than 4096 bytes. The total message length across<br />all containers will be limited to 12kb.<br />Defaults to /dev/termination-log.<br />Cannot be updated."
/><ApiField
  name="terminationMessagePolicy"
  type="[TerminationMessagePolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#terminationmessagepolicy-v1-core)"
  required="false"
  description="Indicate how the termination message should be populated. File will use the contents of<br />terminationMessagePath to populate the container status message on both success and failure.<br />FallbackToLogsOnError will use the last chunk of container log output if the termination<br />message file is empty and the container exited with an error.<br />The log output is limited to 2048 bytes or 80 lines, whichever is smaller.<br />Defaults to File.<br />Cannot be updated."
/><ApiField
  name="imagePullPolicy"
  type="[PullPolicy](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#pullpolicy-v1-core)"
  required="false"
  description="Image pull policy.<br />One of Always, Never, IfNotPresent.<br />Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.<br />Cannot be updated.<br />More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
/><ApiField
  name="securityContext"
  type="[SecurityContext](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.31/#securitycontext-v1-core)"
  required="false"
  description="SecurityContext defines the security options the container should be run with.<br />If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.<br />More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
/><ApiField
  name="stdin"
  type="boolean"
  required="false"
  description="Whether this container should allocate a buffer for stdin in the container runtime. If this<br />is not set, reads from stdin in the container will always result in EOF.<br />Default is false."
/><ApiField
  name="stdinOnce"
  type="boolean"
  required="false"
  description="Whether the container runtime should close the stdin channel after it has been opened by<br />a single attach. When stdin is true the stdin stream will remain open across multiple attach<br />sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the<br />first client attaches to stdin, and then remains open and accepts data until the client disconnects,<br />at which time stdin is closed and remains closed until the container is restarted. If this<br />flag is false, a container processes that reads from stdin will never receive an EOF.<br />Default is false"
/><ApiField
  name="tty"
  type="boolean"
  required="false"
  description="Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.<br />Default is false."
/><ApiField
  name="storage"
  type="[ModelStorageSpec](#modelstoragespec)"
  required="false"
  description="Storage Spec for model location"
/>


